/* AUTO-GENERATED by scripts/build-index.py — do not edit by hand.
   Re-run after changing notes:  python3 scripts/build-index.py */
window.KC_DATA = {
  "generated": "2026-06-10",
  "layers": [
    "foundations",
    "api",
    "agent-sdk",
    "claude-code",
    "patterns",
    "products"
  ],
  "layerLabels": {
    "foundations": "Foundations",
    "api": "Claude API",
    "agent-sdk": "Agent SDK",
    "claude-code": "Claude Code",
    "patterns": "Agent Patterns",
    "products": "Products & Consulting"
  },
  "concepts": [
    {
      "slug": "adversarial-verification",
      "title": "Adversarial verification",
      "kind": "pattern",
      "layer": "patterns",
      "summary": "Spawning independent skeptics whose job is to refute a finding, and keeping it only if it survives. The pattern that stops plausible-but-wrong results from passing as true.",
      "sources": [
        "dynamic-workflow-patterns",
        "completion-is-externalized"
      ],
      "html": "<p class=\"kc-lede\">Spawning independent skeptics whose job is to refute a finding, and keeping it only if it survives. The pattern that stops plausible-but-wrong results from passing as true.</p>\n<p>A single agent grading its own work is unreliable. Give the check to fresh agents prompted to break the claim, and require a majority to fail it before you trust it.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"workflow-patterns\">Dynamic-workflow patterns (the six)</a> — <span class=\"kc-why\">It's one of the core dynamic-workflow patterns.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"externalized-completion\">Externalized completion</a> — <span class=\"kc-why\">Verification is how 'done' gets decided by something other than the agent itself.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns and the three failure modes they fix</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">The agent must never declare its own victory — externalize \"done\" into the harness</a></li></ul>",
      "degree": 2
    },
    {
      "slug": "agent-harness",
      "title": "Agent Harness (the six organs)",
      "kind": "concept",
      "layer": "foundations",
      "summary": "The anatomy of any working agent — the model, tools, a control loop, memory, a verifier, and a way to declare done. Name the six organs and you can dissect Claude Code, the SDK, or any framework the same way.",
      "sources": [
        "anatomy-of-an-agent-harness",
        "harness-engineering-discipline"
      ],
      "html": "<p class=\"kc-lede\">The anatomy of any working agent — the model, tools, a control loop, memory, a verifier, and a way to declare done. Name the six organs and you can dissect Claude Code, the SDK, or any framework the same way.</p>\n<p>Every higher layer is a specific, opinionated harness. Claude Code is a harness; the Agent SDK lets you build one; the API gives you the organs as raw callables.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">runs on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"agent-loop\">Gather → Act → Verify loop</a> — <span class=\"kc-why\">The harness is the machinery that drives the gather→act→verify loop.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"context-window\">Context Window</a> — <span class=\"kc-why\">Memory and tool results all flow through the window.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "agent-loop",
      "title": "Gather → Act → Verify loop",
      "kind": "concept",
      "layer": "foundations",
      "summary": "The single loop every agent runs — gather context, take an action (often a tool call), verify the result, repeat until done. Every layer above is just this loop wrapped at a higher level of abstraction.",
      "sources": [
        "anatomy-of-an-agent-harness",
        "agentic-ai-reference-architecture"
      ],
      "html": "<p class=\"kc-lede\">The single loop every agent runs — gather context, take an action (often a tool call), verify the result, repeat until done. Every layer above is just this loop wrapped at a higher level of abstraction.</p>\n<p>This is the physics of the whole stack. Whether it's a raw API call, the SDK, or Claude Code orchestrating subagents, the same gather→act→verify cycle is running — only the wrapper changes. Learn to see it everywhere and the stack stops looking like 40 unrelated features.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"context-window\">Context Window</a> — <span class=\"kc-why\">Each turn reads from and writes back into the context window.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI System — Reference Architecture</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "agent-sdk",
      "title": "Claude Agent SDK",
      "kind": "system",
      "layer": "agent-sdk",
      "summary": "A library that wraps the raw API loop into a framework — sessions, tools, hooks, subagents, and permissions — so you can build your own harness on the same engine Claude Code runs on. The \"build it yourself\" tier.",
      "sources": [
        "claude-agent-sdk"
      ],
      "html": "<p class=\"kc-lede\">A library that wraps the raw API loop into a framework — sessions, tools, hooks, subagents, and permissions — so you can build your own harness on the same engine Claude Code runs on. The \"build it yourself\" tier.</p>\n<p>Reach for the SDK when Claude Code is too opinionated but the raw API is too bare. It gives you the harness organs as composable parts instead of a finished product.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">runs on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"messages-api\">Messages API</a> — <span class=\"kc-why\">The SDK drives the Messages API loop for you.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"tool-use\">Tool Use</a> — <span class=\"kc-why\">It manages the tool-call round trip as a first-class concept.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"mcp\">Model Context Protocol (MCP)</a> — <span class=\"kc-why\">It connects to MCP servers to extend the agent's toolset.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK — build your own harness on the same engine as Claude Code</a></li></ul>",
      "degree": 9
    },
    {
      "slug": "agent-teams",
      "title": "Agent teams",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "Multiple independent Claude Code sessions that coordinate — they message each other and share a task list, rather than a parent spawning workers that only report back. The \"peers collaborating\" model, distinct from subagents.",
      "sources": [],
      "html": "<p class=\"kc-lede\">Multiple independent Claude Code sessions that coordinate — they message each other and share a task list, rather than a parent spawning workers that only report back. The \"peers collaborating\" model, distinct from subagents.</p>\n<p>Reach for subagents when you want delegation (fan out, collect results); reach for agent teams when you want collaboration (independent sessions working a shared task list and talking to each other). Docs: https://code.claude.com/docs/en/agent-teams</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Agent teams are a Claude Code coordination feature.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"execution-isolation\">Execution-context isolation</a> — <span class=\"kc-why\">Each team member runs in its own independent context.</span></li></ul>\n<div class=\"kc-relhead\">Related</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">alternative to</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-subagents\">Subagents / Task (Claude Code)</a> — <span class=\"kc-why\">Subagents fork from one session and report back; agent teams are independent sessions that coordinate as peers.</span></li></ul>",
      "degree": 3
    },
    {
      "slug": "batch-api",
      "title": "Batch API",
      "kind": "primitive",
      "layer": "api",
      "summary": "Submitting many independent requests as one asynchronous job at a large discount, for work that doesn't need an immediate answer. The right tool for offline evals, bulk extraction, and dataset generation.",
      "sources": [
        "claude-api-agent-primitives"
      ],
      "html": "<p class=\"kc-lede\">Submitting many independent requests as one asynchronous job at a large discount, for work that doesn't need an immediate answer. The right tool for offline evals, bulk extraction, and dataset generation.</p>\n<p>When throughput matters more than latency, batching trades a few hours of wait for a steep price cut — ideal for grading an eval suite or processing a backlog.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"messages-api\">Messages API</a> — <span class=\"kc-why\">A batch is a queue of ordinary Messages API calls run asynchronously.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a></li></ul>",
      "degree": 1
    },
    {
      "slug": "buy-vs-build",
      "title": "Buy-vs-build (the four-altitude decision)",
      "kind": "pattern",
      "layer": "patterns",
      "summary": "The decision of how much harness to own — buy a product, use Claude Code, configure the SDK, or build on the raw API. Pick the highest altitude that still meets the requirement, and drop only when forced.",
      "sources": [
        "claude-code-vs-build-your-own",
        "harness-engineering-discipline"
      ],
      "html": "<p class=\"kc-lede\">The decision of how much harness to own — buy a product, use Claude Code, configure the SDK, or build on the raw API. Pick the highest altitude that still meets the requirement, and drop only when forced.</p>\n<p>Most teams build too low. Start at the top — can a product or Claude Code do it? — and only descend a floor when a hard constraint makes you. Each floor down multiplies the harness you now own.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">'Use Claude Code as-is' is the default altitude.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"agent-sdk\">Claude Agent SDK</a> — <span class=\"kc-why\">'Configure your own harness' drops to the SDK.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"messages-api\">Messages API</a> — <span class=\"kc-why\">'Build from scratch' drops all the way to the raw API.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own harness — the four-altitude decision</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a></li></ul>",
      "degree": 4
    },
    {
      "slug": "cc-hooks",
      "title": "Hooks (Claude Code)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "User-defined handlers — shell commands, HTTP endpoints, or LLM prompts — that Claude Code fires automatically at lifecycle points (before a tool call, after an edit, on stop). How you enforce policy (format, lint, block a path) the agent can't skip, because it isn't the model's choice.",
      "sources": [
        "claude-code-architecture"
      ],
      "html": "<p class=\"kc-lede\">User-defined handlers — shell commands, HTTP endpoints, or LLM prompts — that Claude Code fires automatically at lifecycle points (before a tool call, after an edit, on stop). How you enforce policy (format, lint, block a path) the agent can't skip, because it isn't the model's choice.</p>\n<p>If you find yourself reminding the agent to do the same thing every time, that thing belongs in a hook. The harness executes it; the model can't forget it.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Hooks are a Claude Code automation surface configured in settings.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></li></ul>",
      "degree": 4
    },
    {
      "slug": "cc-mcp-servers",
      "title": "MCP servers (Claude Code)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "External tool/data integrations connected to Claude Code over MCP — GitHub, databases, design tools, internal APIs. How you give the harness capabilities Anthropic didn't ship.",
      "sources": [
        "model-context-protocol",
        "claude-code-architecture"
      ],
      "html": "<p class=\"kc-lede\">External tool/data integrations connected to Claude Code over MCP — GitHub, databases, design tools, internal APIs. How you give the harness capabilities Anthropic didn't ship.</p>\n<p>When the agent can't do something, the answer is usually \"connect an MCP server,\" not \"write a custom tool from scratch.\" Integrations written once travel across the whole stack.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">MCP servers plug into Claude Code's tool surface.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"mcp\">Model Context Protocol (MCP)</a> — <span class=\"kc-why\">They speak the Model Context Protocol to expose tools to the model.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol (MCP) — From Zero to Plumbing</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></li></ul>",
      "degree": 5
    },
    {
      "slug": "cc-subagents",
      "title": "Subagents / Task (Claude Code)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "Claude Code's way of delegating a scoped job to a fresh agent with its own context — used for parallel search, isolated edits, and review. The concrete handle you use to fan work out and keep your main context clean.",
      "sources": [
        "claude-code-multi-agent-development",
        "execution-context-isolation"
      ],
      "html": "<p class=\"kc-lede\">Claude Code's way of delegating a scoped job to a fresh agent with its own context — used for parallel search, isolated edits, and review. The concrete handle you use to fan work out and keep your main context clean.</p>\n<p>Delegate the noisy work — broad greps, log trawls — to a subagent and keep only its findings. This is how a single session takes on tasks too big for one context.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Subagents are a core Claude Code primitive — each runs in its own context window within a single session.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">One shared context vs. many isolated contexts — what actually forks in Claude Code</a></li></ul>",
      "degree": 8
    },
    {
      "slug": "checkpointing",
      "title": "Checkpointing",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "Rewind the file changes Claude made — undo back to a prior state when an edit goes wrong. The safety net that lets you run with looser permissions because a bad change is reversible.",
      "sources": [],
      "html": "<p class=\"kc-lede\">Rewind the file changes Claude made — undo back to a prior state when an edit goes wrong. The safety net that lets you run with looser permissions because a bad change is reversible.</p>\n<p>Checkpointing changes the risk math: if you can rewind, you can let the agent move faster on edits and recover instantly when it overreaches. Docs: https://code.claude.com/docs/en/checkpointing</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Checkpointing is a built-in Claude Code safety feature.</span></li></ul>\n<div class=\"kc-relhead\">Related</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">used with</span> <a href=\"#\" class=\"kc-link\" data-slug=\"permission-modes\">Permission modes</a> — <span class=\"kc-why\">Pairs with permission modes — looser autonomy is safer when edits are one rewind away.</span></li></ul>",
      "degree": 2
    },
    {
      "slug": "claude-code",
      "title": "Claude Code (the harness)",
      "kind": "system",
      "layer": "claude-code",
      "summary": "Anthropic's finished agent harness for software work — a polished, opinionated bundle of tools, memory, subagents, and permissions built on the Agent SDK. The \"use it as-is\" tier of the stack.",
      "sources": [
        "claude-code-architecture"
      ],
      "html": "<p class=\"kc-lede\">Anthropic's finished agent harness for software work — a polished, opinionated bundle of tools, memory, subagents, and permissions built on the Agent SDK. The \"use it as-is\" tier of the stack.</p>\n<p>Everything below in this lane — CLAUDE.md, skills, subagents, worktrees, hooks — is a primitive Claude Code ships. Learn them as the standard parts catalogue of a production agent.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">runs on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"agent-sdk\">Claude Agent SDK</a> — <span class=\"kc-why\">Claude Code is built on the same engine the SDK exposes.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></li></ul>",
      "degree": 17
    },
    {
      "slug": "claude-md",
      "title": "CLAUDE.md (progressive disclosure)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "The project memory file Claude Code loads as standing context — instructions, conventions, and facts the agent should always know. It loads in full every session, so you keep it lean (~200-line target) and offload depth to path-scoped rules and skills that load on demand.",
      "sources": [
        "progressive-disclosure-instructions",
        "claude-code-architecture"
      ],
      "html": "<p class=\"kc-lede\">The project memory file Claude Code loads as standing context — instructions, conventions, and facts the agent should always know. It loads in full every session, so you keep it lean (~200-line target) and offload depth to path-scoped rules and skills that load on demand.</p>\n<p>A common misread: CLAUDE.md is <strong>not</strong> progressively disclosed — it (and its imports) load in full at launch regardless of length. Smallness is a discipline, not a mechanism: keep the core lean and push detail into <code>.claude/rules/</code> (path-scoped instruction files) and skills (load only when used). Auto-memory then saves cross-session learnings so the file doesn't have to carry everything.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">CLAUDE.md is Claude Code's built-in memory surface.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"context-engineering\">Context Engineering</a> — <span class=\"kc-why\">It is context engineering applied to a single durable file.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"progressive-disclosure-instructions\">One giant instruction file fails — structure CLAUDE.md by progressive disclosure</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "company-ai-stack",
      "title": "Company AI stack (bootstrapping)",
      "kind": "concept",
      "layer": "products",
      "summary": "Standing up an organization's AI capability from zero — discovery, picking the smallest set of primitives that meets the need, and phasing the rollout. Where the whole stack gets applied to a real business.",
      "sources": [
        "bootstrapping-company-ai-stack"
      ],
      "html": "<p class=\"kc-lede\">Standing up an organization's AI capability from zero — discovery, picking the smallest set of primitives that meets the need, and phasing the rollout. Where the whole stack gets applied to a real business.</p>\n<p>The consulting move is restraint: choose the fewest, highest-altitude primitives that solve the problem, then phase. The map below this note is the menu you're choosing from.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"buy-vs-build\">Buy-vs-build (the four-altitude decision)</a> — <span class=\"kc-why\">Every component choice is a buy-vs-build altitude decision.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"reference-architecture\">Agentic AI reference architecture</a> — <span class=\"kc-why\">The target shape is a production reference architecture.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"bootstrapping-company-ai-stack\">Scenario — bootstrapping a company's AI stack from zero</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "consulting-discovery",
      "title": "Consulting discovery",
      "kind": "concept",
      "layer": "products",
      "summary": "The upfront phase that finds where AI actually pays off — mapping workflows, naming the failure that hurts, and sizing the smallest valuable first build. The diagnosis before any prescription.",
      "sources": [
        "bootstrapping-company-ai-stack"
      ],
      "html": "<p class=\"kc-lede\">The upfront phase that finds where AI actually pays off — mapping workflows, naming the failure that hurts, and sizing the smallest valuable first build. The diagnosis before any prescription.</p>\n<p>Skip discovery and you build the wrong thing well. Start from the painful workflow and the measurable outcome, not from the technology you're excited to use.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"company-ai-stack\">Company AI stack (bootstrapping)</a> — <span class=\"kc-why\">Discovery is the first phase of bootstrapping a company's stack.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"bootstrapping-company-ai-stack\">Scenario — bootstrapping a company's AI stack from zero</a></li></ul>",
      "degree": 1
    },
    {
      "slug": "context-engineering",
      "title": "Context Engineering",
      "kind": "concept",
      "layer": "foundations",
      "summary": "Curating what the model can see at the moment it acts — the right files, tool results, memory, and retrieved facts, and nothing that wastes the window. The second lever, and where most accuracy problems actually live.",
      "sources": [
        "harness-vs-context-engineering",
        "prompt-context-harness-engineering"
      ],
      "html": "<p class=\"kc-lede\">Curating what the model can see at the moment it acts — the right files, tool results, memory, and retrieved facts, and nothing that wastes the window. The second lever, and where most accuracy problems actually live.</p>\n<p>CLAUDE.md structure, retrieval, and the repository-as-record pattern are all context engineering in practice. If the model is confidently wrong, suspect the context before the prompt.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"three-levels-of-engineering\">Three levels — prompt · context · harness</a> — <span class=\"kc-why\">The second of the three levers.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"context-window\">Context Window</a> — <span class=\"kc-why\">It is the discipline of spending the context window well.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a></li></ul>",
      "degree": 4
    },
    {
      "slug": "context-window",
      "title": "Context Window",
      "kind": "concept",
      "layer": "foundations",
      "summary": "The model's working memory — the finite token budget that holds the prompt, tools, history, and retrieved context for a single turn. It is the scarce resource every higher primitive is ultimately managing.",
      "sources": [
        "harness-vs-context-engineering",
        "prompt-context-harness-engineering"
      ],
      "html": "<p class=\"kc-lede\">The model's working memory — the finite token budget that holds the prompt, tools, history, and retrieved context for a single turn. It is the scarce resource every higher primitive is ultimately managing.</p>\n<p>Everything above — caching, sessions, subagent isolation, CLAUDE.md structure — exists to spend this budget well. When a primitive confuses you, ask \"what is it doing to the context window?\" and its purpose usually snaps into focus.</p>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a></li></ul>",
      "degree": 6
    },
    {
      "slug": "dynamic-workflows",
      "title": "Dynamic workflows (ultracode)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "A JavaScript script that orchestrates subagents at scale — Claude writes the script for the task you describe and a runtime executes it in the background (loops, parallel stages, verify steps it can't drift from). Triggered by the keyword `ultracode`. Research preview.",
      "sources": [
        "dynamic-workflows-and-ultraplan",
        "dynamic-workflow-patterns",
        "workflow-journal-resume"
      ],
      "html": "<p class=\"kc-lede\">A JavaScript script that orchestrates subagents at scale — Claude writes the script for the task you describe and a runtime executes it in the background (loops, parallel stages, verify steps it can't drift from). Triggered by the keyword <code>ultracode</code>. Research preview.</p>\n<p>This is where determinism meets fan-out: the script decides what runs in parallel, what verifies, and what synthesizes, so scale doesn't mean chaos. Runs are also journaled — <code>resumeFromRunId</code> replays finished agents from cache after a mid-run failure, which makes scripted orchestration durable, not just parallel. Note: the trigger keyword is <code>ultracode</code> (the always-on form is <code>/effort ultracode</code>) — distinct from <em>ultraplan</em>, which is a separate cloud plan-mode feature, not this. Docs: https://code.claude.com/docs/en/workflows</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Workflows are a Claude Code orchestration capability.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-subagents\">Subagents / Task (Claude Code)</a> — <span class=\"kc-why\">A workflow pipelines work across many subagents.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the \"Ultraplan\" loop</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns and the three failure modes they fix</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"workflow-journal-resume\">Workflow runs are journaled — resume replays finished agents from cache, so orchestration is durable, not just parallel</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "evals-reliability",
      "title": "Evals & Reliability",
      "kind": "concept",
      "layer": "foundations",
      "summary": "Measuring whether the agent actually works — failure-mode analysis, fixtures, and graded runs — so improvements are real and regressions are caught. Reliability is engineered and measured, not hoped for.",
      "sources": [
        "harness-engineering-discipline"
      ],
      "html": "<p class=\"kc-lede\">Measuring whether the agent actually works — failure-mode analysis, fixtures, and graded runs — so improvements are real and regressions are caught. Reliability is engineered and measured, not hoped for.</p>\n<p>Without evals you are tuning prompts in the dark. They turn \"it feels better\" into a number, and they tell the harness which failure modes are worth a deterministic guard.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering\">Harness Engineering</a> — <span class=\"kc-why\">You harden the harness against the failure modes evals expose.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a></li></ul>",
      "degree": 1
    },
    {
      "slug": "execution-isolation",
      "title": "Execution-context isolation",
      "kind": "concept",
      "layer": "claude-code",
      "summary": "The choice between one shared context and many isolated ones — what actually forks when you spawn agents. Isolation buys parallelism and a clean slate; sharing buys continuity. Knowing which you have prevents whole classes of bugs.",
      "sources": [
        "execution-context-isolation"
      ],
      "html": "<p class=\"kc-lede\">The choice between one shared context and many isolated ones — what actually forks when you spawn agents. Isolation buys parallelism and a clean slate; sharing buys continuity. Knowing which you have prevents whole classes of bugs.</p>\n<p>A subagent does not see your conversation; a worktree does not see your uncommitted files unless you ask. Most \"why didn't it know X\" surprises trace back to a boundary you forgot you crossed.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-subagents\">Subagents / Task (Claude Code)</a> — <span class=\"kc-why\">Each subagent is a forked, isolated context.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"worktrees\">Worktrees (worktree.baseRef)</a> — <span class=\"kc-why\">Worktrees isolate the filesystem the way subagents isolate context.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">One shared context vs. many isolated contexts — what actually forks in Claude Code</a></li></ul>",
      "degree": 4
    },
    {
      "slug": "externalized-completion",
      "title": "Externalized completion",
      "kind": "pattern",
      "layer": "patterns",
      "summary": "Never let the agent declare its own victory — push the definition of \"done\" into the harness as a test, a check, or a gate. The single most reliability-defining move in agent design.",
      "sources": [
        "completion-is-externalized"
      ],
      "html": "<p class=\"kc-lede\">Never let the agent declare its own victory — push the definition of \"done\" into the harness as a test, a check, or a gate. The single most reliability-defining move in agent design.</p>\n<p>An agent that decides it's finished will declare success on incomplete work. Make \"done\" something the world confirms — a green test, a satisfied condition — and the whole system gets trustworthy.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering\">Harness Engineering</a> — <span class=\"kc-why\">Completion lives in the deterministic harness, not the weights.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">The agent must never declare its own victory — externalize \"done\" into the harness</a></li></ul>",
      "degree": 5
    },
    {
      "slug": "goal-mode",
      "title": "Goal mode (/goal)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "In-session, condition-driven repetition. /goal sets a completion condition and keeps working across turns until it holds; after each turn a small fast model (Haiku by default) checks the condition and, if not met, starts another turn. The productized \"ralph loop\" — and a thin wrapper around a session-scoped prompt-based Stop hook.",
      "sources": [
        "automation-control-flow-spectrum",
        "completion-is-externalized"
      ],
      "html": "<p class=\"kc-lede\">In-session, condition-driven repetition. /goal sets a completion condition and keeps working across turns until it holds; after each turn a small fast model (Haiku by default) checks the condition and, if not met, starts another turn. The productized \"ralph loop\" — and a thin wrapper around a session-scoped prompt-based Stop hook.</p>\n<p>Reach for /goal for substantial work with a verifiable end state — \"migrate every call site until tests pass,\" \"split this file until each module is under budget.\" Write the condition as something Claude's own output can demonstrate (the evaluator reads the transcript; it doesn't run tools). Bound it with a turn/time clause and a budget, since like any loop it can run a long time.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">/goal is a Claude Code session command.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-hooks\">Hooks (Claude Code)</a> — <span class=\"kc-why\">It is literally a wrapper around a prompt-based Stop hook — it requires workspace trust and is disabled when hooks are off.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"externalized-completion\">Externalized completion</a> — <span class=\"kc-why\">A separate evaluator model decides 'done', not the agent doing the work — externalized completion baked into the product.</span></li></ul>\n<div class=\"kc-relhead\">Related</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">alternative to</span> <a href=\"#\" class=\"kc-link\" data-slug=\"scheduled-tasks\">Scheduled tasks (/loop)</a> — <span class=\"kc-why\">Both keep one session running; /goal advances when a condition holds, /loop when a clock elapses.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"automation-control-flow-spectrum\">Keeping Claude working — the two axes behind /loop, /goal, hooks, routines &amp; cron</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">The agent must never declare its own victory — externalize \"done\" into the harness</a></li></ul>",
      "degree": 5
    },
    {
      "slug": "harness-engineering",
      "title": "Harness Engineering",
      "kind": "concept",
      "layer": "foundations",
      "summary": "Building the deterministic code around the model — the loops, tools, validators, and retries — so reliability lives outside the weights. The third lever, the highest-leverage, and the most neglected.",
      "sources": [
        "harness-engineering-discipline",
        "prompt-context-harness-engineering"
      ],
      "html": "<p class=\"kc-lede\">Building the deterministic code around the model — the loops, tools, validators, and retries — so reliability lives outside the weights. The third lever, the highest-leverage, and the most neglected.</p>\n<p>The model is a probabilistic component; the harness is where you make the system as a whole dependable. Externalized completion, adversarial verification, and loop-until-done are all harness moves.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"three-levels-of-engineering\">Three levels — prompt · context · harness</a> — <span class=\"kc-why\">The third of the three levers.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"agent-harness\">Agent Harness (the six organs)</a> — <span class=\"kc-why\">Harness engineering is the practice of building the agent harness.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a></li></ul>",
      "degree": 5
    },
    {
      "slug": "loop-engineering",
      "title": "Loop Engineering",
      "kind": "pattern",
      "layer": "patterns",
      "summary": "Stop prompting the agent turn-by-turn; design a system that finds the work, hands it out, checks it, records what's done, and decides the next thing — then let it poke the agent instead of you. It's the harness put on a timer that feeds itself, built from five Claude Code blocks (scheduling, worktrees, skills, plugins+MCP, subagents) plus an on-disk memory. A 2026 practice/preview, not a settled standard.",
      "sources": [
        "designing-loops-not-prompts",
        "loop-is-cron-plus-a-decision-maker",
        "automation-control-flow-spectrum",
        "containment-vs-discrimination"
      ],
      "html": "<p class=\"kc-lede\">Stop prompting the agent turn-by-turn; design a system that finds the work, hands it out, checks it, records what's done, and decides the next thing — then let it poke the agent instead of you. It's the harness put on a timer that feeds itself, built from five Claude Code blocks (scheduling, worktrees, skills, plugins+MCP, subagents) plus an on-disk memory. A 2026 practice/preview, not a settled standard.</p>\n<p>The leverage point moved: the highest-value thing is now designing the loop, not writing each turn. The same five pieces ship in both Claude Code and Codex, so the shape outlives any one tool. But the loop changes the work, it doesn't delete you from it — verification, comprehension debt, and cognitive surrender all get <em>sharper</em> as the loop improves. Build the loop; stay the engineer.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering\">Harness Engineering</a> — <span class=\"kc-why\">A loop is the agent harness put on a timer that spawns helpers and feeds itself — it sits one floor above.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"repo-as-record\">Repository as system-of-record</a> — <span class=\"kc-why\">The loop's memory must live on disk/in the repo because the model forgets everything between runs.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"externalized-completion\">Externalized completion</a> — <span class=\"kc-why\">An unattended loop is only trustworthy if 'done' is externally verified — what /goal's separate Haiku evaluator does.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-subagents\">Subagents / Task (Claude Code)</a> — <span class=\"kc-why\">Splits the maker from the checker so a verifier the loop trusts decides 'done'.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"worktrees\">Worktrees (worktree.baseRef)</a> — <span class=\"kc-why\">Isolates parallel agents so concurrent runs don't collide on the same files.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"skills\">Skills</a> — <span class=\"kc-why\">Writes project intent down once so the loop doesn't re-derive it from zero every cycle.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-mcp-servers\">MCP servers (Claude Code)</a> — <span class=\"kc-why\">MCP servers (the source's 'connectors') let the loop act in real tools — open PRs, update tickets, post to Slack.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-hooks\">Hooks (Claude Code)</a> — <span class=\"kc-why\">Lifecycle hooks plus /loop and /goal give the loop its scheduled heartbeat.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"scheduled-tasks\">Scheduled tasks (/loop)</a> — <span class=\"kc-why\">/loop is the clock-driven heartbeat that turns one run into a recurring in-session loop.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"goal-mode\">Goal mode (/goal)</a> — <span class=\"kc-why\">/goal is the condition-driven heartbeat — keep going until a validator confirms done.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"designing-loops-not-prompts\">Loop engineering — design the system that prompts the agent, not the prompt</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"loop-is-cron-plus-a-decision-maker\">A loop is cron plus a decision-maker in the body</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"automation-control-flow-spectrum\">Keeping Claude working — the two axes behind /loop, /goal, hooks, routines &amp; cron</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"containment-vs-discrimination\">Containment vs discrimination — two ways to organize the stack, for two different questions</a></li></ul>",
      "degree": 10
    },
    {
      "slug": "loop-until-done",
      "title": "Loop-until-done",
      "kind": "pattern",
      "layer": "patterns",
      "summary": "Keep working until an external condition is met — tests pass, K rounds find nothing new, a count is reached — instead of stopping when the model feels finished. The pattern that catches the long tail simple counters miss.",
      "sources": [
        "dynamic-workflow-patterns",
        "completion-is-externalized",
        "automation-control-flow-spectrum"
      ],
      "html": "<p class=\"kc-lede\">Keep working until an external condition is met — tests pass, K rounds find nothing new, a count is reached — instead of stopping when the model feels finished. The pattern that catches the long tail simple counters miss.</p>\n<p>For unknown-size discovery (bugs, edge cases), loop until consecutive rounds come up empty. The exit gate must be a real signal — a passing test, a dry round — not the agent's say-so.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"workflow-patterns\">Dynamic-workflow patterns (the six)</a> — <span class=\"kc-why\">It's one of the core dynamic-workflow patterns.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"externalized-completion\">Externalized completion</a> — <span class=\"kc-why\">The loop's exit condition lives outside the model.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns and the three failure modes they fix</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">The agent must never declare its own victory — externalize \"done\" into the harness</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"automation-control-flow-spectrum\">Keeping Claude working — the two axes behind /loop, /goal, hooks, routines &amp; cron</a></li></ul>",
      "degree": 2
    },
    {
      "slug": "mcp",
      "title": "Model Context Protocol (MCP)",
      "kind": "primitive",
      "layer": "api",
      "summary": "An open protocol for exposing tools and data to a model through a standard server interface, so any client can plug into any tool without bespoke glue. The USB-C port of the agent world.",
      "sources": [
        "model-context-protocol",
        "claude-api-agent-primitives",
        "tool-call-taxonomy"
      ],
      "html": "<p class=\"kc-lede\">An open protocol for exposing tools and data to a model through a standard server interface, so any client can plug into any tool without bespoke glue. The USB-C port of the agent world.</p>\n<p>MCP is why an integration written once (GitHub, Slack, a database) works across Claude Code, the SDK, and the API. Learn the server/connector split and most \"how do I give the agent X\" questions answer themselves.</p>\n<div class=\"kc-relhead\">Related</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">used with</span> <a href=\"#\" class=\"kc-link\" data-slug=\"tool-use\">Tool Use</a> — <span class=\"kc-why\">MCP is its own open standard; in the Claude API its servers surface to the model as tool-use calls.</span></li><li><span class=\"kc-dep\">used with</span> <a href=\"#\" class=\"kc-link\" data-slug=\"messages-api\">Messages API</a> — <span class=\"kc-why\">Provider-neutral — it integrates with the Messages API via the MCP connector, but doesn't depend on it.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol (MCP) — From Zero to Plumbing</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"tool-call-taxonomy\">Tool call, function call, MCP, skill — one containment model</a></li></ul>",
      "degree": 4
    },
    {
      "slug": "messages-api",
      "title": "Messages API",
      "kind": "primitive",
      "layer": "api",
      "summary": "The Claude API endpoint that runs one turn of the loop — you send messages and tool definitions, the model replies with text or tool calls. The engine every harness above is ultimately calling.",
      "sources": [
        "claude-api-agent-primitives"
      ],
      "html": "<p class=\"kc-lede\">The Claude API endpoint that runs one turn of the loop — you send messages and tool definitions, the model replies with text or tool calls. The engine every harness above is ultimately calling.</p>\n<p>Everything from the SDK up is a more ergonomic way to call this endpoint in a loop. Knowing its request/response shape demystifies what \"an agent\" actually is.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"model-tiers\">Model tiers — Opus · Sonnet · Haiku</a> — <span class=\"kc-why\">Each request names a model tier (Opus / Sonnet / Haiku) to run on.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"context-window\">Context Window</a> — <span class=\"kc-why\">The request is exactly what fills the model's context window.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a></li></ul>",
      "degree": 8
    },
    {
      "slug": "model-tiers",
      "title": "Model tiers — Opus · Sonnet · Haiku",
      "kind": "primitive",
      "layer": "api",
      "summary": "The three-tier family that trades capability against speed and cost — Opus for the hardest reasoning, Sonnet for the balanced default, Haiku for fast cheap high-volume work. Choosing the tier is a first-class design decision.",
      "sources": [
        "claude-api-agent-primitives"
      ],
      "html": "<p class=\"kc-lede\">The three-tier family that trades capability against speed and cost — Opus for the hardest reasoning, Sonnet for the balanced default, Haiku for fast cheap high-volume work. Choosing the tier is a first-class design decision.</p>\n<p>Good harnesses route by difficulty: Haiku for mechanical sub-steps, Sonnet for most work, Opus for the genuinely hard call. Tier choice is one of the biggest cost/quality levers in the whole stack. Current lineup (2026-06-07): <strong>Opus 4.8</strong> (<code>claude-opus-4-8</code>, ~$5/$25 per MTok, 1M context), <strong>Sonnet 4.6</strong> (<code>claude-sonnet-4-6</code>, ~$3/$15, 1M context), <strong>Haiku 4.5</strong> (<code>claude-haiku-4-5</code>, ~$1/$5, 200K context).</p>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a></li></ul>",
      "degree": 1
    },
    {
      "slug": "orchestration",
      "title": "Multi-agent orchestration",
      "kind": "pattern",
      "layer": "patterns",
      "summary": "Coordinating many agents toward one outcome — fan-out for coverage, independent perspectives for confidence, and scale beyond a single context. Composing subagents into a reliable whole.",
      "sources": [
        "claude-code-multi-agent-development",
        "dynamic-workflow-patterns"
      ],
      "html": "<p class=\"kc-lede\">Coordinating many agents toward one outcome — fan-out for coverage, independent perspectives for confidence, and scale beyond a single context. Composing subagents into a reliable whole.</p>\n<p>The art is deciding what fans out, what verifies, and what synthesizes. More agents only help if the orchestration structure turns their output into a better answer, not just more noise.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-subagents\">Subagents / Task (Claude Code)</a> — <span class=\"kc-why\">Orchestration is built out of subagent delegation.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"execution-isolation\">Execution-context isolation</a> — <span class=\"kc-why\">It relies on isolating each agent's context to parallelize safely.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns and the three failure modes they fix</a></li></ul>",
      "degree": 4
    },
    {
      "slug": "permission-modes",
      "title": "Permission modes",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "How much Claude Code can do without asking. Six modes — default (reads only), acceptEdits, plan, auto (preview), dontAsk (pre-approved tools only), and bypassPermissions (everything) — the dial you set to match trust against task risk.",
      "sources": [
        "claude-code-architecture"
      ],
      "html": "<p class=\"kc-lede\">How much Claude Code can do without asking. Six modes — default (reads only), acceptEdits, plan, auto (preview), dontAsk (pre-approved tools only), and bypassPermissions (everything) — the dial you set to match trust against task risk.</p>\n<p>Turn the dial up for safe, repetitive work and down for anything irreversible. A denied action is feedback, not a wall — adjust the approach rather than retrying verbatim. Plan mode is itself one of these modes — read-and-propose with no edits.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Permission modes are a Claude Code safety control.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></li></ul>",
      "degree": 4
    },
    {
      "slug": "plan-mode",
      "title": "Plan mode",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "A read-only phase where Claude Code explores and proposes an approach for your approval before touching anything. The guardrail that turns \"it changed 40 files I didn't expect\" into a reviewable plan first.",
      "sources": [
        "dynamic-workflows-and-ultraplan",
        "claude-code-architecture"
      ],
      "html": "<p class=\"kc-lede\">A read-only phase where Claude Code explores and proposes an approach for your approval before touching anything. The guardrail that turns \"it changed 40 files I didn't expect\" into a reviewable plan first.</p>\n<p>Use it for anything non-trivial: explore, get sign-off on the plan, then implement. It is the cheapest way to catch a wrong mental model before code is written.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"permission-modes\">Permission modes</a> — <span class=\"kc-why\">Plan mode is one of Claude Code's permission modes — read-and-propose with no edits.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the \"Ultraplan\" loop</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></li></ul>",
      "degree": 1
    },
    {
      "slug": "plugins",
      "title": "Plugins (and marketplaces)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "The packaging layer of Claude Code — a plugin bundles skills, hooks, subagents, and MCP servers into one installable unit, distributed through marketplaces. How you share and reuse a whole extension setup instead of wiring each piece by hand.",
      "sources": [],
      "html": "<p class=\"kc-lede\">The packaging layer of Claude Code — a plugin bundles skills, hooks, subagents, and MCP servers into one installable unit, distributed through marketplaces. How you share and reuse a whole extension setup instead of wiring each piece by hand.</p>\n<p>Plugins are the \"install once, get the whole setup\" layer that sits above the individual extension primitives — the natural unit for a team or a community to ship a curated Claude Code configuration. Docs: https://code.claude.com/docs/en/plugins</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Plugins are Claude Code's packaging/distribution mechanism.</span></li></ul>\n<div class=\"kc-relhead\">Related</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">used with</span> <a href=\"#\" class=\"kc-link\" data-slug=\"skills\">Skills</a> — <span class=\"kc-why\">A plugin can bundle skills.</span></li><li><span class=\"kc-dep\">used with</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-hooks\">Hooks (Claude Code)</a> — <span class=\"kc-why\">A plugin can bundle hooks.</span></li><li><span class=\"kc-dep\">used with</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-subagents\">Subagents / Task (Claude Code)</a> — <span class=\"kc-why\">A plugin can bundle subagents.</span></li><li><span class=\"kc-dep\">used with</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-mcp-servers\">MCP servers (Claude Code)</a> — <span class=\"kc-why\">A plugin can bundle MCP server configs.</span></li></ul>",
      "degree": 5
    },
    {
      "slug": "prompt-caching",
      "title": "Prompt Caching",
      "kind": "primitive",
      "layer": "api",
      "summary": "Reusing the model's processed prefix across calls so repeated context (system prompt, tools, large files) isn't re-billed and re-processed each turn. The main lever for making long agent loops fast and cheap.",
      "sources": [
        "claude-api-agent-primitives"
      ],
      "html": "<p class=\"kc-lede\">Reusing the model's processed prefix across calls so repeated context (system prompt, tools, large files) isn't re-billed and re-processed each turn. The main lever for making long agent loops fast and cheap.</p>\n<p>The default 5-minute cache TTL is why long-running loops keep their prefix warm and why sleeping past it costs more (a 1-hour TTL option exists at extra cost). A lot of agent cost optimization is really cache-hit optimization.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"messages-api\">Messages API</a> — <span class=\"kc-why\">Caching is a feature of the Messages API request.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"context-window\">Context Window</a> — <span class=\"kc-why\">It caches a stable prefix of the context window.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a></li></ul>",
      "degree": 2
    },
    {
      "slug": "prompt-engineering",
      "title": "Prompt Engineering",
      "kind": "concept",
      "layer": "foundations",
      "summary": "Shaping what you say to the model — instructions, examples, and output format — to steer a single response. The first lever teams reach for, and the one with the lowest ceiling on reliability.",
      "sources": [
        "prompt-context-harness-engineering",
        "harness-vs-context-engineering"
      ],
      "html": "<p class=\"kc-lede\">Shaping what you say to the model — instructions, examples, and output format — to steer a single response. The first lever teams reach for, and the one with the lowest ceiling on reliability.</p>\n<p>Necessary but not sufficient: a perfect prompt still can't fix missing context or a missing verification step. Treat it as the cheapest lever, not the main one.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"three-levels-of-engineering\">Three levels — prompt · context · harness</a> — <span class=\"kc-why\">The first of the three levers.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a></li></ul>",
      "degree": 1
    },
    {
      "slug": "reference-architecture",
      "title": "Agentic AI reference architecture",
      "kind": "pattern",
      "layer": "patterns",
      "summary": "The production-system view of an agent — how routing, orchestration, memory, and verification fit together into something you'd actually ship. The blueprint that turns primitives into a system.",
      "sources": [
        "agentic-ai-reference-architecture"
      ],
      "html": "<p class=\"kc-lede\">The production-system view of an agent — how routing, orchestration, memory, and verification fit together into something you'd actually ship. The blueprint that turns primitives into a system.</p>\n<p>This is the same gather→act→verify loop zoomed out to a whole product. Use it as the checklist for what a real deployment needs beyond a clever prompt.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"orchestration\">Multi-agent orchestration</a> — <span class=\"kc-why\">Orchestration is the reference architecture's execution core.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"repo-as-record\">Repository as system-of-record</a> — <span class=\"kc-why\">The repository is its durable memory and system of record.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI System — Reference Architecture</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "repo-as-record",
      "title": "Repository as system-of-record",
      "kind": "pattern",
      "layer": "patterns",
      "summary": "Treat the repository as the agent's only durable memory — code, docs, and decisions committed so state survives across sessions and agents. If it isn't in the repo, it didn't happen.",
      "sources": [
        "repository-as-system-of-record"
      ],
      "html": "<p class=\"kc-lede\">Treat the repository as the agent's only durable memory — code, docs, and decisions committed so state survives across sessions and agents. If it isn't in the repo, it didn't happen.</p>\n<p>Agents forget everything between sessions; the repo is what they remember. Write decisions and learnings into tracked files so the next run — human or agent — starts from truth, not from scratch.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"context-engineering\">Context Engineering</a> — <span class=\"kc-why\">The repo is curated context that persists between runs.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"repository-as-system-of-record\">The repository is the agent's only durable memory — make it the system of record</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "routines",
      "title": "Routines (cloud)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "A saved Claude Code config — prompt + repos + connectors — that runs unattended on Anthropic-managed cloud infrastructure on a schedule, a GitHub event, or an API call. The \"set it and forget it, laptop closed\" tier. Runs autonomously with no permission prompts, so its reach is scoped by the repos and connectors you attach.",
      "sources": [
        "claude-code-routines",
        "automation-control-flow-spectrum",
        "loop-is-cron-plus-a-decision-maker"
      ],
      "html": "<p class=\"kc-lede\">A saved Claude Code config — prompt + repos + connectors — that runs unattended on Anthropic-managed cloud infrastructure on a schedule, a GitHub event, or an API call. The \"set it and forget it, laptop closed\" tier. Runs autonomously with no permission prompts, so its reach is scoped by the repos and connectors you attach.</p>\n<p>Interactive Claude Code keeps you in the loop approving as you go; a routine freezes the plan and tools into a saved config that a trigger pulls off the shelf and runs in the cloud. The unit of value moves from a session you drive to a standing order that runs itself. It's the durable cousin of the session-scoped <code>/loop</code> — reach for it when work must run with your machine off.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Routines are the unattended-cloud member of the Claude Code primitive set (Claude Code on the web).</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-mcp-servers\">MCP servers (Claude Code)</a> — <span class=\"kc-why\">A routine bundles connectors/MCP servers into its saved config so the cloud run can reach external systems.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-routines\">Claude Code Routines — unattended cloud automation</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"automation-control-flow-spectrum\">Keeping Claude working — the two axes behind /loop, /goal, hooks, routines &amp; cron</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"loop-is-cron-plus-a-decision-maker\">A loop is cron plus a decision-maker in the body</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "sandboxing",
      "title": "Sandboxing",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "Run the Bash tool inside a restricted sandbox with filesystem and network limits, so shell commands can't reach beyond what you allow. The enforcement substrate that makes higher autonomy safe.",
      "sources": [],
      "html": "<p class=\"kc-lede\">Run the Bash tool inside a restricted sandbox with filesystem and network limits, so shell commands can't reach beyond what you allow. The enforcement substrate that makes higher autonomy safe.</p>\n<p>Permissions answer \"may the agent do this?\"; the sandbox answers \"and how much damage can it do if it does?\" Together they let you grant more autonomy with a bounded blast radius. Docs: https://code.claude.com/docs/en/sandboxing</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Sandboxing is a Claude Code execution-safety feature.</span></li></ul>\n<div class=\"kc-relhead\">Related</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">used with</span> <a href=\"#\" class=\"kc-link\" data-slug=\"permission-modes\">Permission modes</a> — <span class=\"kc-why\">Permission modes decide what's allowed; the sandbox enforces the blast radius when it runs.</span></li></ul>",
      "degree": 2
    },
    {
      "slug": "scheduled-tasks",
      "title": "Scheduled tasks (/loop)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "In-session, clock-driven repetition. The /loop bundled skill re-runs a prompt on an interval (or a Claude-chosen delay), converting it to a cron expression via the CronCreate/CronList/CronDelete tools. Session-scoped and local — it fires between turns of an open session, inherits its permissions, and expires 7 days after creation.",
      "sources": [
        "automation-control-flow-spectrum",
        "loop-is-cron-plus-a-decision-maker",
        "designing-loops-not-prompts"
      ],
      "html": "<p class=\"kc-lede\">In-session, clock-driven repetition. The /loop bundled skill re-runs a prompt on an interval (or a Claude-chosen delay), converting it to a cron expression via the CronCreate/CronList/CronDelete tools. Session-scoped and local — it fires between turns of an open session, inherits its permissions, and expires 7 days after creation.</p>\n<p>Use it to poll a deploy, babysit a PR, or check a long build during development. It feels like cron because it uses cron syntax under the hood — but it runs a model that decides what to do each tick, fires only while the session is open and idle, and self-deletes after 7 days. For durable, laptop-closed scheduling, graduate to a routine.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Scheduled tasks are a Claude Code session feature; the cron tools are its internal scheduling API.</span></li><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"skills\">Skills</a> — <span class=\"kc-why\">/loop is a bundled skill — the same invocation surface as any other slash command.</span></li></ul>\n<div class=\"kc-relhead\">Related</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">alternative to</span> <a href=\"#\" class=\"kc-link\" data-slug=\"routines\">Routines (cloud)</a> — <span class=\"kc-why\">Same 'repeat on a clock' need; /loop is local and needs the session open, a routine runs unattended in the cloud.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"automation-control-flow-spectrum\">Keeping Claude working — the two axes behind /loop, /goal, hooks, routines &amp; cron</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"loop-is-cron-plus-a-decision-maker\">A loop is cron plus a decision-maker in the body</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"designing-loops-not-prompts\">Loop engineering — design the system that prompts the agent, not the prompt</a></li></ul>",
      "degree": 5
    },
    {
      "slug": "sdk-hooks",
      "title": "Hooks (SDK)",
      "kind": "primitive",
      "layer": "agent-sdk",
      "summary": "Callbacks the SDK fires at fixed points in the agent lifecycle — tool calls, session start/stop, subagent and prompt events, compaction — so you can allow, deny, modify, or log behavior without depending on the model choosing to comply.",
      "sources": [
        "claude-agent-sdk"
      ],
      "html": "<p class=\"kc-lede\">Callbacks the SDK fires at fixed points in the agent lifecycle — tool calls, session start/stop, subagent and prompt events, compaction — so you can allow, deny, modify, or log behavior without depending on the model choosing to comply.</p>\n<p>Hooks are harness engineering made concrete: anything that must always happen belongs in a hook, not in a prompt instruction the model might ignore.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"agent-sdk\">Claude Agent SDK</a> — <span class=\"kc-why\">Hooks are an extension point of the SDK runtime.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK — build your own harness on the same engine as Claude Code</a></li></ul>",
      "degree": 1
    },
    {
      "slug": "sdk-permissions",
      "title": "Permissions & auth (SDK)",
      "kind": "primitive",
      "layer": "agent-sdk",
      "summary": "The SDK's controls over what tools an agent may use and what needs approval — permission modes, allow/deny rules, and the canUseTool callback for everything else. The boundary that makes an autonomous agent safe to run. (Authentication is configured separately.)",
      "sources": [
        "claude-agent-sdk"
      ],
      "html": "<p class=\"kc-lede\">The SDK's controls over what tools an agent may use and what needs approval — permission modes, allow/deny rules, and the canUseTool callback for everything else. The boundary that makes an autonomous agent safe to run. (Authentication is configured separately.)</p>\n<p>Autonomy without a permission boundary is how agents do damage. Decide up front what is auto-allowed, what prompts, and what is forbidden.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"agent-sdk\">Claude Agent SDK</a> — <span class=\"kc-why\">Permission and auth handling are built into the SDK runtime.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK — build your own harness on the same engine as Claude Code</a></li></ul>",
      "degree": 1
    },
    {
      "slug": "sdk-sessions",
      "title": "Sessions (SDK)",
      "kind": "primitive",
      "layer": "agent-sdk",
      "summary": "The SDK's unit of a continuing conversation — it carries message history, tool state, and context across turns so an agent can run a long task without you re-threading state by hand.",
      "sources": [
        "claude-agent-sdk"
      ],
      "html": "<p class=\"kc-lede\">The SDK's unit of a continuing conversation — it carries message history, tool state, and context across turns so an agent can run a long task without you re-threading state by hand.</p>\n<p>A session is where memory lives between turns. Most \"the agent forgot\" problems are really session/context-management problems.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"agent-sdk\">Claude Agent SDK</a> — <span class=\"kc-why\">Sessions are the SDK's core stateful abstraction.</span></li><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"context-window\">Context Window</a> — <span class=\"kc-why\">A session is, in effect, managed context-window state over time.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK — build your own harness on the same engine as Claude Code</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "sdk-subagents",
      "title": "Subagents (SDK)",
      "kind": "primitive",
      "layer": "agent-sdk",
      "summary": "Spawning a fresh agent with its own context to handle a scoped task and report back a result. The SDK primitive that makes fan-out, isolation, and multi-agent orchestration possible.",
      "sources": [
        "claude-agent-sdk",
        "claude-code-multi-agent-development"
      ],
      "html": "<p class=\"kc-lede\">Spawning a fresh agent with its own context to handle a scoped task and report back a result. The SDK primitive that makes fan-out, isolation, and multi-agent orchestration possible.</p>\n<p>A subagent trades shared context for a clean slate — you give it a task and get back a conclusion, not the whole transcript. This is the building block under every orchestration pattern.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"agent-sdk\">Claude Agent SDK</a> — <span class=\"kc-why\">Subagents are an SDK capability for delegating work.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"sdk-sessions\">Sessions (SDK)</a> — <span class=\"kc-why\">Each subagent runs in its own session/context.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK — build your own harness on the same engine as Claude Code</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a></li></ul>",
      "degree": 2
    },
    {
      "slug": "skills",
      "title": "Skills",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "Packaged, model-invocable capabilities — a folder with instructions (and optional scripts) the agent loads when a task matches. Reusable expertise that auto-fires when relevant and can also be called as a slash command.",
      "sources": [
        "claude-code-architecture",
        "tool-call-taxonomy"
      ],
      "html": "<p class=\"kc-lede\">Packaged, model-invocable capabilities — a folder with instructions (and optional scripts) the agent loads when a task matches. Reusable expertise that auto-fires when relevant and can also be called as a slash command.</p>\n<p>A skill is how you teach the harness a repeatable procedure once and have it apply itself thereafter. The same definition both auto-fires and is invokable by name. Only <code>SKILL.md</code> is required, and a skill is independent of CLAUDE.md — reach for a skill when a section of CLAUDE.md has grown from a <em>fact</em> into a <em>procedure</em>.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Skills are a Claude Code extension mechanism.</span></li></ul>\n<div class=\"kc-relhead\">Related</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">used with</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-md\">CLAUDE.md (progressive disclosure)</a> — <span class=\"kc-why\">Complement to CLAUDE.md — a skill's body loads only when used, vs CLAUDE.md which is always in context. Often you graduate a grown CLAUDE.md procedure into a skill.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"tool-call-taxonomy\">Tool call, function call, MCP, skill — one containment model</a></li></ul>",
      "degree": 6
    },
    {
      "slug": "slash-commands",
      "title": "Slash commands",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "The /command invoke surface. Custom commands have been merged into skills — `.claude/commands/deploy.md` and `.claude/skills/deploy/SKILL.md` both create `/deploy`. A skill that should only run when you ask sets `disable-model-invocation: true`.",
      "sources": [
        "claude-code-architecture"
      ],
      "html": "<p class=\"kc-lede\">The /command invoke surface. Custom commands have been merged into skills — <code>.claude/commands/deploy.md</code> and <code>.claude/skills/deploy/SKILL.md</code> both create <code>/deploy</code>. A skill that should only run when you ask sets <code>disable-model-invocation: true</code>.</p>\n<p>Rule of thumb lives inside skills now: a capability that should auto-fire <em>and</em> be invokable is a normal skill; a deliberate, side-effecting ritual (deploy, ship) sets <code>disable-model-invocation: true</code> so only <code>/name</code> runs it. One system, no duplication.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"skills\">Skills</a> — <span class=\"kc-why\">Slash commands are how you invoke a skill (or a built-in) by name; custom commands are now skills under the hood.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></li></ul>",
      "degree": 1
    },
    {
      "slug": "three-levels-of-engineering",
      "title": "Three levels — prompt · context · harness",
      "kind": "concept",
      "layer": "foundations",
      "summary": "The mental model that splits agent work into three levers — the prompt (what you say), the context (what the model can see), and the harness (the code around the model). Knowing which lever a problem lives on tells you where to fix it.",
      "sources": [
        "prompt-context-harness-engineering",
        "harness-vs-context-engineering"
      ],
      "html": "<p class=\"kc-lede\">The mental model that splits agent work into three levers — the prompt (what you say), the context (what the model can see), and the harness (the code around the model). Knowing which lever a problem lives on tells you where to fix it.</p>\n<p>This is the debugging ladder for the entire stack: a flaky output is usually a prompt problem, a wrong-facts problem is usually context, and a reliability problem is almost always the harness. Most teams over-invest in prompts and under-invest in the harness.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">depends on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"agent-loop\">Gather → Act → Verify loop</a> — <span class=\"kc-why\">All three levers tune the same gather→act→verify loop.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a></li></ul>",
      "degree": 4
    },
    {
      "slug": "tool-use",
      "title": "Tool Use",
      "kind": "primitive",
      "layer": "api",
      "summary": "The mechanism that lets the model call functions you define — it emits a structured tool call, your code runs it, you feed the result back. This is the \"act\" step of the loop made concrete.",
      "sources": [
        "claude-api-agent-primitives",
        "claude-code-architecture",
        "tool-call-taxonomy"
      ],
      "html": "<p class=\"kc-lede\">The mechanism that lets the model call functions you define — it emits a structured tool call, your code runs it, you feed the result back. This is the \"act\" step of the loop made concrete.</p>\n<p>Every capability an agent has — reading files, running code, searching — is a tool. Master this one primitive and most of the stack is just bigger and bigger libraries of tools. One nuance: <em>client tools</em> your code executes (the loop above) vs <em>server tools</em> (web_search, code_execution, web_fetch) that Anthropic executes for you — for those, you don't run anything.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">runs on</span> <a href=\"#\" class=\"kc-link\" data-slug=\"messages-api\">Messages API</a> — <span class=\"kc-why\">Tool calls ride the Messages API request/response cycle.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"tool-call-taxonomy\">Tool call, function call, MCP, skill — one containment model</a></li></ul>",
      "degree": 3
    },
    {
      "slug": "workflow-patterns",
      "title": "Dynamic-workflow patterns (the six)",
      "kind": "pattern",
      "layer": "patterns",
      "summary": "The named, reusable shapes of agent orchestration — pipeline, parallel fan-out, loop-until-dry, judge panel, and friends — each fixing a specific failure mode. A vocabulary for composing reliable multi-agent work.",
      "sources": [
        "dynamic-workflow-patterns",
        "dynamic-workflows-and-ultraplan"
      ],
      "html": "<p class=\"kc-lede\">The named, reusable shapes of agent orchestration — pipeline, parallel fan-out, loop-until-dry, judge panel, and friends — each fixing a specific failure mode. A vocabulary for composing reliable multi-agent work.</p>\n<p>Don't reinvent orchestration each time — reach for the pattern that matches the failure you're trying to prevent (coverage, confidence, or scale). They compose into bigger harnesses.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows\">Dynamic workflows (ultracode)</a> — <span class=\"kc-why\">The patterns are implemented on the dynamic-workflow engine.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"orchestration\">Multi-agent orchestration</a> — <span class=\"kc-why\">Each pattern is a specific orchestration structure.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns and the three failure modes they fix</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the \"Ultraplan\" loop</a></li></ul>",
      "degree": 4
    },
    {
      "slug": "worktrees",
      "title": "Worktrees (worktree.baseRef)",
      "kind": "primitive",
      "layer": "claude-code",
      "summary": "Giving an agent its own isolated git worktree so parallel agents edit code without colliding. The baseRef setting (fresh vs head) decides whether that worktree branches from origin or your current HEAD.",
      "sources": [
        "worktree-base-ref-fresh-vs-head",
        "claude-code-multi-agent-development"
      ],
      "html": "<p class=\"kc-lede\">Giving an agent its own isolated git worktree so parallel agents edit code without colliding. The baseRef setting (fresh vs head) decides whether that worktree branches from origin or your current HEAD.</p>\n<p>fresh branches from origin/main for a clean base; head branches from your local work so the agent sees uncommitted changes. Pick wrong and the agent either misses your context or inherits your mess.</p>\n<div class=\"kc-relhead\">Depends on</div><ul class=\"kc-rellist\"><li><span class=\"kc-dep\">part of</span> <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code\">Claude Code (the harness)</a> — <span class=\"kc-why\">Worktree isolation is a Claude Code execution feature.</span></li><li><span class=\"kc-dep\">uses</span> <a href=\"#\" class=\"kc-link\" data-slug=\"cc-subagents\">Subagents / Task (Claude Code)</a> — <span class=\"kc-why\">Worktrees are how multiple subagents mutate files in parallel safely.</span></li></ul>\n<div class=\"kc-relhead\">Sources — go deeper</div><ul class=\"kc-srclist\"><li><a href=\"#\" class=\"kc-link\" data-slug=\"worktree-base-ref-fresh-vs-head\">worktree.baseRef — fresh vs head decides what an agent's worktree branches from</a></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a></li></ul>",
      "degree": 4
    }
  ],
  "conceptEdges": [
    {
      "from": "adversarial-verification",
      "to": "workflow-patterns",
      "type": "part-of",
      "why": "It's one of the core dynamic-workflow patterns."
    },
    {
      "from": "adversarial-verification",
      "to": "externalized-completion",
      "type": "depends-on",
      "why": "Verification is how 'done' gets decided by something other than the agent itself."
    },
    {
      "from": "agent-harness",
      "to": "agent-loop",
      "type": "runs-on",
      "why": "The harness is the machinery that drives the gather→act→verify loop."
    },
    {
      "from": "agent-harness",
      "to": "context-window",
      "type": "depends-on",
      "why": "Memory and tool results all flow through the window."
    },
    {
      "from": "agent-loop",
      "to": "context-window",
      "type": "depends-on",
      "why": "Each turn reads from and writes back into the context window."
    },
    {
      "from": "agent-sdk",
      "to": "messages-api",
      "type": "runs-on",
      "why": "The SDK drives the Messages API loop for you."
    },
    {
      "from": "agent-sdk",
      "to": "tool-use",
      "type": "uses",
      "why": "It manages the tool-call round trip as a first-class concept."
    },
    {
      "from": "agent-sdk",
      "to": "mcp",
      "type": "uses",
      "why": "It connects to MCP servers to extend the agent's toolset."
    },
    {
      "from": "agent-teams",
      "to": "claude-code",
      "type": "part-of",
      "why": "Agent teams are a Claude Code coordination feature."
    },
    {
      "from": "agent-teams",
      "to": "cc-subagents",
      "type": "alternative-to",
      "why": "Subagents fork from one session and report back; agent teams are independent sessions that coordinate as peers."
    },
    {
      "from": "agent-teams",
      "to": "execution-isolation",
      "type": "uses",
      "why": "Each team member runs in its own independent context."
    },
    {
      "from": "batch-api",
      "to": "messages-api",
      "type": "depends-on",
      "why": "A batch is a queue of ordinary Messages API calls run asynchronously."
    },
    {
      "from": "buy-vs-build",
      "to": "claude-code",
      "type": "uses",
      "why": "'Use Claude Code as-is' is the default altitude."
    },
    {
      "from": "buy-vs-build",
      "to": "agent-sdk",
      "type": "uses",
      "why": "'Configure your own harness' drops to the SDK."
    },
    {
      "from": "buy-vs-build",
      "to": "messages-api",
      "type": "uses",
      "why": "'Build from scratch' drops all the way to the raw API."
    },
    {
      "from": "cc-hooks",
      "to": "claude-code",
      "type": "part-of",
      "why": "Hooks are a Claude Code automation surface configured in settings."
    },
    {
      "from": "cc-mcp-servers",
      "to": "claude-code",
      "type": "part-of",
      "why": "MCP servers plug into Claude Code's tool surface."
    },
    {
      "from": "cc-mcp-servers",
      "to": "mcp",
      "type": "uses",
      "why": "They speak the Model Context Protocol to expose tools to the model."
    },
    {
      "from": "cc-subagents",
      "to": "claude-code",
      "type": "part-of",
      "why": "Subagents are a core Claude Code primitive — each runs in its own context window within a single session."
    },
    {
      "from": "checkpointing",
      "to": "claude-code",
      "type": "part-of",
      "why": "Checkpointing is a built-in Claude Code safety feature."
    },
    {
      "from": "checkpointing",
      "to": "permission-modes",
      "type": "used-with",
      "why": "Pairs with permission modes — looser autonomy is safer when edits are one rewind away."
    },
    {
      "from": "claude-code",
      "to": "agent-sdk",
      "type": "runs-on",
      "why": "Claude Code is built on the same engine the SDK exposes."
    },
    {
      "from": "claude-md",
      "to": "claude-code",
      "type": "part-of",
      "why": "CLAUDE.md is Claude Code's built-in memory surface."
    },
    {
      "from": "claude-md",
      "to": "context-engineering",
      "type": "uses",
      "why": "It is context engineering applied to a single durable file."
    },
    {
      "from": "company-ai-stack",
      "to": "buy-vs-build",
      "type": "uses",
      "why": "Every component choice is a buy-vs-build altitude decision."
    },
    {
      "from": "company-ai-stack",
      "to": "reference-architecture",
      "type": "uses",
      "why": "The target shape is a production reference architecture."
    },
    {
      "from": "consulting-discovery",
      "to": "company-ai-stack",
      "type": "part-of",
      "why": "Discovery is the first phase of bootstrapping a company's stack."
    },
    {
      "from": "context-engineering",
      "to": "three-levels-of-engineering",
      "type": "part-of",
      "why": "The second of the three levers."
    },
    {
      "from": "context-engineering",
      "to": "context-window",
      "type": "depends-on",
      "why": "It is the discipline of spending the context window well."
    },
    {
      "from": "dynamic-workflows",
      "to": "claude-code",
      "type": "part-of",
      "why": "Workflows are a Claude Code orchestration capability."
    },
    {
      "from": "dynamic-workflows",
      "to": "cc-subagents",
      "type": "uses",
      "why": "A workflow pipelines work across many subagents."
    },
    {
      "from": "evals-reliability",
      "to": "harness-engineering",
      "type": "depends-on",
      "why": "You harden the harness against the failure modes evals expose."
    },
    {
      "from": "execution-isolation",
      "to": "cc-subagents",
      "type": "uses",
      "why": "Each subagent is a forked, isolated context."
    },
    {
      "from": "execution-isolation",
      "to": "worktrees",
      "type": "uses",
      "why": "Worktrees isolate the filesystem the way subagents isolate context."
    },
    {
      "from": "externalized-completion",
      "to": "harness-engineering",
      "type": "depends-on",
      "why": "Completion lives in the deterministic harness, not the weights."
    },
    {
      "from": "goal-mode",
      "to": "claude-code",
      "type": "part-of",
      "why": "/goal is a Claude Code session command."
    },
    {
      "from": "goal-mode",
      "to": "cc-hooks",
      "type": "depends-on",
      "why": "It is literally a wrapper around a prompt-based Stop hook — it requires workspace trust and is disabled when hooks are off."
    },
    {
      "from": "goal-mode",
      "to": "externalized-completion",
      "type": "uses",
      "why": "A separate evaluator model decides 'done', not the agent doing the work — externalized completion baked into the product."
    },
    {
      "from": "goal-mode",
      "to": "scheduled-tasks",
      "type": "alternative-to",
      "why": "Both keep one session running; /goal advances when a condition holds, /loop when a clock elapses."
    },
    {
      "from": "harness-engineering",
      "to": "three-levels-of-engineering",
      "type": "part-of",
      "why": "The third of the three levers."
    },
    {
      "from": "harness-engineering",
      "to": "agent-harness",
      "type": "uses",
      "why": "Harness engineering is the practice of building the agent harness."
    },
    {
      "from": "loop-engineering",
      "to": "harness-engineering",
      "type": "depends-on",
      "why": "A loop is the agent harness put on a timer that spawns helpers and feeds itself — it sits one floor above."
    },
    {
      "from": "loop-engineering",
      "to": "repo-as-record",
      "type": "depends-on",
      "why": "The loop's memory must live on disk/in the repo because the model forgets everything between runs."
    },
    {
      "from": "loop-engineering",
      "to": "externalized-completion",
      "type": "depends-on",
      "why": "An unattended loop is only trustworthy if 'done' is externally verified — what /goal's separate Haiku evaluator does."
    },
    {
      "from": "loop-engineering",
      "to": "cc-subagents",
      "type": "uses",
      "why": "Splits the maker from the checker so a verifier the loop trusts decides 'done'."
    },
    {
      "from": "loop-engineering",
      "to": "worktrees",
      "type": "uses",
      "why": "Isolates parallel agents so concurrent runs don't collide on the same files."
    },
    {
      "from": "loop-engineering",
      "to": "skills",
      "type": "uses",
      "why": "Writes project intent down once so the loop doesn't re-derive it from zero every cycle."
    },
    {
      "from": "loop-engineering",
      "to": "cc-mcp-servers",
      "type": "uses",
      "why": "MCP servers (the source's 'connectors') let the loop act in real tools — open PRs, update tickets, post to Slack."
    },
    {
      "from": "loop-engineering",
      "to": "cc-hooks",
      "type": "uses",
      "why": "Lifecycle hooks plus /loop and /goal give the loop its scheduled heartbeat."
    },
    {
      "from": "loop-engineering",
      "to": "scheduled-tasks",
      "type": "uses",
      "why": "/loop is the clock-driven heartbeat that turns one run into a recurring in-session loop."
    },
    {
      "from": "loop-engineering",
      "to": "goal-mode",
      "type": "uses",
      "why": "/goal is the condition-driven heartbeat — keep going until a validator confirms done."
    },
    {
      "from": "loop-until-done",
      "to": "workflow-patterns",
      "type": "part-of",
      "why": "It's one of the core dynamic-workflow patterns."
    },
    {
      "from": "loop-until-done",
      "to": "externalized-completion",
      "type": "depends-on",
      "why": "The loop's exit condition lives outside the model."
    },
    {
      "from": "mcp",
      "to": "tool-use",
      "type": "used-with",
      "why": "MCP is its own open standard; in the Claude API its servers surface to the model as tool-use calls."
    },
    {
      "from": "mcp",
      "to": "messages-api",
      "type": "used-with",
      "why": "Provider-neutral — it integrates with the Messages API via the MCP connector, but doesn't depend on it."
    },
    {
      "from": "messages-api",
      "to": "model-tiers",
      "type": "depends-on",
      "why": "Each request names a model tier (Opus / Sonnet / Haiku) to run on."
    },
    {
      "from": "messages-api",
      "to": "context-window",
      "type": "depends-on",
      "why": "The request is exactly what fills the model's context window."
    },
    {
      "from": "orchestration",
      "to": "cc-subagents",
      "type": "uses",
      "why": "Orchestration is built out of subagent delegation."
    },
    {
      "from": "orchestration",
      "to": "execution-isolation",
      "type": "uses",
      "why": "It relies on isolating each agent's context to parallelize safely."
    },
    {
      "from": "permission-modes",
      "to": "claude-code",
      "type": "part-of",
      "why": "Permission modes are a Claude Code safety control."
    },
    {
      "from": "plan-mode",
      "to": "permission-modes",
      "type": "part-of",
      "why": "Plan mode is one of Claude Code's permission modes — read-and-propose with no edits."
    },
    {
      "from": "plugins",
      "to": "claude-code",
      "type": "part-of",
      "why": "Plugins are Claude Code's packaging/distribution mechanism."
    },
    {
      "from": "plugins",
      "to": "skills",
      "type": "used-with",
      "why": "A plugin can bundle skills."
    },
    {
      "from": "plugins",
      "to": "cc-hooks",
      "type": "used-with",
      "why": "A plugin can bundle hooks."
    },
    {
      "from": "plugins",
      "to": "cc-subagents",
      "type": "used-with",
      "why": "A plugin can bundle subagents."
    },
    {
      "from": "plugins",
      "to": "cc-mcp-servers",
      "type": "used-with",
      "why": "A plugin can bundle MCP server configs."
    },
    {
      "from": "prompt-caching",
      "to": "messages-api",
      "type": "part-of",
      "why": "Caching is a feature of the Messages API request."
    },
    {
      "from": "prompt-caching",
      "to": "context-window",
      "type": "depends-on",
      "why": "It caches a stable prefix of the context window."
    },
    {
      "from": "prompt-engineering",
      "to": "three-levels-of-engineering",
      "type": "part-of",
      "why": "The first of the three levers."
    },
    {
      "from": "reference-architecture",
      "to": "orchestration",
      "type": "uses",
      "why": "Orchestration is the reference architecture's execution core."
    },
    {
      "from": "reference-architecture",
      "to": "repo-as-record",
      "type": "uses",
      "why": "The repository is its durable memory and system of record."
    },
    {
      "from": "repo-as-record",
      "to": "context-engineering",
      "type": "depends-on",
      "why": "The repo is curated context that persists between runs."
    },
    {
      "from": "routines",
      "to": "claude-code",
      "type": "part-of",
      "why": "Routines are the unattended-cloud member of the Claude Code primitive set (Claude Code on the web)."
    },
    {
      "from": "routines",
      "to": "cc-mcp-servers",
      "type": "uses",
      "why": "A routine bundles connectors/MCP servers into its saved config so the cloud run can reach external systems."
    },
    {
      "from": "sandboxing",
      "to": "claude-code",
      "type": "part-of",
      "why": "Sandboxing is a Claude Code execution-safety feature."
    },
    {
      "from": "sandboxing",
      "to": "permission-modes",
      "type": "used-with",
      "why": "Permission modes decide what's allowed; the sandbox enforces the blast radius when it runs."
    },
    {
      "from": "scheduled-tasks",
      "to": "claude-code",
      "type": "part-of",
      "why": "Scheduled tasks are a Claude Code session feature; the cron tools are its internal scheduling API."
    },
    {
      "from": "scheduled-tasks",
      "to": "skills",
      "type": "part-of",
      "why": "/loop is a bundled skill — the same invocation surface as any other slash command."
    },
    {
      "from": "scheduled-tasks",
      "to": "routines",
      "type": "alternative-to",
      "why": "Same 'repeat on a clock' need; /loop is local and needs the session open, a routine runs unattended in the cloud."
    },
    {
      "from": "sdk-hooks",
      "to": "agent-sdk",
      "type": "part-of",
      "why": "Hooks are an extension point of the SDK runtime."
    },
    {
      "from": "sdk-permissions",
      "to": "agent-sdk",
      "type": "part-of",
      "why": "Permission and auth handling are built into the SDK runtime."
    },
    {
      "from": "sdk-sessions",
      "to": "agent-sdk",
      "type": "part-of",
      "why": "Sessions are the SDK's core stateful abstraction."
    },
    {
      "from": "sdk-sessions",
      "to": "context-window",
      "type": "depends-on",
      "why": "A session is, in effect, managed context-window state over time."
    },
    {
      "from": "sdk-subagents",
      "to": "agent-sdk",
      "type": "part-of",
      "why": "Subagents are an SDK capability for delegating work."
    },
    {
      "from": "sdk-subagents",
      "to": "sdk-sessions",
      "type": "uses",
      "why": "Each subagent runs in its own session/context."
    },
    {
      "from": "skills",
      "to": "claude-code",
      "type": "part-of",
      "why": "Skills are a Claude Code extension mechanism."
    },
    {
      "from": "skills",
      "to": "claude-md",
      "type": "used-with",
      "why": "Complement to CLAUDE.md — a skill's body loads only when used, vs CLAUDE.md which is always in context. Often you graduate a grown CLAUDE.md procedure into a skill."
    },
    {
      "from": "slash-commands",
      "to": "skills",
      "type": "part-of",
      "why": "Slash commands are how you invoke a skill (or a built-in) by name; custom commands are now skills under the hood."
    },
    {
      "from": "three-levels-of-engineering",
      "to": "agent-loop",
      "type": "depends-on",
      "why": "All three levers tune the same gather→act→verify loop."
    },
    {
      "from": "tool-use",
      "to": "messages-api",
      "type": "runs-on",
      "why": "Tool calls ride the Messages API request/response cycle."
    },
    {
      "from": "workflow-patterns",
      "to": "dynamic-workflows",
      "type": "uses",
      "why": "The patterns are implemented on the dynamic-workflow engine."
    },
    {
      "from": "workflow-patterns",
      "to": "orchestration",
      "type": "uses",
      "why": "Each pattern is a specific orchestration structure."
    },
    {
      "from": "worktrees",
      "to": "claude-code",
      "type": "part-of",
      "why": "Worktree isolation is a Claude Code execution feature."
    },
    {
      "from": "worktrees",
      "to": "cc-subagents",
      "type": "uses",
      "why": "Worktrees are how multiple subagents mutate files in parallel safely."
    }
  ],
  "nodes": [
    {
      "slug": "agentic-ai-reference-architecture",
      "title": "Agentic AI System — Reference Architecture",
      "kind": "concept",
      "layer": "patterns",
      "tags": [
        "agents",
        "orchestration",
        "architecture",
        "observability",
        "memory"
      ],
      "depth": "evergreen",
      "claudeSpecific": false,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/agentic-ai-reference-architecture.md",
      "html": "<h1>Agentic AI System — Reference Architecture</h1>\n<p><img src=\"notes/agentic-ai-reference-architecture.jpeg\" alt=\"Agentic AI System Reference Architecture\" loading=\"lazy\"></p>\n<h2>TL;DR</h2>\n<p>A layered blueprint for a production agentic system. Four core qualities up top — <strong>goal-driven, multi-agent, orchestrated, observable, reliable</strong> — realized through nine layers from the user request down to shared infrastructure, with monitoring, reliability, and governance as cross-cutting concerns on the side.</p>\n<h2>The nine layers</h2>\n<ol><li><strong>User / Client Layer</strong> — entry points: web/mobile, chat/voice, API/SDK, enterprise systems. Sends <em>user request</em>, receives <em>response</em>.</li><li><strong>Orchestration / Control Plane</strong> — the orchestrator / workflow engine (planner, router, scheduler, policy enforcer). Sub-blocks: task decomposition, agent selection, plan &amp; execution manager, state &amp; context manager, guardrails &amp; policy.</li><li><strong>Agent Layer (specialized agents)</strong> — research, reasoning, action, data, communication agents. Each owns a narrow job (analyze, plan/decide, execute &amp; call tools, query data, summarize).</li><li><strong>Tools &amp; Integrations Layer</strong> — web search, APIs, code execution, databases, file/doc processing, other services.</li><li><strong>Memory &amp; Knowledge Layer</strong> — short-term (context), long-term (vector DB), knowledge base (docs), episodic/event store (history), user/org profile store.</li><li><strong>Monitoring &amp; Observability</strong> — tracing &amp; logging (end-to-end), metrics &amp; dashboards (latency, tokens, cost), alerts, audit &amp; compliance.</li><li><strong>Reliability &amp; Failure Management</strong> — error detection, retry &amp; backoff, fallback / alternate agents, human-in-the-loop, circuit breaker.</li><li><strong>Governance &amp; Security</strong> — authn/authz, data privacy &amp; PII protection, policy enforcement, model &amp; prompt guardrails, compliance &amp; audit.</li><li><strong>Foundation / Infrastructure (cross-cutting)</strong> — LLM providers, model gateway (routing, rate limits, cost mgmt), vector DB, data storage, queue / event bus, cache, secrets manager, CI/CD &amp; deployment.</li></ol>\n<h2>The flow (numbered in the diagram)</h2>\n<ol><li>User request enters the system →</li><li>Orchestrator decomposes the task &amp; selects agents →</li><li>Agents use tools &amp; memory to execute →</li><li>System updates memory &amp; state →</li><li>Observability captures insights &amp; feedback.</li></ol>\n<p>Solid arrows = sync / data flow; dashed = async / event flow.</p>\n<h2>Why it matters / how I'd apply it</h2>\n<p>A checklist for \"what am I missing\" when designing an agent system. The easy parts (agents + tools + an orchestrator) are layers 2–5; the parts that make it <em>production-grade</em> are 6–8 (observability, reliability, governance) — exactly the ones that get skipped in prototypes. Use it to audit any agent build: can I trace a request end-to-end? Is there a fallback and a circuit breaker? Where do guardrails and PII protection sit? Layer 9 (model gateway, secrets, cost mgmt) is the shared infra worth standardizing across products.</p>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a> — the inner gather→act→verify loop each agent runs.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a> — concrete multi-agent patterns for the Agent Layer (3).</li><li>incoming: <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol</a> is <strong>part-of</strong> this (layer 4, Tools &amp; Integrations).</li></ul>",
      "connCount": 9
    },
    {
      "slug": "anatomy-of-an-agent-harness",
      "title": "The Anatomy of an Agent Harness",
      "kind": "concept",
      "layer": "foundations",
      "tags": [
        "harness",
        "agents",
        "orchestration",
        "memory",
        "context-engineering",
        "tool-use",
        "verification"
      ],
      "depth": "evergreen",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/anatomy-of-an-agent-harness.md",
      "html": "<h1>The Anatomy of an Agent Harness</h1>\n<blockquote><p>Distilled from Akshay Pachaar's X article <em>\"The Anatomy of an Agent Harness\"</em> (2026-04-06) — a synthesis across Anthropic, OpenAI, Perplexity, and LangChain.</p></blockquote>\n<h2>TL;DR</h2>\n<p>The <strong>agent harness</strong> is the complete software infrastructure wrapping an LLM — orchestration loop, tools, memory, context management, state, error handling, guardrails — that turns a stateless model into a capable agent. The provocative, well-evidenced claim: <strong>the harness, not the model, usually decides whether an agent works.</strong> \"If you're not the model, you're the harness\" (Vivek Trivedy, LangChain).</p>\n<h2>The core argument</h2>\n<blockquote><p><em>\"The problem isn't your model. It's everything around your model.\"</em></p></blockquote>\n<ul><li><strong>The harness is the product.</strong> LangChain changed <strong>only the infrastructure</strong> around their LLM (same model, same weights) and jumped from outside the top 30 to <strong>rank 5 on TerminalBench 2.0</strong> — a 20+ position move from harness design alone. A separate project hit 76.4% by having an LLM <em>optimize the harness itself</em>, beating hand-designed systems.</li><li><strong>Agent vs harness.</strong> The \"agent\" is the <em>emergent behavior</em> (goal-directed, tool-using, self-correcting). The harness is the <em>machinery</em> producing it. \"I built an agent\" really means \"I built a harness and pointed it at a model.\" Anthropic literally calls the Agent SDK <em>\"the agent harness that powers Claude Code.\"</em></li><li><strong>The computer analogy</strong> (Beren Millidge): a raw LLM is a <strong>CPU</strong> with no RAM/disk/IO. The context window is <strong>RAM</strong> (fast, limited); external DBs are <strong>disk</strong> (large, slow); tools are <strong>device drivers</strong>; the harness is the <strong>operating system</strong>. We've reinvented the Von Neumann architecture.</li><li><strong>Three nested levels</strong> (this is <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a>): <em>prompt</em> engineering crafts the instruction → <em>context</em> engineering manages what the model sees and when → <em>harness</em> engineering encompasses both plus tool orchestration, state, error recovery, verification, safety, and lifecycle.</li></ul>\n<h2>The 12 components of a production harness</h2>\n<p>Akshay frames the harness as a twelve-part anatomy; the eleven he walks through in detail:</p>\n<ol><li><strong>Orchestration loop</strong> — the heartbeat: the Thought→Action→Observation (ReAct) cycle. Anthropic calls their runtime a <em>\"dumb loop\"</em> — all intelligence lives in the model; the harness just manages turns.</li><li><strong>Tools</strong> — the agent's hands: schemas injected into context; registration, validation, sandboxed execution, result formatting. Claude Code ships six categories (file, search, exec, web, code-intel, subagent).</li><li><strong>Memory</strong> — multi-timescale. Claude Code's three-tier hierarchy: a ~150-char always-loaded index → detailed topic files on demand → raw transcripts via search. Key rule: <strong>treat memory as a <em>hint</em>, verify against real state before acting.</strong> (CLAUDE.md / MEMORY.md.)</li><li><strong>Context management</strong> — where agents fail silently. <strong>Context rot</strong>: performance drops 30%+ when key content lands mid-window (\"Lost in the Middle\"). Fixes: <strong>compaction</strong>, observation masking, just-in-time retrieval (grep/glob over loading whole files), subagent delegation. Goal: <em>the smallest set of high-signal tokens.</em></li><li><strong>Prompt construction</strong> — hierarchical assembly: system prompt + tool defs + memory + history + user msg, by priority.</li><li><strong>Output parsing</strong> — native tool-calling (structured <code>tool_calls</code>, not free-text). Tool calls → execute &amp; loop; none → final answer.</li><li><strong>State management</strong> — checkpoints &amp; resume. Claude Code's twist: <strong>git commits as checkpoints</strong> + progress files as scratchpads.</li><li><strong>Error handling</strong> — errors compound: 10 steps at 99% each ≈ <strong>90% end-to-end</strong>. Classify transient / LLM-recoverable / user-fixable / unexpected; return tool errors <em>into</em> the loop so the model self-corrects.</li><li><strong>Guardrails &amp; safety</strong> — Anthropic separates <em>permission</em> from <em>reasoning</em>: the model decides what to attempt; the tool system decides what's allowed (~40 independently gated capabilities, staged confirmation).</li><li><strong>Verification loops</strong> — what separates demos from production: rules-based (tests/linters), visual (screenshots), LLM-as-judge. Boris Cherny: a way to verify its own work improves quality <strong>2–3×</strong>.</li><li><strong>Subagent orchestration</strong> — Claude Code's three models: <strong>Fork</strong> (identical context copy), <strong>Teammate</strong> (separate pane, file-mailbox), <strong>Worktree</strong> (isolated git branch per agent).</li></ol>\n<p><strong>The loop in motion:</strong> assemble → infer → classify output → execute tools (read-only concurrent, mutating serial) → package results → update/compact context → repeat, until a layered termination condition fires. For tasks spanning <em>many</em> context windows, the <strong>\"Ralph Loop\"</strong> uses the filesystem (git logs + progress files) for continuity across sessions.</p>\n<h2>The strategic layer</h2>\n<ul><li><strong>Scaffolding is temporary.</strong> As models improve, harness complexity should <em>decrease</em> — Manus was rebuilt 5× in 6 months, each time removing complexity. <strong>Co-evolution:</strong> models are now post-trained <em>with</em> their harness, so swapping tool implementations can degrade performance.</li><li><strong>The future-proofing test:</strong> if performance scales up with better models <em>without adding harness complexity</em>, the design is sound.</li><li><strong>Seven decisions every harness makes:</strong> single vs multi-agent (max a single agent first; split only past ~10 overlapping tools) · ReAct vs plan-and-execute · context strategy · verification design (Fowler's <em>guides</em> vs <em>sensors</em>) · permission architecture · <strong>tool scoping</strong> (Vercel cut 80% of v0's tools and got <em>better</em> results) · <strong>harness thickness</strong> (Anthropic bets thin + model improvement, and deletes planning steps as models internalize them).</li></ul>\n<h2>Mental model / why it matters</h2>\n<p>This is the <strong>expansion of the harness layer</strong> of <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a> into a concrete, twelve-organ checklist — and it reframes the debugging map: <em>when an agent fails, don't blame the model, look at the harness.</em> The deeper lesson for an architect: <strong>harness design is where the differentiated engineering lives</strong> — two products on the <em>same model</em> can be 20 ranks apart purely on harness quality.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Audit checklist:</strong> walk a client's agent through the 12 components — most prototypes have 1–6 and skip memory, state, error handling, verification, and orchestration, which is exactly why they demo well and fail in production.</li><li><strong>Architecture conversation:</strong> the seven decisions are a ready-made discovery framework for a client engagement.</li><li><strong>Claude-stack advantage:</strong> the Agent SDK / Claude Code ships most of these organs as primitives — you <em>configure</em> a harness instead of hand-rolling one. Bet <strong>thin</strong>, lean on verification (2–3× quality), and scope tools aggressively.</li></ul>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a> — this dissects the harness level into components.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — the canonical thin harness; primitives = organs.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a> — the production view of the same anatomy.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol</a> — the \"tools\" organ (component 2).</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the Ultraplan loop</a> — subagent orchestration (component 11), scripted.</li></ul>",
      "connCount": 11
    },
    {
      "slug": "automation-control-flow-spectrum",
      "title": "Keeping Claude working — the two axes behind /loop, /goal, hooks, routines & cron",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "automation",
        "scheduling",
        "loops",
        "goal",
        "hooks",
        "routines",
        "cron",
        "claude-code"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/automation-control-flow-spectrum.md",
      "html": "<h1>Keeping Claude working — the two axes behind /loop, /goal, hooks, routines &amp; cron</h1>\n<blockquote><p><strong>TL;DR.</strong> <code>/loop</code>, <code>/goal</code>, hooks, routines, cron all \"make Claude keep working,\" which is why they blur together. They stop blurring once you place each on <strong>two axes</strong>: (1) <strong>WHERE it runs</strong> — inside an <em>open session</em> you're driving, or <em>independently</em> of any session; and (2) <strong>WHAT starts the next unit of work</strong> — a <strong>clock</strong> (an interval elapses), a <strong>condition</strong> (a check passes), or an <strong>event</strong> (something happens). Every primitive is one cell in that grid, and the cell tells you when to reach for it.</p></blockquote>\n<h2>The grid</h2>\n<table><thead><tr><th></th><th><strong>Clock</strong> (interval)</th><th><strong>Condition</strong> (a check passes)</th><th><strong>Event</strong> (something happens)</th></tr></thead><tbody><tr><td><strong>In an open session</strong></td><td><strong><code>/loop</code></strong> — re-run a prompt every N min</td><td><strong><code>/goal</code></strong> — keep going until a small model confirms done; custom <strong>Stop hook</strong></td><td><strong>Hooks</strong> (PreToolUse, Stop, …); <strong>Channels</strong> (CI pushes a failure in)</td></tr><tr><td><strong>Independent of session</strong></td><td><strong>Cloud routines</strong> (schedule); <strong>Desktop scheduled tasks</strong>; <strong>OS cron</strong>; GitHub Actions <code>schedule</code></td><td>—</td><td><strong>Cloud routines</strong> (GitHub event / API <code>/fire</code>)</td></tr></tbody></table>\n<p>Two official framings sit inside this grid. <strong>Three ways to keep one session running:</strong> <code>/loop</code> (next turn when an interval elapses), <code>/goal</code> (next turn when the previous one finishes, stop when a condition holds), and a <strong>Stop hook</strong> (next turn after each turn, your script/prompt decides). <strong>Three ways to schedule work independent of a session:</strong> <strong>cloud routines</strong> (Anthropic infra), <strong>desktop scheduled tasks</strong> (your machine), and <code>/loop</code> (local, session open).</p>\n<h2>The primitives, one line each</h2>\n<ul><li><strong><code>/loop</code></strong> — a bundled skill that re-runs a prompt on an interval. You give an interval + prompt (fixed cron), a prompt only (Claude picks a 1-min-to-1-hour delay each iteration), or nothing (a built-in maintenance prompt, or your <code>loop.md</code>). It converts the interval to a cron expression via the <strong><code>CronCreate</code>/<code>CronList</code>/<code>CronDelete</code></strong> tools. <strong>Session-scoped, local, 7-day expiry</strong>, needs the session open. See <a href=\"#\" class=\"kc-link\" data-slug=\"scheduled-tasks\">scheduled tasks</a>.</li><li><strong><code>/goal</code></strong> — set a completion condition; after each turn a <strong>small fast model (Haiku by default)</strong> checks whether it holds and, if not, runs another turn. It's a <strong>wrapper around a session-scoped prompt-based Stop hook</strong>. Condition-driven, not clock-driven. See <a href=\"#\" class=\"kc-link\" data-slug=\"goal-mode\">goal mode</a>.</li><li><strong>Hooks</strong> — user-defined handlers (shell / HTTP / prompt) the harness fires <strong>deterministically</strong> at lifecycle events (<code>PreToolUse</code>, <code>Stop</code>, <code>SessionStart</code>, …). Event-driven, and the model <em>can't skip them</em>. See <a href=\"#\" class=\"kc-link\" data-slug=\"cc-hooks\">hooks</a>.</li><li><strong>Routines</strong> — a saved config (prompt + repos + connectors) that runs <strong>unattended on Anthropic's cloud</strong> on a schedule, a GitHub event, or an API call. Persistent, laptop closed. See <a href=\"#\" class=\"kc-link\" data-slug=\"routines\">routines</a>.</li><li><strong>OS cron</strong> — your operating system's scheduler firing a <strong>fixed shell script</strong> on a timer. No model, no decision in the body — the thing <code>/loop</code> is <em>named after</em> but is not.</li></ul>\n<h2>Why X, not Y (the questions this answers)</h2>\n<ul><li><strong>Why <code>/goal</code> when <code>/loop</code> exists?</strong> Different trigger for the next turn. <code>/loop</code> is <strong>clock-driven</strong> (\"check the deploy every 5 minutes\"); <code>/goal</code> is <strong>condition-driven</strong> (\"keep working until the auth tests pass and lint is clean\"). Use <code>/loop</code> to <em>poll</em>; use <code>/goal</code> to <em>finish a task with a verifiable end state.</em></li><li><strong>Loops vs cron jobs?</strong> <code>/loop</code> <em>uses</em> cron syntax (<code>CronCreate</code>) and so <em>feels</em> like cron — but <strong>OS cron runs a fixed script</strong>, while <code>/loop</code> runs a <strong>model that decides</strong> what to do each tick (see <a href=\"#\" class=\"kc-link\" data-slug=\"loop-is-cron-plus-a-decision-maker\">a loop is cron plus a decision-maker</a>). Also <code>/loop</code> fires <em>between turns of an open session</em> and <strong>expires in 7 days</strong>; OS cron runs system-wide forever.</li><li><strong>Loops vs routines?</strong> Same \"repeat\" idea, <strong>different infrastructure.</strong> <code>/loop</code> is <strong>local and needs the session open</strong>; a <strong>routine runs in Anthropic's cloud, unattended, persistent</strong> — and a routine run has <strong>no permission prompts</strong>, so scope its repos/connectors first.</li><li><strong>Where does Boris's \"close your laptop\" tip land?</strong> That's the move from the top row (in-session <code>/loop</code>, <code>/goal</code>) to the bottom row (cloud routines) of the grid.</li></ul>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Diagnose with the two questions.</strong> \"Does it need my session open?\" and \"what should start the next run — a clock, a condition, or an event?\" Those two answers pick the primitive every time.</li><li><strong>Don't reach for a routine to poll a build</strong> — that's a <code>/loop</code>. Don't reach for <code>/loop</code> for a nightly job that must run with your laptop shut — that's a routine. Don't use a clock at all when you mean \"until done\" — that's <code>/goal</code>.</li><li><strong>Guardrails are non-negotiable on the condition/clock axes.</strong> A <code>/goal</code> or fixed <code>/loop</code> can run a long time; cap iterations, detect no-progress, set a budget (see the economics in <a href=\"#\" class=\"kc-link\" data-slug=\"loop-is-cron-plus-a-decision-maker\">loop is cron plus a decision-maker</a>).</li></ul>\n<h2>Provenance</h2>\n<p>Distilled from official docs (<code>code.claude.com/docs/en/{scheduled-tasks,goal,routines,hooks}</code>), retrieved 2026-06-09, and cross-checked against the loops discourse the <a href=\"#\" class=\"kc-link\" data-slug=\"loop-is-cron-plus-a-decision-maker\">Matt Van Horn article</a> synthesizes. <code>/goal</code> requires Claude Code v2.1.139+; scheduled tasks require v2.1.72+.</p>\n<h2>Connections</h2>\n<ul><li><strong>contrasts-with <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-routines\">Claude Code Routines</a></strong> — cloud/unattended vs in-session local: the two opposite corners of the grid.</li><li><strong>builds-on <a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">Never let the agent declare its own victory</a></strong> — <code>/goal</code>'s condition axis only works because a separate evaluator owns \"done.\"</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"designing-loops-not-prompts\">Loop engineering</a></strong> — these are the scheduling/heartbeat block of a loop; the spectrum says which to pick.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"loop-is-cron-plus-a-decision-maker\">A loop is cron plus a decision-maker</a></strong> — that note defines what a loop is; this one separates the concrete primitives.</li></ul>",
      "connCount": 6
    },
    {
      "slug": "bootstrapping-company-ai-stack",
      "title": "Scenario — bootstrapping a company's AI stack from zero",
      "kind": "scenario",
      "layer": "products",
      "tags": [
        "consulting",
        "scenario",
        "rollout",
        "mcp",
        "build-vs-buy",
        "enterprise-stack",
        "skills",
        "hooks"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/bootstrapping-company-ai-stack.md",
      "html": "<h1>Scenario — bootstrapping a company's AI stack from zero</h1>\n<blockquote><p><em>\"A small/medium company reaches out: they have no idea how to get an AI stack going. Get their employees set up with the basic infrastructure to start building — with custom skills, hooks, MCPs for customer, business, and operational use cases.\"</em> — the canonical first engagement.</p></blockquote>\n<h2>TL;DR</h2>\n<p>You don't sell them \"an AI.\" You install <strong>layers and habits</strong>: give people capable agents now, wire those agents to the company's own data, codify conventions as shared skills/hooks/<code>CLAUDE.md</code>, then build the few differentiated agents that are actually worth building. Most of it is <strong>buy + configure</strong>; only the differentiated slice is <strong>build</strong>. The decision engine is <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own harness — the four-altitude decision</a>.</p>\n<h2>Phase 0 — Discovery (the questions that scope everything)</h2>\n<ul><li><strong>Who builds?</strong> Ratio of developers to business users — decides Claude Code/SDK vs a bought low-code platform (Copilot Studio / Dust / Le Chat).</li><li><strong>Where do you already live?</strong> Cloud (AWS/GCP/Azure), model provider, primary language — pick the runtime that fits (Bedrock AgentCore / Vertex Agent Engine / Azure Foundry) instead of fighting it.</li><li><strong>What data + systems?</strong> The list of internal APIs/DBs/SaaS becomes your MCP-server backlog.</li><li><strong>Regulatory posture?</strong> PII/HIPAA/data-residency → on-prem/VPC options (Cohere North, self-host, in-cloud SDK auth via Bedrock/Vertex/Foundry).</li><li><strong>Three use-case buckets</strong> (sort every idea into these — they map to different altitudes):<ul><li><strong>Operational</strong> (internal productivity: coding, ops, support triage) → mostly <em>buy/use</em> (Claude Code for devs, enterprise chat for everyone).</li><li><strong>Business</strong> (internal workflows over company data: reporting, knowledge, automation) → <em>configure</em> (Agent SDK / platform + internal MCP servers).</li><li><strong>Customer</strong> (in-product, customer-facing agents) → <em>configure/own</em> (Agent SDK or own-the-loop, with guardrails + eval as non-negotiable).</li></ul></li></ul>\n<h2>The rollout (each phase = one altitude of <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own harness — the four-altitude decision</a>)</h2>\n<ol><li><strong>Give people capable agents now (BUY/USE).</strong> Devs → <strong>Claude Code</strong> (CLI; MCP-capable). Everyone else → an enterprise chat assistant (Claude / ChatGPT Enterprise / Copilot / Le Chat / Dust). Immediate productivity, zero build. <em>Win in week one.</em></li><li><strong>Wire agents to company data (the integration backbone).</strong> Wrap internal APIs/Lambda/DBs as <strong>MCP servers</strong> (<a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol (MCP) — From Zero to Plumbing</a>); AWS <strong>AgentCore Gateway</strong> is the managed \"APIs/Lambda → MCP tools\" path; discover via the <strong>MCP Registry</strong>. This is what turns generic chat into <em>your company's</em> assistant.</li><li><strong>Codify conventions as shared primitives.</strong> A company-standard <code>CLAUDE.md</code>, a shared <strong>skills</strong> library (the team's repeatable procedures), and <strong>hooks</strong> as guardrails (the Tide pattern: a hook enforces the mechanically-checkable slice of a policy). Now every employee's agent inherits the same standards. See <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a>.</li><li><strong>Build the differentiated agents (CONFIGURE/OWN).</strong> Only the customer/business cases that a bought tool can't do well → the <strong>Agent SDK</strong> (<a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK — build your own harness on the same engine as Claude Code</a>) or raw API, deployed on a managed runtime, embedded in the product. This is the small, high-value build slice — not the whole stack.</li><li><strong>Stand up the cross-cutting platform layers</strong> as usage grows (the <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI System — Reference Architecture</a> in practice):<ul><li><strong>Model gateway/router</strong> — LiteLLM / OpenRouter / Portkey (one interface, fallbacks, cost control).</li><li><strong>Observability + eval</strong> — Langfuse (OSS) / LangSmith / Braintrust / Arize (OpenTelemetry-based).</li><li><strong>Guardrails</strong> — NeMo Guardrails / Guardrails AI (PII, jailbreak, topic, hallucination rails).</li><li><strong>Identity/policy</strong> — AgentCore Identity (Cognito/Okta/Entra) + Cedar policy, or your IdP.</li></ul></li></ol>\n<h2>Build-vs-buy, said plainly</h2>\n<ul><li><strong>Non-technical org / fast value</strong> → <em>buy</em> a platform (Copilot Studio, Dust, Le Chat, North).</li><li><strong>Technical org / differentiated need</strong> → <em>configure</em> on a framework (Agent SDK / LangGraph / ADK / Agent Framework) over a managed runtime (AgentCore / Vertex Agent Engine / Anthropic Managed Agents).</li><li><strong>Either way</strong> → start at the highest altitude that works and descend only when forced.</li></ul>\n<h2>Mental model / why it matters</h2>\n<p>This is the <strong>products layer</strong> of the <a href=\"#\" class=\"kc-link\" data-slug=\"big-picture\">The Big Picture — how the Claude stack fits together</a> spine — where the theory becomes billable. The engagement's real product isn't a model; it's <strong>infrastructure + habits</strong>: agents in hand, data wired in via MCP, conventions encoded as skills/hooks, and a deliberate (small) build list. It's the <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own harness — the four-altitude decision</a> decision tree, run across a whole org and sequenced into phases.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Lead with Phase 1 + 2</strong> — capable agents + internal MCP servers buy trust fast and are mostly configuration, not building.</li><li><strong>Resist building in Phase 4 until Phases 1–3 are real.</strong> Most \"we need a custom AI\" asks are solved by an MCP server + a skill, not a bespoke agent.</li><li><strong>Make eval/guardrails a Phase-4 entry gate</strong> for anything customer-facing — no eval, no ship.</li><li><strong>Leave them self-sufficient:</strong> the deliverable is a shared skills/hooks/<code>CLAUDE.md</code> repo their own people can extend — exactly the kind of self-organizing base this knowledge center models.</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>used-in</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own</a> — the decision engine for the whole rollout.</li><li><strong>used-in</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol</a> — Phase 2 integration backbone.</li><li><strong>used-in</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a> — the target end-state.</li><li><strong>used-in</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — Phase 1 + the shared skills/hooks/CLAUDE.md.</li></ul>",
      "connCount": 4
    },
    {
      "slug": "claude-agent-sdk",
      "title": "The Claude Agent SDK — build your own harness on the same engine as Claude Code",
      "kind": "concept",
      "layer": "agent-sdk",
      "tags": [
        "agent-sdk",
        "claude-code",
        "hooks",
        "subagents",
        "mcp",
        "sessions",
        "headless"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/claude-agent-sdk.md",
      "html": "<h1>The Claude Agent SDK — build your own harness on the same engine as Claude Code</h1>\n<h2>TL;DR</h2>\n<p>The <strong>Claude Agent SDK</strong> (Python + TypeScript) gives you <em>\"the same tools, agent loop, and context management that power Claude Code\"</em> — as a library you embed in your own app. It's the answer to \"do I have to build my own harness?\": <strong>mostly no — you configure one.</strong> Renamed from \"Claude Code SDK\" in 2025 when Anthropic generalized it beyond coding. This is the missing middle between hand-rolling the <a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a> and using the finished <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a>.</p>\n<h2>What it gives you (the primitives you already know — now programmatic)</h2>\n<p>Everything you learned as Claude Code \"features\" is an SDK API surface:</p>\n<ul><li><strong>Built-in tools</strong> — Read, Write, Edit, Bash, Glob, Grep, WebSearch, WebFetch, Monitor, AskUserQuestion.</li><li><strong>Hooks</strong> — <code>PreToolUse</code>, <code>PostToolUse</code>, <code>Stop</code>, <code>SessionStart</code>, <code>SessionEnd</code>, <code>UserPromptSubmit</code>, … to validate / log / block / transform. (Same hooks as your Tide guardrails.)</li><li><strong>Subagents</strong> — define specialized agents (<code>AgentDefinition</code>) invoked via the <code>Agent</code> tool; messages carry <code>parent_tool_use_id</code> for attribution. See <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a>.</li><li><strong>MCP</strong> — connect external systems via <code>mcp_servers</code> (stdio command or remote) — <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol (MCP) — From Zero to Plumbing</a>.</li><li><strong>Permissions</strong> — <code>allowed_tools</code> pre-approves; <code>permission_mode</code> (e.g. <code>acceptEdits</code>); plus interactive approval flows.</li><li><strong>Sessions</strong> — context persists; capture <code>session_id</code> from the <code>init</code> message, then <code>resume</code> or fork. Session state is JSONL on your filesystem.</li><li><strong>Filesystem config</strong> — loads <code>.claude/</code> + <code>~/.claude/</code>: <strong>Skills</strong> (<code>SKILL.md</code>), Commands, <strong>Memory (<code>CLAUDE.md</code>)</strong>, Plugins. <code>setting_sources</code> controls what loads.</li></ul>\n<h2>Install / run / deploy (datestamped 2026-06)</h2>\n<ul><li><strong>Python:</strong> <code>pip install claude-agent-sdk</code> (Python ≥3.10). <strong>TypeScript:</strong> <code>npm install @anthropic-ai/claude-agent-sdk</code> (bundles a native Claude Code binary as an optional dep — no separate install).</li><li><strong>CLI vs SDK vs raw API:</strong> same capabilities, different interface. Anthropic's own split — <strong>CLI</strong> for interactive dev / one-off tasks; <strong>SDK</strong> for CI/CD, custom apps, production automation; the <strong>Client SDK / raw API</strong> when you must own the loop. Headless path: <code>claude -p</code> (print mode).</li><li><strong>Managed Agents</strong> — a hosted REST option where Anthropic runs the agent loop <em>and</em> a per-session sandbox, for when you don't want to operate session/sandbox infra yourself.</li><li><strong>Auth for production:</strong> API key, or Amazon Bedrock (<code>CLAUDE_CODE_USE_BEDROCK=1</code>), Google Vertex (<code>..._VERTEX=1</code>), Azure AI Foundry (<code>..._FOUNDRY=1</code>).</li><li><strong>Watch-outs:</strong> as of <strong>2026-06-15</strong>, Agent SDK + <code>claude -p</code> usage on subscription plans draws from a separate monthly <strong>\"Agent SDK credit\"</strong> (budget for it). Branding rules forbid partners calling their product \"Claude Code.\"</li></ul>\n<h2>Mental model / why it matters</h2>\n<p>This is the <strong>agent-sdk layer</strong> of the <a href=\"#\" class=\"kc-link\" data-slug=\"big-picture\">The Big Picture — how the Claude stack fits together</a> spine — the framework that turns the <a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a> into a configurable harness. The big unlock for the consulting goal: <strong>Claude Code is not the only thing you can build with the machinery behind Claude Code.</strong> Same loop, your product, your UX, your tools — non-developer end users, a branded chat surface, a CI job, a customer-facing agent. The skills/hooks/MCP/subagent concepts transfer 1:1; you're swapping the <em>interface</em>, not relearning the <em>primitives</em>.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>The \"graduation path\":</strong> prototype on the <a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a> → adopt the Agent SDK when you need managed sessions/permissions/hooks → keep Claude Code (the CLI) for the dev team in parallel. See the decision tree in <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own harness — the four-altitude decision</a>.</li><li><strong>Embed, don't rebuild:</strong> when a client needs an internal \"agent over our systems,\" the SDK + their MCP servers gets there without a bespoke orchestration layer.</li><li><strong>Deploy near the model:</strong> Bedrock/Vertex/Foundry auth lets a regulated client run the SDK inside their existing cloud + compliance boundary.</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">Claude API agent primitives</a> — wraps the raw tool-use loop.</li><li><strong>enables</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — the SDK is the harness that powers Claude Code.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol</a> — external systems via <code>mcp_servers</code>.</li><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a> — ships the 12 organs as primitives.</li></ul>",
      "connCount": 6
    },
    {
      "slug": "claude-api-agent-primitives",
      "title": "The Claude API agent primitives — the engine the harness runs on",
      "kind": "concept",
      "layer": "api",
      "tags": [
        "claude-api",
        "tool-use",
        "mcp",
        "prompt-caching",
        "batch",
        "agents",
        "foundations"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/claude-api-agent-primitives.md",
      "html": "<h1>The Claude API agent primitives — the engine the harness runs on</h1>\n<h2>TL;DR</h2>\n<p>Below every harness is the <strong>raw Claude API</strong>: the Messages API plus a handful of capabilities you can call with no framework at all. The agent \"loop\" is literally <strong><code>while response.stop_reason === \"tool_use\": run the tool, feed the result back</code></strong>. Anthropic's own guidance: <em>start here</em> — \"many patterns can be implemented in a few lines of code.\" Everything above this layer (Agent SDK, Claude Code) is convenience wrapped around these primitives.</p>\n<h2>The primitives</h2>\n<ul><li><strong>Tool use (function calling).</strong> You declare tools as JSON schemas in the request. The model replies with <code>stop_reason: \"tool_use\"</code> and a structured tool call; <em>you</em> execute it and append the result as a <code>tool_result</code> message, then call again. That hand-rolled loop <strong>is</strong> an agent. This is the single most important primitive — it's the seam where the model reaches the world.</li><li><strong>MCP connector.</strong> The Messages API can connect to <strong>remote MCP servers</strong> directly, so a bare API call gets tools without you writing per-tool schemas — see <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol (MCP) — From Zero to Plumbing</a>. (Anthropic's <code>build-with-claude/mcp</code> page now redirects to modelcontextprotocol.io.)</li><li><strong>Prompt caching.</strong> Mark stable prefixes (system prompt, tool defs, long context) as cacheable to cut cost/latency on repeated calls — essential once an agent loops many turns over the same context.</li><li><strong>Batch API.</strong> Submit large async jobs (classification, extraction, evals) at lower cost when you don't need an interactive turn.</li><li><strong>Computer use.</strong> A built-in <code>computer</code> tool for screen control (screenshots + mouse/keyboard) — the primitive behind \"the agent drives a GUI.\"</li><li><strong>Extended thinking, 1M context, citations, files</strong> — request-level capabilities the loop can opt into. Datestamp these; they move (see <a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK — build your own harness on the same engine as Claude Code</a> for the SDK that surfaces them).</li></ul>\n<h2>Mental model / why it matters</h2>\n<p>This is the <strong>engine layer</strong> of the <a href=\"#\" class=\"kc-link\" data-slug=\"big-picture\">The Big Picture — how the Claude stack fits together</a> spine — the floor with the fewest abstractions and the most control. The takeaway that dissolves a lot of \"which framework?\" anxiety: <strong>an agent is not a product you buy, it's a loop you can write in ~20 lines.</strong> A framework is worth adopting only when it earns its abstraction back; if you can't see your own prompts and tool results, you can't debug the agent. Owning the loop at this layer is the <em>baseline</em> against which every higher layer (SDK, Claude Code, LangGraph, …) should justify itself.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Prototype on the raw API first</strong> to learn a client's problem shape before committing to any framework — cheap, transparent, throwaway.</li><li><strong>Reach for prompt caching early</strong> on any looping agent; it's the biggest single cost lever and is invisible at the framework layer if you don't know it exists.</li><li><strong>Use \"can I express this as a few-line tool-use loop?\" as a litmus test</strong>: if yes, a heavyweight agent framework is probably premature — see <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own harness — the four-altitude decision</a>.</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>enables</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">Claude Agent SDK</a> — the SDK automates this exact loop.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol</a> — the MCP connector lives here.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a> — these are the API-level harness organs.</li></ul>",
      "connCount": 5
    },
    {
      "slug": "claude-code-architecture",
      "title": "Claude Code Architecture — field guide to the primitives",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "claude-code",
        "agents",
        "harness",
        "skills",
        "hooks",
        "mcp",
        "workflows",
        "architecture"
      ],
      "depth": "evergreen",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/claude-code-architecture.md",
      "html": "<h1>Claude Code Architecture — field guide</h1>\n<p>The markdown companion to the interactive site at <span class=\"kc-ref\"><code>/docs/concepts/</code></span>. This is the distilled, greppable version; the site is the clickable one.</p>\n<h2>The one idea</h2>\n<p>Claude Code is a <strong>harness</strong> wrapped around a model. Every named feature exists to control <strong>one of four things</strong>:</p>\n<ol><li><strong>What the model knows</strong> — soft context &amp; instructions (CLAUDE.md, rules files, skills, output styles, effort). <em>Model-mediated: it may comply.</em></li><li><strong>What the model can reach</strong> — tools &amp; external systems (built-in tools, MCP servers).</li><li><strong>What is guaranteed</strong> — deterministic enforcement (hooks, permission rules, managed policies). <em>Client-enforced regardless of the model.</em></li><li><strong>How work is coordinated</strong> — single-thread → fleet (subagents, agent view, agent teams, dynamic workflows, worktrees).</li></ol>\n<h2>The two axes</h2>\n<ul><li><strong>Soft vs Hard.</strong> Does the model <em>choose</em> to follow it (CLAUDE.md, skills) or does the client <em>enforce</em> it no matter what (hooks, policies)? Soft text can never be a guardrail.</li><li><strong>Who holds the plan.</strong> You in one prompt → the model in one session → the model delegating (subagents) → you supervising many (agent view) → a lead coordinating peers (agent teams) → a script (dynamic workflows). Further right = more parallel, less watched.</li></ul>\n<h2>How it nests</h2>\n<pre><code>managed policies            ← hard ceiling over everything (org governance)\n└── harness (one session)   ← Gather → Act → Verify loop\n    ├── memory / CLAUDE.md   ← soft context in Gather\n    ├── skills               ← procedures pulled into Act on demand\n    ├── MCP servers          ← extra tools available in Act\n    ├── hooks                ← deterministic code at lifecycle events\n    └── subagents            ← delegated workers (own context, summary returns)\n  ...when one session isn't enough:\n    agent view  = you supervising MANY sessions\n    agent teams = a LEAD coordinating PEER sessions that message each other\n    workflows   = a SCRIPT spawning dozens–hundreds of subagents\n    worktrees   = git isolation UNDER all of the above</code></pre>\n<h2>The ten primitives (one line each)</h2>\n<table><thead><tr><th>Primitive</th><th>One line</th><th>Enforcement</th></tr></thead><tbody><tr><td><strong>Conventions</strong> (CLAUDE.md / rules files)</td><td>Soft markdown guidance read every session</td><td>Soft</td></tr><tr><td><strong>Policies</strong> (managed settings + permission rules)</td><td>Client-enforced governance; can't be overridden</td><td><strong>Hard</strong></td></tr><tr><td><strong>Hooks</strong></td><td>Your code, run on lifecycle events</td><td><strong>Hard</strong></td></tr><tr><td><strong>Skills</strong></td><td>Reusable procedures loaded on demand (what \"a routine\" usually means)</td><td>Soft</td></tr><tr><td><strong>MCP</strong></td><td>Protocol giving the model new tools &amp; data</td><td>Tools model-invoked</td></tr><tr><td><strong>Subagents</strong></td><td>Delegated worker; only a summary returns</td><td>Soft</td></tr><tr><td><strong>Agent teams</strong></td><td>Lead + peer sessions that message each other</td><td>Soft</td></tr><tr><td><strong>Dynamic workflows</strong></td><td>A script Claude writes that orchestrates many agents</td><td>Soft</td></tr><tr><td><strong>Worktrees + Agent view</strong></td><td>Git isolation + a dashboard to supervise many sessions</td><td>Soft</td></tr><tr><td><strong>Routines</strong></td><td>Saved config run unattended on Anthropic's cloud on a trigger (schedule / GitHub / API)</td><td>Soft (reach hard-bounded)</td></tr></tbody></table>\n<h2>When to reach for each (free-form, not a ladder)</h2>\n<p>There's no fixed order to learn these — go broad or deep as a real need appears. The one piece of sequencing advice that holds: <strong>start with CLAUDE.md and add each primitive only when a concrete trigger shows up</strong> (see the trigger ladder on the site's Decide page).</p>\n<ul><li><em>Drive one session well:</em> CLAUDE.md / memory, slash commands, the Explore→Plan→Code→Commit loop.</li><li><em>Extend a session:</em> skills, MCP, hooks, subagents.</li><li><em>Orchestrate at scale &amp; govern:</em> agent teams, dynamic workflows, worktrees + agent view, plugins, policies.</li><li><em>Operate unattended:</em> routines — a saved config the cloud runs on a schedule / GitHub event / API call.</li></ul>\n<h2>Terminology corrections (common confusions)</h2>\n<ul><li><strong>Conventions ≠ Policies.</strong> CLAUDE.md is <em>soft</em> guidance; managed settings are <em>hard</em>, client-enforced. Not synonyms.</li><li><strong>\"Routines\" now names two different things.</strong> <em>Colloquially</em>, a saved procedure you call \"a routine\" → a <em>skill</em> (orchestration → <em>dynamic workflow</em>; default voice/format → <em>output style</em>). <em>Officially</em>, <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-routines\">Routines</a> is a real feature: a saved config run unattended on Anthropic's cloud on a schedule / GitHub event / API call — not a saved procedure.</li><li><strong>Rules ≠ Hooks.</strong> Three things wear \"rule\": <em>hooks</em> (code that runs), <em>permission rules</em> (allow/ask/deny config), and <code>.claude/rules/*.md</code> (soft memory). Only a hook executes code.</li></ul>\n<h2>When to use what (fast)</h2>\n<ul><li>A <em>fact</em> always needed → CLAUDE.md. A <em>procedure</em> sometimes → skill. A <em>guardrail</em> → hook/permission rule.</li><li><em>Reach</em> an external system → MCP. <em>Offload</em> a noisy side task → subagent.</li><li>Peers that must <em>talk</em> → agent team. <em>Massive fan-out</em> + cross-check → dynamic workflow.</li><li>Many <em>independent</em> sessions you supervise → agent view (+ worktrees to isolate files).</li></ul>\n<h2>Sources</h2>\n<ul><li><a href=\"https://code.claude.com/docs/en/features-overview\" target=\"_blank\" rel=\"noopener\">Features overview / trigger ladder</a></li><li><a href=\"https://code.claude.com/docs/en/agents\" target=\"_blank\" rel=\"noopener\">Run agents in parallel</a> · <a href=\"https://code.claude.com/docs/en/workflows\" target=\"_blank\" rel=\"noopener\">Dynamic workflows</a></li><li><a href=\"https://code.claude.com/docs/en/sub-agents\" target=\"_blank\" rel=\"noopener\">Subagents</a> · <a href=\"https://code.claude.com/docs/en/agent-teams\" target=\"_blank\" rel=\"noopener\">Agent teams</a> · <a href=\"https://code.claude.com/docs/en/agent-view\" target=\"_blank\" rel=\"noopener\">Agent view</a> · <a href=\"https://code.claude.com/docs/en/worktrees\" target=\"_blank\" rel=\"noopener\">Worktrees</a></li><li><a href=\"https://code.claude.com/docs/en/skills\" target=\"_blank\" rel=\"noopener\">Skills</a> · <a href=\"https://code.claude.com/docs/en/mcp\" target=\"_blank\" rel=\"noopener\">MCP</a> · <a href=\"https://code.claude.com/docs/en/hooks\" target=\"_blank\" rel=\"noopener\">Hooks</a> · <a href=\"https://code.claude.com/docs/en/memory\" target=\"_blank\" rel=\"noopener\">Memory</a> · <a href=\"https://code.claude.com/docs/en/settings\" target=\"_blank\" rel=\"noopener\">Settings</a></li><li><a href=\"https://claude.com/blog/introducing-dynamic-workflows-in-claude-code\" target=\"_blank\" rel=\"noopener\">Dynamic workflows announcement</a> · <a href=\"https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills\" target=\"_blank\" rel=\"noopener\">Agent Skills</a></li></ul>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a> — the loop every primitive plugs into.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol</a> — the model's reach into external tools.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a> — the \"coordinate work\" primitives.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the Ultraplan loop</a> — script-orchestrates-many-agents.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-routines\">Claude Code Routines</a> — the unattended cloud \"operate\" primitive.</li></ul>",
      "connCount": 18
    },
    {
      "slug": "claude-code-multi-agent-development",
      "title": "Claude Code — Multi-Agent Development",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "claude-code",
        "agents",
        "multi-agent",
        "worktrees",
        "subagents",
        "orchestration"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/claude-code-multi-agent-development.md",
      "html": "<h1>Claude Code — Multi-Agent Development</h1>\n<p><img src=\"notes/claude-code-multi-agent-development.jpg\" alt=\"Claude Code multi-agent development methods\" loading=\"lazy\"></p>\n<blockquote><p>Source: Alpha Clarity, LLC presentation (© 2026), by @avthar (x.com/@avthar · youtube.com/@avtharai).</p></blockquote>\n<h2>TL;DR</h2>\n<p>Three ways to run more than one Claude at once, chosen by how much <strong>coordination</strong> the work needs: isolated parallel instances (worktrees), a coordinating team (Agent Teams), or fire-and-forget child agents (parallel subagents).</p>\n<h2>The three methods</h2>\n<table><thead><tr><th></th><th>Multiple Claude Codes w/ Git Worktrees</th><th>Agent Teams <em>(experimental)</em></th><th>Parallel Subagents</th></tr></thead><tbody><tr><td><strong>Overview</strong></td><td>Separate Claude instances running in parallel, each in its own isolated worktree on the same repo</td><td>A team lead delegates tasks to teammates that work in parallel from a shared task list</td><td>Child instances spawned via the Agent tool to handle focused jobs and return results</td></tr><tr><td><strong>Coordination</strong></td><td>None — each instance is isolated from the rest</td><td>Multi-way — leads assign tasks &amp; synthesize results; teammates can message lead and each other</td><td>One-way — subagents report back to the parent thread</td></tr><tr><td><strong>Best for</strong></td><td>Building multiple independent features in parallel</td><td>Building one complex feature that needs coordination across components</td><td>Research, investigation, analysis where you just need answers back</td></tr><tr><td><strong>How to use it</strong></td><td><code>claude -w feature-name</code></td><td>\"Create an Agent Team for Task X. Spawn teammates for A, B and C\"</td><td>\"Use parallel subagents to do X\"</td></tr></tbody></table>\n<p><strong>Rule of thumb:</strong> more independence → worktrees; more coordination → agent teams; just need an answer → subagents.</p>\n<h2>5 things to keep in mind (Multi-Claude)</h2>\n<p><img src=\"notes/claude-code-multi-agent-5-things.jpg\" alt=\"Multi Claude — 5 things to keep in mind\" loading=\"lazy\"></p>\n<ol><li><strong>Git worktrees apply to your project folder, not external systems</strong> (DBs, deployed services, etc. are shared).</li><li><strong>Parallel Claude sessions consume more tokens.</strong></li><li><strong>Untracked files (e.g. <code>.env</code>) don't auto-carry into new worktrees</strong> — use <code>.worktreeinclude</code>.</li><li><strong>Worktrees are easier with web apps (e.g. Next.js), less so for iOS / Mac apps.</strong></li><li><strong>Don't keep old worktrees lying around — clean up after use.</strong></li></ol>\n<h2>Why it matters / how I'd apply it</h2>\n<p>A clean decision framework for <em>when</em> to parallelize Claude Code work, plus the operational gotchas that bite in practice. Pick the method by coordination need — don't reach for an Agent Team when isolated worktrees suffice, and don't burn tokens on parallel sessions for work that's actually sequential. Watch the worktree footguns (#3 <code>.env</code> and #5 cleanup) — they map onto the harness/isolation behavior this very knowledge repo runs under.</p>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>part-of</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — worktrees, subagents and agent teams are the \"coordinate work\" primitives.</li><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a> — subagents live in the harness ACT step.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a> — multi-agent patterns for the Agent Layer (3).</li></ul>",
      "connCount": 9
    },
    {
      "slug": "claude-code-routines",
      "title": "Claude Code Routines — unattended cloud automation",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "claude-code",
        "routines",
        "automation",
        "scheduling",
        "triggers",
        "cloud",
        "github",
        "mcp"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/claude-code-routines.md",
      "html": "<h1>Claude Code Routines — unattended cloud automation</h1>\n<h2>TL;DR</h2>\n<p>A <strong>routine</strong> is a <em>saved Claude Code configuration</em> — a prompt + one or more repositories + connectors/MCP — packaged once and run <strong>automatically on Anthropic-managed cloud infrastructure</strong>, with your laptop closed. Each routine fires on one or more <strong>triggers</strong>: a schedule, a GitHub event, or an API call. It's part of <em>Claude Code on the web</em>, and it's in <strong>research preview</strong> (2026-06-06).</p>\n<h2>The mental model</h2>\n<p>Interactive Claude Code = you in the loop, approving as you go. A routine = \"set it and forget it.\" The plan and the tools are frozen into a saved config; a trigger pulls it off the shelf and runs it <strong>unattended in the cloud</strong>. The unit of value moves from <em>a session you drive</em> to <em>a standing order that runs itself</em>.</p>\n<h2>Three triggers (combinable in one routine)</h2>\n<ul><li><strong>Scheduled</strong> — a recurring cadence (hourly / daily / weekdays / weekly; minimum interval <strong>1 hour</strong>; custom cron via <code>/schedule update</code>), or a single <strong>one-off</strong> run at a future timestamp.</li><li><strong>API</strong> — a per-routine HTTPS endpoint. <code>POST .../routines/&lt;id&gt;/fire</code> with a bearer token and an optional freeform <code>text</code> payload (e.g. an alert body) starts a run and returns a session URL. Ships behind an experimental beta header.</li><li><strong>GitHub</strong> — runs on repository events (<code>pull_request.*</code>, <code>release.*</code>) with filters (author, base/head branch, labels, draft/merged, etc.). Each matching event starts its own session.</li></ul>\n<p>One routine can attach all three (e.g. a PR-review routine that also runs nightly and can be fired from a deploy script).</p>\n<h2>Where it's managed</h2>\n<p>Create and manage at <strong>claude.ai/code/routines</strong>, in the <strong>Desktop app</strong>, or from the CLI with <strong><code>/schedule</code></strong> (<code>/schedule list | update | run</code>). The CLI creates <strong>scheduled</strong> triggers only — API and GitHub triggers are added on the web. All three surfaces write to the same cloud account.</p>\n<h2>Reach &amp; safety</h2>\n<p>A run is a <strong>fully autonomous cloud session: no permission prompts, no approval mode.</strong> What it can touch is fixed <em>before</em> it runs, by four knobs:</p>\n<ul><li><strong>Repositories</strong> selected (cloned per run from the default branch) + the branch-push setting — by default it can only push to <strong><code>claude/</code>-prefixed branches</strong> unless <em>Allow unrestricted branch pushes</em> is enabled.</li><li><strong>Environment</strong> — network access (default <strong>Trusted</strong>: package registries + common dev domains only), environment variables/secrets, and a cached setup script.</li><li><strong>Connectors</strong> included (MCP). All tools of an included connector are usable, <strong>including writes</strong>, with no per-run approval.</li></ul>\n<p>A routine belongs to your <strong>individual</strong> claude.ai account (not shared), and everything it does appears <strong>as you</strong> (your GitHub identity, your connectors). Team/Enterprise admins can disable routines org-wide.</p>\n<h2>Usage &amp; cost</h2>\n<p>Routines draw down subscription usage like interactive sessions, <strong>plus a per-account daily run cap</strong>. One-off runs are exempt from the daily cap (but still consume regular usage). Available on Pro / Max / Team / Enterprise with Claude Code on the web enabled.</p>\n<h2>The terminology collision (read this)</h2>\n<p>If you've ever called a <em>saved, repeatable procedure</em> \"a routine,\" you meant a <strong><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Skill</a></strong> — that long-standing correction still holds. But Anthropic now <em>also</em> ships a feature <strong>literally named Routines</strong>, and it's a different thing: <strong>unattended cloud automation</strong> triggered on a schedule / GitHub event / API call — not a procedure loaded into a session. The clean split: <strong>a Skill is the <em>what to do</em>; a Routine is the <em>when &amp; where it runs unattended</em>.</strong> A routine can even invoke skills (and connectors) inside its cloud run.</p>\n<h2>When to reach for it / when not</h2>\n<ul><li><strong>Reach for it:</strong> recurring unattended maintenance (triage, changelogs, docs-drift PRs); Claude reacting to a GitHub event on its own; an external system firing a Claude run via an authenticated webhook; any \"should keep working with my laptop closed\" job.</li><li><strong>Not</strong> for a task that repeats <em>inside an open session</em> → <code>/loop</code> (local, in-session). <strong>Not</strong> for a schedule that must run <em>on your own machine</em> → Desktop scheduled tasks. <strong>Not</strong> for a pure pass/fail CI gate → GitHub Actions.</li></ul>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>part-of</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — the field guide's unattended \"operate\" primitive.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns</a> — <code>/loop</code> is the in-session local cousin of a scheduled routine.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol (MCP)</a> — connectors/MCP bundled into the saved config give the cloud run its reach.</li><li><strong>contrasts-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">One shared context vs. many isolated contexts</a> — routine runs have no permission prompts/approval mode, unlike interactive sessions.</li><li><strong>alternative-to</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a> — unattended cloud trigger vs. supervised local agents.</li></ul>",
      "connCount": 9
    },
    {
      "slug": "claude-code-vs-build-your-own",
      "title": "Use Claude Code vs build your own harness — the four-altitude decision",
      "kind": "concept",
      "layer": "patterns",
      "tags": [
        "decision-framework",
        "harness",
        "agent-sdk",
        "build-vs-buy",
        "consulting",
        "frameworks"
      ],
      "depth": "evergreen",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/claude-code-vs-build-your-own.md",
      "html": "<h1>Use Claude Code vs build your own harness — the four-altitude decision</h1>\n<h2>TL;DR</h2>\n<p>\"Should I keep using Claude Code or build my own harness?\" is a <strong>false binary</strong>. There are <strong>four altitudes</strong> of the same engine, and the right answer for a real org is usually <strong>several at once, in layers</strong>. The skill is matching each <em>use case</em> to the <em>lowest altitude that solves it</em> — not picking one tool for everything. Crucially: <strong>nothing you learned about Claude Code is wasted</strong> — skills, hooks, MCP, subagents, <code>CLAUDE.md</code> are the primitives at <em>every</em> altitude.</p>\n<h2>The four altitudes (same engine, rising abstraction)</h2>\n<pre><code>BUY a finished product   ── Copilot Studio · Dust · Le Chat · (Claude Code, for devs)\n   ▲ fastest time-to-value, least control, non-developers\nUSE the finished harness ── Claude Code CLI / Managed Agents\n   ▲ great for technical + internal + interactive work, zero build\nCONFIGURE a harness      ── Claude Agent SDK  (or LangGraph / OpenAI Agents SDK / Google ADK / MS Agent Framework)\n   ▲ your product/UX, managed loop, you own tools+policy\nOWN the loop             ── raw Claude API tool-use loop  (claude-api-agent-primitives)\n   ▲ maximum control, you write the orchestration; the baseline everything else must beat</code></pre>\n<p>Each rung trades <strong>control for leverage</strong>. Lower = more control, more code. Higher = faster, less control. (See <a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK — build your own harness on the same engine as Claude Code</a> for the middle rungs and <a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives — the engine the harness runs on</a> for the floor.)</p>\n<h2>The decision tree</h2>\n<ol><li><strong>Are the steps predictable?</strong> → it's a <em>workflow</em>, not an agent — write deterministic code with LLM steps inside it. (Anthropic: don't reach for an autonomous agent when a fixed path works.)</li><li><strong>Is the user a developer doing technical/internal work?</strong> → <strong>use Claude Code</strong> (the CLI). Don't build anything. This is the default and it's already what you do every day.</li><li><strong>Do you need a custom product surface</strong> (branded UI, non-dev end users, embedded in your app, customer-facing)? → <strong>configure a harness on the Agent SDK</strong> — same primitives, your interface.</li><li><strong>Did you hit the framework's ceiling</strong> (need full control of prompts/context/control-flow, or the abstraction is obscuring your bug)? → <strong>drop to the raw API and own the loop.</strong></li><li><strong>Is fast time-to-value over company data for business users the goal?</strong> → <strong>buy a platform</strong> (Copilot Studio / Dust / Le Chat / Cohere North) rather than build.</li></ol>\n<h2>The two canonical framings (read these)</h2>\n<ul><li><strong>Anthropic — <em>Building Effective Agents</em>:</strong> \"the most successful implementations weren't using complex frameworks… they were building with simple, composable patterns.\" <strong>Start with the API directly</strong>; add complexity only when a simpler system <em>demonstrably</em> underperforms; agents add latency + cost and need sandboxing/guardrails; prioritize transparency of planning steps.</li><li><strong>12-Factor Agents (HumanLayer/Dex):</strong> production agents are \"<strong>mostly just software</strong>\" with LLM steps strategically placed. Builders \"hit an <strong>80% quality ceiling</strong> with frameworks, then must reverse-engineer to proceed.\" Advocates <strong>selective, principle-based</strong> adoption — own your prompts, own your context window, own your control flow — over wholesale framework lock-in.</li></ul>\n<h2>The non-Anthropic options (so you can advise vendor-neutrally) — datestamped 2026-06</h2>\n<p>At the \"configure a harness\" rung, the Agent SDK competes with: <strong>LangGraph</strong> (stateful/resumable graph runtime + LangSmith tracing), <strong>OpenAI Agents SDK + Responses API + AgentKit</strong>, <strong>Google ADK</strong> (polyglot, + A2A + Vertex Agent Engine), <strong>Microsoft Agent Framework</strong> (the merged successor to AutoGen + Semantic Kernel, .NET/Python), <strong>CrewAI</strong> (role-based multi-agent), and framework-agnostic managed runtimes like <strong>AWS Bedrock AgentCore</strong> and <strong>Vertex Agent Engine</strong>. Pick by where the org already lives (cloud, model provider, language) — not by hype.</p>\n<h2>Mental model / why it matters</h2>\n<p>This note is the <strong>patterns-layer</strong> hinge of the <a href=\"#\" class=\"kc-link\" data-slug=\"big-picture\">The Big Picture — how the Claude stack fits together</a> spine: it converts the abstract \"<a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a>\" decision into a checklist an architect can run in a meeting. The reframe that ends the anxiety: <strong>\"build my own harness\" almost never means writing an agent loop from scratch — it means choosing which altitude, and configuring the primitives you already know.</strong> Bet <em>thin</em> (Anthropic's stance: as models improve, harness complexity should shrink), verify aggressively, and only descend an altitude when the one above provably can't do the job.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Run the decision tree per use case, not per company.</strong> A client typically lands on <em>Claude Code for devs + a bought platform for business users + a few Agent-SDK builds for differentiated workflows</em> — all three, layered. This is the spine of <a href=\"#\" class=\"kc-link\" data-slug=\"bootstrapping-company-ai-stack\">Scenario — bootstrapping a company's AI stack from zero</a>.</li><li><strong>Default to the highest altitude that works</strong>, then justify every descent in writing (control you need, ceiling you hit). It keeps you honest and the system maintainable.</li><li><strong>Make the API loop your baseline benchmark:</strong> if a framework isn't beating ~20 lines of tool-use loop on <em>your</em> task, don't adopt it yet.</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a> — turns \"thickness/single-vs-multi\" into a buy/build choice.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">Claude Agent SDK</a> — the \"configure a harness\" rung.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — the \"use the finished harness\" rung.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a> — the production layers any rung still needs.</li></ul>",
      "connCount": 6
    },
    {
      "slug": "completion-is-externalized",
      "title": "The agent must never declare its own victory — externalize \"done\" into the harness",
      "kind": "concept",
      "layer": "patterns",
      "tags": [
        "verification",
        "e2e-testing",
        "observability",
        "feature-list",
        "scope-control"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/completion-is-externalized.md",
      "html": "<h1>The agent must never declare its own victory — externalize \"done\" into the harness</h1>\n<p><strong>TL;DR</strong> — Agents are <em>systematically overconfident</em>: they judge completion locally (clean syntax, green unit tests) while correctness is global. Asked to grade their own work, they grade generously. So the single most important harness rule is: <strong>completion judgment is not made by the agent itself.</strong> The harness owns \"done\" — through a feature-list state machine, layered/end-to-end verification, hard scope limits, and runtime observability that turns vague failure into specific feedback.</p>\n<h2>Why agents quit too early</h2>\n<ul><li><strong>Confidence-calibration bias</strong> — self-assessed completion exceeds actual accuracy.</li><li><strong>Unit tests enable the trap</strong> — they isolate and mock, masking interface mismatches, state- propagation errors, resource leaks, and environment-dependent failures.</li><li><strong>Self-evaluation bias</strong> — a model grading itself is lenient. (Anthropic: a bare agent's game editor failed functionally; the same model with separate planner/generator/evaluator roles succeeded.)</li></ul>\n<h2>Four harness mechanisms that take \"done\" out of the agent's hands</h2>\n<ol><li><strong>Feature lists as primitives, not docs.</strong> Each feature = <strong>Behavior + Verification + State</strong>, running a four-state machine <code>not_started → active → blocked | passing</code>, where <em>only a passing verification command</em> triggers <code>passing</code>. The scheduler, verifier, handoff reporter, and progress tracker all read it — \"un-bypassable, like a DB constraint vs. an app-layer check.\" Framing: <em>unresolved features are pressure; zero pressure = project complete.</em></li><li><strong>Three-layer verification.</strong> Syntax/static → runtime behavior (tests, startup, critical paths) → system-level (E2E/integration). Each must pass <em>in order</em>; no refactoring until core function verifies (stops boundary-shifting).</li><li><strong>End-to-end &gt; unit.</strong> Only E2E exercises real boundaries — the Electron renderer/preload/service each pass units yet five defects appear only end-to-end. Knowing work faces integration validation <em>changes how the agent codes</em>: from isolated functions to \"how does this connect upstream?\"</li><li><strong>Observability inside the harness.</strong> The <em>harness</em> (not agent self-logging) collects runtime signals — agents don't know what signals they'll need, and ad-hoc logs aren't analyzable. Two layers: <strong>runtime</strong> (logs/traces/health = \"what did the system do\") and <strong>process</strong> (plans, sprint contracts, evaluator rubrics = \"why should this change be accepted\"). A rubric line like <em>\"contrast 2.1:1 vs required 4.5:1\"</em> converts blind retries into actionable fixes — one case cut a task from 45→15 min at identical model capability.</li></ol>\n<p>Plus a scope guard: <strong>WIP = 1.</strong> Agents overreach (expand scope) and under-finish; attention splits C/k across k tasks until nothing completes, and lines-of-code is <em>weakly negatively</em> correlated with completion. Allow one active task, define completion as executable evidence (\"<code>curl</code> returns 201\"), and block new activations while verified-completion &lt; 1.0. <em>\"Do less but finish\" beats \"do more, half-done\"</em> (+37% completion).</p>\n<h2>Mental model / why it matters</h2>\n<p>The unifying move is <strong>separating the worker from the checker</strong> and giving the checker the only key to the \"done\" door. The agent generates; an independent gate — a verification command, an E2E run, an evaluator rubric, a state machine — decides. \"Code review shows what was <em>written</em>; runtime tracing shows what actually <em>ran</em> — you need both.\" This is also why \"convert written rules into running checks\": a constraint in prose is a suggestion, a constraint as a lint rule or CI gate is a law. (This vault's own <code>validate-note.sh</code> hook is the pattern in miniature — it <em>blocks</em> a malformed note rather than trusting the author to comply.)</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Install a done-gate.</strong> The first reliability fix in any engagement: define completion as an executable command per task and forbid self-declared completion — make a passing command the only transition to <code>passing</code>.</li><li><strong>Promote E2E for cross-component changes</strong> and turn recurring review comments into automated checks; it shifts the agent's coding style toward integration-awareness for free.</li><li><strong>Bake observability into the harness, not the prompt.</strong> Standard traces (OpenTelemetry, session → task → verify spans) + evaluator rubrics give the agent specific, self-correcting feedback instead of \"might be a timing issue.\"</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>Part of <a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a></strong> — the Feedback subsystem made rigorous; the harness, not the model, holds the completion signal.</li><li><strong>Builds on <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns and the three failure modes they fix</a></strong> — worker-vs-checker separation is adversarial verification and generate-and-filter applied to the \"done\" decision; planner/generator/evaluator is the orchestrator shape.</li><li><strong>Used with <a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a></strong> — operationalizes the verification organ (three layers, E2E, runtime + process observability feeding the loop).</li><li><strong>Used with <a href=\"#\" class=\"kc-link\" data-slug=\"repository-as-system-of-record\">The repository is the agent's only durable memory — make it the system of record</a></strong> — the feature-list state machine and its <code>passing</code>/<code>blocked</code> states are durable repo files the next session trusts as ground truth.</li></ul>",
      "connCount": 8
    },
    {
      "slug": "containment-vs-discrimination",
      "title": "Containment vs discrimination — two ways to organize the stack, for two different questions",
      "kind": "concept",
      "layer": "patterns",
      "tags": [
        "mental-model",
        "organization",
        "claude-code",
        "learning"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/containment-vs-discrimination.md",
      "html": "<h1>Containment vs discrimination — two ways to organize the stack, for two different questions</h1>\n<blockquote><p><strong>TL;DR.</strong> When a Claude Code family blurs together (the coordination primitives; the \"keep Claude working\" primitives), you can organize it two ways, and they are <strong>not rivals — they answer different questions.</strong> <strong>Containment</strong> answers <em>\"what runs inside what\"</em> (a nesting ladder, good for <strong>understanding</strong>). <strong>Discrimination</strong> answers <em>\"which one do I reach for\"</em> (a sibling table split on axes, good for <strong>deciding</strong>). Confusing the two is what makes \"is this a level or a cousin?\" feel ambiguous.</p></blockquote>\n<h2>The two lenses</h2>\n<ul><li><strong>Containment (nesting / a ladder).</strong> <code>prompt ⊂ context ⊂ harness ⊂ loop</code>, or worktrees nesting <em>under</em> a coordination approach. It states a true <em>is-inside</em> relationship: a loop re-runs harness passes; a routine <em>\"runs a session on a schedule\"</em> (a trigger wrapped <strong>around</strong> a whole session). Use it to <strong>see the stack</strong>.</li><li><strong>Discrimination (a sibling table / a grid).</strong> <code>/loop</code> vs <code>/goal</code> vs a routine; subagents vs agent view vs teams vs workflows. These are <strong>alternatives you pick between</strong>, not a stack you climb. Use it to <strong>make the choice</strong> at the moment you have to.</li></ul>\n<h2>What the official docs actually do</h2>\n<p>The docs lean <strong>discrimination</strong>, and they fence the two families apart:</p>\n<ul><li>The <a href=\"https://code.claude.com/docs/en/agents\" target=\"_blank\" rel=\"noopener\">agents page</a> compares the coordination primitives with three <strong>questions</strong> (\"who coordinates? do workers talk? same files?\") — siblings, not a ladder — and then explicitly says the keep-working features \"solve <strong>a different problem</strong> than splitting work across agents.\"</li><li>The <a href=\"https://code.claude.com/docs/en/scheduled-tasks\" target=\"_blank\" rel=\"noopener\">scheduled-tasks page</a> organizes <code>/loop</code> / routines / desktop tasks with a <strong>\"Compare scheduling options\" table</strong> whose spine row is <em>\"Requires open session.\"</em></li></ul>\n<p>So the docs almost never draw a nesting ladder — they draw <strong>pick-between tables</strong>, because a reader's real question at the point of use is \"which one?\", and those are alternatives.</p>\n<h2>The reconciliation (the move worth keeping)</h2>\n<p>A blurry family usually needs <strong>both lenses at once</strong> — applied to different jobs:</p>\n<ol><li><strong>Place it by containment.</strong> On the mental-model page, loop engineering is the ④ outermost ring (<code>… ⊂ loop</code>) — that placement is containment-correct and matches the docs' \"a routine wraps a session\" language.</li><li><strong>Fill it by discrimination.</strong> Inside that section, render the where×trigger grid, because that is how you actually choose — and it's the exact shape the docs use.</li></ol>\n<p>The tell that you need both: the keep-working family <strong>straddles the session boundary</strong> — <code>/loop</code>·<code>/goal</code>·hooks drive turns <em>inside</em> a session, routines·cron·Actions fire a whole session from <em>outside</em>. A single clean \"level\" can't hold that; a grid with a \"where it runs\" axis can. (See <a href=\"#\" class=\"kc-link\" data-slug=\"automation-control-flow-spectrum\">the where×trigger grid</a>.)</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>When a family confuses a client, ask which question they're asking.</strong> \"How does this fit together?\" → draw the containment ladder. \"Which do I use here?\" → draw the discrimination table. Answering the wrong one is why explanations land flat.</li><li><strong>Don't force a ladder.</strong> If the members straddle a boundary (in-session vs unattended; local vs cloud), a level is the wrong primitive — reach for a grid with that boundary as an axis.</li><li><strong>Mirror the source's shape.</strong> Anthropic's docs teach these as pick-between tables; matching that shape keeps derived material (slides, internal wikis) consistent with what a client will later read upstream.</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"automation-control-flow-spectrum\">Keeping Claude working — the two axes</a></strong> — that grid is the discrimination view this lens names and justifies.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt ⊂ Context ⊂ Harness engineering</a></strong> — the containment ladder; this lens says what question it answers.</li><li><strong>builds-on <a href=\"#\" class=\"kc-link\" data-slug=\"designing-loops-not-prompts\">Design loops, not prompts</a></strong> — placement (containment) vs content (discrimination) resolves where loop engineering sits.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"tool-call-taxonomy\">The tool-call taxonomy</a></strong> — same separate-by-question move on a smaller confusable family.</li></ul>",
      "connCount": 4
    },
    {
      "slug": "designing-loops-not-prompts",
      "title": "Loop engineering — design the system that prompts the agent, not the prompt",
      "kind": "concept",
      "layer": "patterns",
      "tags": [
        "orchestration",
        "loops",
        "automation",
        "harness",
        "subagents"
      ],
      "depth": "seedling",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/designing-loops-not-prompts.md",
      "html": "<h1>Loop engineering — design the system that prompts the agent, not the prompt</h1>\n<blockquote><p><strong>TL;DR.</strong> For ~two years you got value from a coding agent by <em>holding it</em>: write a good prompt, read the reply, write the next prompt — one turn at a time. <strong>Loop engineering</strong> replaces <em>you-as-the-prompter</em> with a small system that finds the work, hands it out, checks it, records what's done, and decides the next thing — then lets that system poke the agent instead of you. Peter Steinberger: <em>\"You shouldn't be prompting coding agents anymore. You should be designing loops that prompt your agents.\"</em> Boris Cherny (head of Claude Code): <em>\"I don't prompt Claude anymore. I have loops running that prompt Claude… My job is to write loops.\"</em> It's <strong>five building blocks + one memory</strong>, and Claude Code ships all five today. It's a preview, not a settled practice: watch token cost and watch quality/slop. <strong>Build the loop, but stay the engineer.</strong></p></blockquote>\n<h2>Where it sits on the spine</h2>\n<p>Loop engineering is <strong>one floor above <a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">harness engineering</a></strong>. The <em>harness</em> is the environment a single agent runs inside — its loop, tools, memory, verification. A <em>loop</em> is that harness <strong>on a timer that spawns helpers and feeds itself</strong>. Prompt engineering → context engineering → harness engineering → <strong>loop engineering</strong>: at each step the leverage point moves outward, from the words, to the window, to the environment, to the self-driving system. The work doesn't get easier — the leverage point moves, and loop <em>design</em> is harder than prompt writing, not easier.</p>\n<h2>The five building blocks (+ memory) — mapped to official Claude Code terms</h2>\n<p>The article's real insight: a year ago a loop meant a pile of bespoke bash you owned forever; now the pieces <strong>ship inside the products</strong>, and the same shape works whether you sit in Claude Code or Codex. Here are the five blocks <strong>mapped to the official Claude Code vocabulary</strong> (the article's looser words in parentheses), so this stays your source of truth:</p>\n<ol><li><strong>Scheduling / automation — the heartbeat.</strong> <em>(article: \"Automations\")</em> What turns one run into a recurring loop. In Claude Code: <strong><code>/loop</code></strong> runs a prompt on a cadence and <strong><code>/goal</code></strong> runs across turns until a condition holds; lifecycle <strong><a href=\"#\" class=\"kc-link\" data-slug=\"cc-hooks\">hooks</a></strong> fire shell commands at events (<code>SessionStart</code>, <code>PreToolUse</code>, <code>Stop</code>, …). ⚠️ <strong>Accuracy note:</strong> <code>/loop</code> and the <code>CronCreate</code>/<code>CronDelete</code> tools are <strong>session-scoped</strong> — they stop when the session ends. To \"keep running after you close the laptop\" you need <strong><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-routines\">Routines</a></strong>, <strong>GitHub Actions</strong>, or <strong>Claude Code on the web</strong>, which run on Anthropic/CI infrastructure. The article blurs these two; they are not the same.</li><li><strong><a href=\"#\" class=\"kc-link\" data-slug=\"worktrees\">Worktrees</a> — parallel without collisions.</strong> A git worktree is a separate working directory on its own branch sharing the repo's history, so two agents can't touch each other's checkout. Official handles: the <strong><code>--worktree</code> / <code>-w</code></strong> CLI flag, and <strong><code>isolation: worktree</code></strong> in a subagent's frontmatter (each helper gets a fresh, self-cleaning checkout). The worktree removes the <em>mechanical</em> collision; <strong>your review bandwidth is still the ceiling</strong> on how many you can run (the \"orchestration tax\").</li><li><strong><a href=\"#\" class=\"kc-link\" data-slug=\"skills\">Skills</a> — written-down project intent.</strong> A folder with a <code>SKILL.md</code> (instructions + metadata, optional scripts/refs/assets). Without skills the loop re-derives your project from zero every cycle and fills gaps with confident guesses (\"intent debt\"); with skills, intent is written once on the outside and <strong>compounds</strong>. A tight, boring <code>description</code> beats a clever one because it's what makes the skill auto-trigger.</li><li><strong><a href=\"#\" class=\"kc-link\" data-slug=\"plugins\">Plugins</a> + <a href=\"#\" class=\"kc-link\" data-slug=\"cc-mcp-servers\">MCP servers</a> — the loop touches your real tools.</strong> ⚠️ <strong>Terminology fix:</strong> the article calls these \"connectors\"; the official Claude Code term is <strong>MCP servers</strong> (Model Context Protocol). \"Connector\" is only claude.ai/Routines UI shorthand. MCP is what lets the loop read your issue tracker, query a DB, hit staging, or post to Slack — the difference between an agent that <em>says</em> \"here's the fix\" and a loop that opens the PR, links the ticket, and pings the channel once CI is green. <strong>Plugins</strong> are the packaging format that ships skills + subagents + MCP config so a teammate installs the whole setup in one go.</li><li><strong><a href=\"#\" class=\"kc-link\" data-slug=\"cc-subagents\">Subagents</a> — keep the maker away from the checker.</strong> The single most useful structural move: the agent that wrote the code is too generous grading its own homework, so a <em>second</em> agent — different instructions, often a different/stronger model — checks it. Defined as markdown files in <code>.claude/agents/</code>. (Distinct from <strong>agent teams</strong>, which are separate coordinating <em>sessions</em> passing work via <code>SendMessage</code> — an experimental flag — not in-session delegation.) Subagents cost more tokens; spend them where a second opinion is worth paying for.</li></ol>\n<p><strong>The sixth thing — memory.</strong> A markdown file, a Linear board — anything outside the single conversation that holds <em>what's done</em> and <em>what's next</em>. It sounds too dumb to matter, but it's the trick every long-running agent depends on: <strong>the model forgets everything between runs, so memory must live on disk, not in context.</strong> The agent forgets; the <a href=\"#\" class=\"kc-link\" data-slug=\"repository-as-system-of-record\">repo doesn't</a>.</p>\n<h2>What one loop looks like</h2>\n<p>An automation runs every morning. Its prompt calls a <strong>triage skill</strong> that reads yesterday's CI failures, open issues, and recent commits and writes findings to a state file (markdown or Linear). For each finding worth doing, the thread opens an isolated <strong>worktree</strong> and sends a <strong>subagent</strong> to draft the fix and a <em>second</em> subagent to review it against the project skills and tests. <strong>MCP servers</strong> let the loop open the PR and update the ticket; anything it can't handle lands in a triage inbox for you. The <strong>state file</strong> is the spine — it remembers what was tried, what passed, what's open — so tomorrow's run resumes instead of restarting. <strong>You designed it once; you prompted none of those steps.</strong> And it's the same loop in Codex or Claude Code because the pieces are the same pieces.</p>\n<h2>Mental model / why it matters</h2>\n<ul><li><strong>The leverage point moved, the job didn't get easier.</strong> Cherny's claim isn't \"less work\" — it's that the highest-value thing you do is now <em>designing the loop</em>, not <em>writing the turn</em>.</li><li><strong>Same five pieces everywhere.</strong> Once you see the shape (heartbeat → isolate → write-down-intent → reach-real-tools → maker/checker → memory) you stop arguing about which tool and design a loop that survives a tool switch.</li><li><strong><code>/goal</code> is the maker/checker split applied to \"done.\"</strong> A fresh small model (Haiku) decides if the stop condition holds, not the agent that did the work — <a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">externalized completion</a> baked into the product.</li></ul>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Sell loop <em>design</em>, not loop <em>autopilot</em>.</strong> For a client, the deliverable is a documented loop (the triage skill, the verifier subagent, the state-file convention) — and the discipline to keep a human at the verification gate. A loop running unattended is also a loop <em>making mistakes</em> unattended.</li><li><strong>Budget tokens explicitly.</strong> Usage swings wildly; subagents and <code>/goal</code> multiply model+tool calls. Price the loop before you let it run on a schedule, especially for token-poor clients.</li><li><strong>Three problems get <em>sharper</em>, not easier, as the loop improves:</strong> verification (\"done\" is a claim, not a proof — you still ship code you confirmed), <strong>comprehension debt</strong> (the faster it ships code you didn't write, the wider the gap between what exists and what you understand), and <strong>cognitive surrender</strong> (it's tempting to stop having an opinion). Designing the loop with judgment is the cure; designing it to avoid thinking is the accelerant — <em>same action, opposite result.</em></li><li><strong>Two people, same loop, opposite outcomes.</strong> One moves faster on work they understand deeply; the other avoids understanding the work at all. The loop can't tell the difference — you can.</li></ul>\n<h2>Provenance &amp; caveats</h2>\n<ul><li>Source is a <strong>2026 opinion thread by Addy Osmani</strong> synthesizing Steinberger and Cherny — framing, not a spec. Datestamp: retrieved 2026-06-09.</li><li><strong>Codex parity is the author's claim, not verified here.</strong> Codex has parallel ideas (a goal mode, skills, agents), but the slash-command syntax, <code>SKILL.md</code>/TOML file formats, and MCP equivalence aren't officially documented as identical. Treat \"all five in both\" as directional.</li><li>Corrections applied vs. the source: <strong>\"connectors\" → MCP servers</strong>; <strong>scheduling split</strong> into session-scoped (<code>/loop</code>, cron tools) vs. unattended (Routines / GitHub Actions / web).</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>builds-on <a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a></strong> — a loop is the harness on a timer that feeds itself; loop engineering is the next floor up.</li><li><strong>builds-on <a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">Never let the agent declare its own victory</a></strong> — an unattended loop is only trustworthy if \"done\" is externally verified, which is what <code>/goal</code>'s separate evaluator does.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a></strong> — worktrees + subagents (the maker/checker split) are the multi-agent primitives the loop runs on a schedule.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"repository-as-system-of-record\">The repository is the agent's only durable memory</a></strong> — the loop's sixth piece, memory, must live on disk because the model forgets between runs.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-routines\">Claude Code Routines — unattended cloud automation</a></strong> — the official way a loop keeps running unattended after you close the laptop.</li></ul>",
      "connCount": 8
    },
    {
      "slug": "dynamic-workflow-patterns",
      "title": "Six dynamic-workflow patterns and the three failure modes they fix",
      "kind": "concept",
      "layer": "patterns",
      "tags": [
        "claude-code",
        "workflows",
        "orchestration",
        "patterns",
        "subagents",
        "verification"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/dynamic-workflow-patterns.md",
      "html": "<h1>Six dynamic-workflow patterns and the three failure modes they fix</h1>\n<blockquote><p>Companion to <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the Ultraplan loop</a>: that note explains the <code>agent()</code> / <code>parallel()</code> / <code>pipeline()</code> <em>primitives</em>; this one names the reusable <em>shapes</em> you build out of them, and the failure modes that justify the cost.</p></blockquote>\n<h2>TL;DR</h2>\n<p>A dynamic workflow is Claude writing a <strong>JavaScript harness that spawns isolated subagents</strong>. You reach for one to beat three failure modes a single long context falls into, and you structure it with one of <strong>six recurring patterns</strong>. The patterns are the vocabulary; the failure modes are the <em>why</em>.</p>\n<h2>The three failure modes (the \"why fan out at all\")</h2>\n<p>A single agent grinding through one growing context drifts in three predictable ways:</p>\n<ol><li><strong>Agentic laziness</strong> — it declares done before the task actually is; coverage silently caps.</li><li><strong>Self-preferential bias</strong> — asked to check its own work, it rates its own output highly.</li><li><strong>Goal drift</strong> — across many turns it loses fidelity to the original objective.</li></ol>\n<p>Forking work into focused, isolated subagents attacks all three: each child has a <em>narrow</em> objective (less room to be lazy), a <strong>separate</strong> verifier can't be biased toward an output it didn't write, and a short single-purpose context can't drift far.</p>\n<h2>The six patterns</h2>\n<ol><li><strong>Classify-and-act</strong> — first agent <em>routes</em> the work by type, downstream agents handle each class. (The orchestration version of prompt routing.)</li><li><strong>Fan-out-and-synthesize</strong> — split a task across N agents working independently, then merge their results into one answer. The bread-and-butter <code>parallel()</code> shape.</li><li><strong>Adversarial verification</strong> — a <em>separate</em> agent (or panel) is tasked to <strong>refute</strong> each finding. Directly kills self-preferential bias — the verifier has no stake in the claim.</li><li><strong>Generate-and-filter</strong> — produce several candidate ideas/solutions, then filter/test down to the survivors. Breadth first, judgement second.</li><li><strong>Tournament</strong> — pairwise-compare candidates bracket-style to surface the best. Use when \"best of many\" needs better than a single global score.</li><li><strong>Loop-until-done</strong> — keep spawning workers until an explicit completion criterion is met (e.g. K consecutive rounds find nothing new). The antidote to laziness — <em>you</em> define \"done,\" not the model's mood.</li></ol>\n<h2>Practical levers the article calls out</h2>\n<ul><li><strong>Explicit token budgets</strong> — scale fan-out depth/agent count to a stated budget instead of letting it run unbounded.</li><li><strong>A hard-completion <code>/goal</code></strong> — bind the loop to a real done-condition so it can't quit early.</li><li><strong><code>/loop</code></strong> for recurring workflows; save a proven workflow as a reusable <strong>Skill</strong>.</li><li><strong>Quarantine pattern</strong> — run untrusted/external content through an isolated agent so a prompt injection can't reach the main context.</li></ul>\n<h2>Mental model / why it matters</h2>\n<p>Map each pattern to a failure mode and the catalog stops being a list to memorise: <em>loop-until-done</em> ⟂ laziness, <em>adversarial verification</em> ⟂ self-preference, <em>classify-and-act</em> / explicit budgets ⟂ drift. The deeper point from the sibling note still governs: <strong>fan-out helps <em>independent</em> work and fights <em>interdependent</em> work</strong> — if the value is in the relationships between items (a coherent graph), keep it in one head; if it's in the items, fan out.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Audit / review engagements:</strong> fan-out-and-synthesize finders → <strong>adversarial verification</strong> before anything is reported. Never let the finder grade its own findings to a client.</li><li><strong>Vendor / design selection:</strong> generate-and-filter, or a <strong>tournament</strong> when the field is wide and a single rubric score won't separate the top two.</li><li><strong>Exhaustive sweeps (\"find <em>all</em> the X\"):</strong> loop-until-done with a \"K dry rounds\" stop, and <strong>log what was dropped</strong> — silent caps read as \"we covered everything\" when you didn't.</li><li><strong>Handling untrusted input:</strong> default to the <strong>quarantine</strong> pattern; it's a cheap guardrail against injection in any client-facing automation.</li></ul>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the Ultraplan loop</a> — the named shapes on top of the raw primitives.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">One shared context vs. many isolated contexts</a> — every pattern forks the main window into isolated subagents.</li><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a> — verification and loop organs, externalised across agents.</li><li><strong>used-in</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a> — how the agent layer gets composed in practice.</li></ul>",
      "connCount": 7
    },
    {
      "slug": "dynamic-workflows-and-ultraplan",
      "title": "Dynamic Workflows & the \"Ultraplan\" loop",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "claude-code",
        "workflows",
        "subagents",
        "orchestration",
        "planning",
        "meta"
      ],
      "depth": "evergreen",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/dynamic-workflows-and-ultraplan.md",
      "html": "<h1>Dynamic Workflows &amp; the \"Ultraplan\" loop</h1>\n<blockquote><p><strong>Why this note exists.</strong> It captures the exact technique used to <em>build</em> this knowledge center, so the method itself becomes reusable reference material — dog-fooding the dot-connecting model.</p></blockquote>\n<blockquote><p>⚠️ <strong>Naming note (2026-06-07).</strong> \"Ultraplan\" here is <em>my own term</em> for the research→synthesis→plan-gate→execute meta-loop — coined before Claude Code shipped features with overlapping names. In the official docs these are now two distinct things: <strong>dynamic workflows</strong> are triggered by the keyword <strong><code>ultracode</code></strong> (not \"ultraplan\"), and <strong>ultraplan</strong> is a <em>separate cloud</em> feature that hands a planning task to a Claude Code-on-the-web session in plan mode. The concept graph uses the docs' meaning; this note keeps my original loop terminology.</p></blockquote>\n<h2>TL;DR</h2>\n<p>A <strong>dynamic workflow</strong> is a deterministic JavaScript script that fans work out across many subagents and collects their results — you use it when a task splits into independent sub-questions you want answered <em>concurrently</em> and <em>verifiably</em>. <strong>\"Ultraplan\"</strong> is the larger loop that wraps a workflow in disciplined planning: <strong>fan-out research → transparent synthesis → plan-mode gate → execute</strong>. The workflow gives you breadth and parallelism; the plan gate makes sure you think before you touch.</p>\n<h2>The mental model</h2>\n<p>A single agent in one context is a straight line: it reasons, acts, verifies, repeats — all sequentially, all competing for one finite context window. A workflow turns that line into a <strong>tree</strong>: the orchestrator spawns N child agents, each with its <em>own</em> fresh context, and merges what comes back. So a workflow is best understood as a <strong>scripted harness over many sub-harnesses</strong> — which is why it <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">builds-on</a> the gather→act→verify loop: every child runs that loop independently.</p>\n<h2><code>parallel()</code> vs <code>pipeline()</code> — the core decision</h2>\n<ul><li><strong><code>parallel()</code> is a barrier:</strong> launch all thunks, <em>wait for every one</em>, then continue. Use it when you need <strong>all</strong> results together before the next step (e.g. synthesize one design from four independent research streams — you can't synthesize until all four land).</li><li><strong><code>pipeline()</code> has no barrier:</strong> each item flows through all stages independently; item A can be in stage 3 while item B is still in stage 1. Use it when each item's stages are independent of the other items. Wall-clock = slowest single chain, not sum-of-slowest-per-stage.</li><li><strong>Default to <code>pipeline()</code>.</strong> Reach for <code>parallel()</code> only when a stage genuinely needs cross-item context (dedup across all findings, early-exit on zero, \"compare against the others\").</li></ul>\n<h2>Structured returns beat prose</h2>\n<p>Give each agent a <strong>JSON schema</strong> so it returns validated data, not text you have to parse. The orchestrator then reasons over clean objects. Validation happens at the tool layer, so the agent retries until the shape is right.</p>\n<h2>When an extra agent <em>won't</em> change the answer (just as important)</h2>\n<p>Fan-out shines for <strong>independent</strong> work. It <em>fights you</em> for <strong>interdependent</strong> work. Example from this very build: wiring typed connections across six notes is a <em>graph</em> problem — the edges must be globally coherent. Blind parallel agents would each propose links in isolation, then you'd burn effort reconciling contradictions. One reasoner holding the whole picture wins. <strong>Knowing when not to fan out is part of the skill.</strong> A good heuristic: <em>is the value in the items, or in the relationships between them?</em> Items → fan out. Relationships → keep it in one head.</p>\n<h2>The Ultraplan loop (what produced this repo)</h2>\n<ol><li><strong>Recon</strong> — read the actual material first; ground the plan in reality, not assumptions.</li><li><strong>Fan-out research</strong> — a <code>parallel()</code> workflow of focused researchers (here: self-organizing PKM, ingestion mechanics, Claude Code skills/hooks, exam scope), each returning a structured schema.</li><li><strong>Transparent synthesis</strong> — <em>you</em> (the orchestrator) merge the findings in the open, so the human sees the reasoning and the research actually changes the design.</li><li><strong>Plan-mode gate</strong> — write the plan, get explicit approval before any edit. Think before touch.</li><li><strong>Execute</strong> — and narrate the non-obvious technique choices so they become learnable.</li></ol>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Audits &amp; research</strong> (\"review this codebase\", \"compare these 5 vendors\"): <code>parallel()</code> finders → adversarial verify → synthesis. Independent angles, merged once.</li><li><strong>Migrations &amp; sweeps</strong> (\"apply this change across 40 files\"): <code>pipeline()</code>, one item per chain, <code>isolation: 'worktree'</code> if agents mutate files concurrently.</li><li><strong>Client-facing decisions:</strong> always wrap fan-out in the plan gate — clients approve the plan, not the diff. Show the synthesis so the recommendation is legible, not a black box.</li><li><strong>Cost discipline:</strong> a workflow can spawn dozens of agents. Scope it to what the task needs, and skip agents that won't change the answer.</li></ul>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>part-of</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — one of the orchestration primitives.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a> — the deterministic cousin of agent teams / parallel subagents.</li><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a> — a workflow is a scripted harness over many sub-harnesses.</li></ul>",
      "connCount": 8
    },
    {
      "slug": "execution-context-isolation",
      "title": "One shared context vs. many isolated contexts — what actually forks in Claude Code",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "claude-code",
        "subagents",
        "context",
        "modes",
        "permissions",
        "orchestration"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/execution-context-isolation.md",
      "html": "<h1>One shared context vs. many isolated contexts</h1>\n<blockquote><p><strong>Why this note exists.</strong> The single most clarifying question when reasoning about Claude Code behaviour is: <em>does this run in the one shared context window, or does it fork a fresh one?</em> Almost every \"wait, which is which?\" confusion dissolves once you sort each feature into one of those two buckets.</p></blockquote>\n<h2>TL;DR</h2>\n<p>The <strong>default Claude Code harness plans AND executes in ONE shared context window</strong> — your messages, its reasoning, every tool call and tool result pile into a single growing conversation. The escape hatch is <strong>forking a fresh context</strong>: anything that is an <em>agent</em> (subagent) runs in its <strong>own isolated window</strong> and returns only a summary. The rule that resolves all the confusion: <strong>modes</strong> stay in the one window; <strong>agents</strong> fork new ones.</p>\n<h2>The two buckets</h2>\n<table><thead><tr><th>Bucket</th><th>What runs there</th><th>Context</th></tr></thead><tbody><tr><td><strong>Modes</strong></td><td>The main loop, under different rules of engagement</td><td><strong>Same</strong> shared window</td></tr><tr><td><strong>Agents</strong></td><td>Subagents the main loop spawns</td><td><strong>Own</strong> isolated window; only a result returns</td></tr></tbody></table>\n<h3>Modes — same window, different rules</h3>\n<p>A mode changes <em>how</em> the one main loop behaves, not <em>where</em> it runs. On CLI <strong>2.1.165</strong> the six permission modes (verified via <code>claude --permission-mode</code>) are:</p>\n<ul><li><strong><code>default</code></strong> — asks before edits / risky commands.</li><li><strong><code>plan</code></strong> — investigates read-only, drafts a plan, waits for approval, <em>then</em> executes — <strong>all in the same window</strong>.</li><li><strong><code>acceptEdits</code></strong> — applies edits without prompting each one.</li><li><strong><code>bypassPermissions</code></strong> — skips all permission checks (the \"yolo\" mode).</li><li><strong><code>auto</code></strong> — a classifier decides per-action what's safe to run vs. ask (<code>claude auto-mode</code> inspects it).</li><li><strong><code>dontAsk</code></strong> — proceeds without asking but stays inside configured allow rules.</li></ul>\n<h3>Agents — fork a fresh window</h3>\n<p>Anything described as an <em>agent</em> gets its <strong>own</strong> context, does focused work, and hands back only its final result (not its intermediate reasoning). Built-in subagent types include <strong>general-purpose</strong>, <strong>Explore</strong> (read-only fan-out search — returns conclusions, not file dumps), the <strong>Plan</strong> agent (architect that returns a plan), <strong>claude</strong>, and <strong>claude-code-guide</strong>.</p>\n<h2>The trap: \"Plan\" means two different things</h2>\n<ul><li><strong>Plan <em>mode</strong></em> = the approve-then-execute flow → <strong>same</strong> window.</li><li><strong>Plan <em>agent</strong></em> = a subagent type that designs a plan → <strong>separate</strong> window.</li></ul>\n<p>Same word, unrelated mechanisms. This is the #1 source of \"but you just said…\" confusion.</p>\n<h2>Where skills sit (the hybrid)</h2>\n<p>A <strong>skill</strong> runs <em>inline in the main context</em> — its instructions load into the one shared window. But a skill can itself <strong>fan out</strong> to isolated subagents. <code>deep-research</code> is the clean example: the <em>orchestration</em> (deciding scope, assembling the report) lives in the main window, while the <em>heavy lifting</em> (many searches, fetches, adversarial verification) happens in <strong>separate</strong> subagent contexts, and only distilled results flow back.</p>\n<p>So the full hierarchy of \"does it fork?\":</p>\n<ul><li><strong>Mode</strong> → no (same window).</li><li><strong>Skill</strong> → no by default (inline), but <em>may</em> spawn agents that do.</li><li><strong>Agent / subagent / workflow</strong> → yes (fresh isolated window per agent).</li></ul>\n<h2>Mental model / why it matters</h2>\n<p>Context is the scarce resource (see <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">harness vs context</a>) — one window has finite room and degrades as it fills (laziness, drift, self-preference creep in on long single contexts). <strong>Forking context is the lever</strong> against all three: push a noisy side-quest into an Explore agent and the main window keeps the <em>finding</em>, not the 40 files it read. Knowing which bucket a feature is in tells you (a) whether its work pollutes your main context, and (b) whether you'll get reasoning back or just a summary.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Keep the main window clean.</strong> Offload broad search/log-trawling to <strong>Explore</strong> or a subagent; you keep the conclusion, not the dump.</li><li><strong>Don't expect a mode to isolate.</strong> Plan <em>mode</em> won't stop a long investigation from bloating your context — for that you need an <em>agent</em>.</li><li><strong>Pick the permission mode to the trust level</strong>, not the task: <code>plan</code> for client-facing think-before-touch, <code>acceptEdits</code>/<code>auto</code> for trusted sandboxes, <code>bypassPermissions</code> only in isolated no-internet sandboxes.</li><li><strong>Explain the \"summary returns\" contract to clients:</strong> a subagent's reasoning is <em>gone</em> once it reports back — if you need the audit trail, capture it inside the agent or use a workflow with structured returns.</li></ul>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a> — context is the scarce resource; forking it is the move.</li><li><strong>part-of</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — the \"how work is coordinated\" axis, made concrete.</li><li><strong>enables</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the Ultraplan loop</a> — a workflow forks the main window N-fold.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a> — subagents / teams / agent view all run in separate contexts.</li></ul>",
      "connCount": 9
    },
    {
      "slug": "harness-engineering-discipline",
      "title": "Harness engineering — reliability lives outside the model weights",
      "kind": "concept",
      "layer": "foundations",
      "tags": [
        "harness",
        "reliability",
        "agent-architecture",
        "evals"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/harness-engineering-discipline.md",
      "html": "<h1>Harness engineering — reliability lives outside the model weights</h1>\n<p><strong>TL;DR</strong> — A capable model is necessary but not sufficient. Top coding agents post 50–60% on benchmarks yet do <em>worse</em> on real tasks, because real tasks come with vague specs, missing context, no verification, and no cross-session memory. The fix is almost never a bigger model — it's a better <strong>harness</strong>: <em>everything in the engineering infrastructure outside the model weights</em>. Harness engineering is the discipline of building that infrastructure so a fixed model actually realizes its capability.</p>\n<h2>The capability ≠ reliability gap</h2>\n<p>Anthropic's own controlled experiment is the cleanest proof: the <em>same</em> Opus 4.5 model failed to build a game editor on its own, and succeeded once wrapped in a planner→generator→evaluator three-agent architecture. The weights never changed; the environment did. So a failure is rarely a \"the model can't\" problem — it's a <strong>harness-induced failure</strong>, and harness defects are fixable by you.</p>\n<h3>The five failure modes (attribute every failure to one)</h3>\n<ol><li><strong>Vague requirements</strong> — implicit conventions were never written down, so the agent guesses.</li><li><strong>Incomplete environment</strong> — context burned fixing deps instead of doing the task.</li><li><strong>No verification</strong> — the agent declares done on syntax/unit-test signal (\"context anxiety\").</li><li><strong>Missing task definition</strong> — no explicit, executable success criteria.</li><li><strong>Cross-session state loss</strong> — every session restarts from zero.</li></ol>\n<p>The diagnostic mindset: pin each failure to one of these layers, fix <em>that</em> layer, and never fail that way again.</p>\n<h2>What a harness actually is — the five subsystems</h2>\n<p>A harness is not a prompt file. It's five subsystems, each closing one failure mode:</p>\n<table><thead><tr><th>Subsystem</th><th>Concretely</th><th>Closes</th></tr></thead><tbody><tr><td><strong>Instructions</strong></td><td><code>CLAUDE.md</code>/<code>AGENTS.md</code> (~100 lines): overview, run commands, hard constraints, doc links</td><td>vague requirements</td></tr><tr><td><strong>Tools</strong></td><td>enough shell/package access, least-privilege not security-first</td><td>overreach on setup</td></tr><tr><td><strong>Environment</strong></td><td>self-describing runtime — lockfiles, <code>.nvmrc</code>, reproducible containers</td><td>incomplete setup</td></tr><tr><td><strong>State</strong></td><td>progress files read at session start, updated at session end</td><td>cross-session loss</td></tr><tr><td><strong>Feedback</strong></td><td>the verification commands (test/lint/typecheck/build) listed explicitly</td><td>premature \"done\"</td></tr></tbody></table>\n<p><strong>\"The repo IS the spec.\"</strong> Everything the agent needs must live in the repository as structured files — see <a href=\"#\" class=\"kc-link\" data-slug=\"repository-as-system-of-record\">The repository is the agent's only durable memory — make it the system of record</a>.</p>\n<h2>Mental model / why it matters</h2>\n<p>The headline number isn't a model benchmark — it's the <strong>controlled-exclusion curve</strong>. A real team drove the <em>same</em> model from <strong>20% → 60% → 80% → 80–100%</strong> task success purely by adding harness subsystems one at a time. That gives you both a method and a budgeting tool: remove a subsystem, measure the drop, and you've quantified its ROI. Empirically the <strong>Feedback</strong> subsystem (just listing the verify commands) is the highest ROI for the lowest spend. This reframes \"make the agent better\" from <em>prompt-whispering</em> into <em>infrastructure engineering</em> — the part you can actually version, test, and improve.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Lead with the curve, not the model.</strong> When a client blames the model, run a controlled-exclusion test: add <code>CLAUDE.md</code>, then verify-commands, then a <code>PROGRESS.md</code>, and chart the success rate. It reframes the spend from \"wait for GPT-N+1\" to \"build the harness you already can.\"</li><li><strong>Audit by subsystem.</strong> Score a codebase on all five (Instructions/Tools/Environment/State/Feedback); the empty column is the next sprint. Cheapest win is usually Feedback.</li><li><strong>Treat the harness as the deliverable.</strong> In an engagement, the durable asset you hand over isn't a clever prompt — it's the five-subsystem scaffold that makes <em>their</em> future agents reliable.</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>Builds on <a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a></strong> — that note dissects the harness into six organs (loop, context, tools, memory, verification, delegation); this one gives the <em>why</em> and a five-subsystem audit for deciding which organ to fund first.</li><li><strong>Contrasts with <a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a></strong> — the whole thesis is that reliability is a <em>harness</em>-level property, not a prompt-level one; the 20%→100% swing happens with the prompt fixed.</li><li><strong>Used with <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a></strong> — same nested model; this adds the empirical controlled-exclusion test for locating which level a bug lives on.</li><li><strong>Used with <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own harness — the four-altitude decision</a></strong> — the buy→use→configure→own ladder is really a choice of how much harness you assemble yourself vs. inherit.</li><li>Anchors a cluster: <a href=\"#\" class=\"kc-link\" data-slug=\"repository-as-system-of-record\">The repository is the agent's only durable memory — make it the system of record</a> (State+Instructions made concrete), <a href=\"#\" class=\"kc-link\" data-slug=\"progressive-disclosure-instructions\">One giant instruction file fails — structure CLAUDE.md by progressive disclosure</a> (the Instructions subsystem), and <a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">The agent must never declare its own victory — externalize \"done\" into the harness</a> (the Feedback subsystem).</li></ul>",
      "connCount": 8
    },
    {
      "slug": "harness-vs-context-engineering",
      "title": "Harness vs Context vs Prompt Engineering",
      "kind": "concept",
      "layer": "foundations",
      "tags": [
        "prompting",
        "context-engineering",
        "harness",
        "agents",
        "cross-cutting"
      ],
      "depth": "evergreen",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/harness-vs-context-engineering.md",
      "html": "<h1>Harness vs Context vs Prompt Engineering</h1>\n<blockquote><p><strong>Canonical home.</strong> This content previously lived as a visual page in the Tide repo (<code>/tide/harness-vs-context-engineering.html</code>). It now lives here in the AI Knowledge Center, and is rendered interactively on the <span class=\"kc-ref\">mental-model page</span> of the Claude Code Architecture site. See the Tide-removal checklist in <code>LEARNINGS.md</code>.</p></blockquote>\n<h2>TL;DR</h2>\n<p>Three nested levels of control over an LLM, each a bigger unit of work than the last. <strong>Prompt engineering</strong> shapes one input. <strong>Context engineering</strong> manages what stays in the window across steps. <strong>Harness engineering</strong> wraps the whole gather → act → verify loop — and contains the other two inside it.</p>\n<pre><code>prompt  ⊂  context  ⊂  harness\n(message)  (memory)   (machine)</code></pre>\n<h2>The three levels</h2>\n<h3>1. Prompt engineering — <em>the message</em> (unit: one input)</h3>\n<p>Compose a single best input from five ingredients, send it, then refine the weakest ingredient and repeat.</p>\n<ul><li><strong>Role · Context · Instructions · Examples · Format</strong></li><li>Loop: <code>compose → send → generate → refine the weakest ingredient</code>.</li></ul>\n<h3>2. Context engineering — <em>the memory</em> (unit: what stays in the window, step by step)</h3>\n<p>You have a <strong>finite context budget</strong>. A <em>curator</em> selects, compresses, and drops material so only what matters reaches the model each step.</p>\n<ul><li>Inputs to curate: user query, system prompt, retrieved docs, tool outputs, memory, prior turns.</li><li>Flow: <code>gather → curate (select/compress/drop) → feed → inference → step output</code>.</li><li>After each step, <strong>update the working set</strong>: new outputs become context for the next step.</li></ul>\n<h3>3. Harness engineering — <em>the machine</em> (unit: the whole loop)</h3>\n<p>The outer machine with three zones; prompt and context engineering <strong>live inside its Gather/Act steps</strong>.</p>\n<ul><li><strong>① GATHER</strong> — context-engineering zone: curator + finite context window. Sources: user request, memory (CLAUDE.md, sessions), prior tool outputs, retrieved docs.</li><li><strong>② ACT</strong> — prompt-engineering zone: assemble one prompt → inference → branch to <strong>tools</strong> (exec, fetch, MCP) or <strong>subagents</strong> (specialists).</li><li><strong>③ VERIFY</strong> — a verifier (tests, LLM-as-judge) checks the result. Pass → final response. Fail → <strong>retry: re-run gather → act → verify with updated context.</strong></li></ul>\n<h2>Why it matters — it tells you where a bug lives</h2>\n<ul><li>Bad single answer → fix the <strong>prompt</strong>.</li><li>Model forgetting / drowning in irrelevant tokens → fix <strong>context</strong> (the curator).</li><li>Agent does the right thing once but can't recover, loop, or self-check → fix the <strong>harness</strong> (verify + retry).</li></ul>\n<p>Maps directly onto Claude Code: CLAUDE.md + session = memory; the curated context window = gather; tool/subagent calls = act; tests/judges = verify. This is the frame the whole <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code primitive set</a> hangs off — every primitive is a way to tune one zone of this loop.</p>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a> — the original diagram this narrative expands.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a> — the full production system view of this loop.</li><li>see also → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — every primitive maps onto this loop.</li></ul>",
      "connCount": 9
    },
    {
      "slug": "loop-is-cron-plus-a-decision-maker",
      "title": "A loop is cron plus a decision-maker in the body",
      "kind": "concept",
      "layer": "patterns",
      "tags": [
        "loops",
        "automation",
        "orchestration",
        "cron",
        "goal",
        "skills",
        "economics"
      ],
      "depth": "seedling",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/loop-is-cron-plus-a-decision-maker.md",
      "html": "<h1>A loop is cron plus a decision-maker in the body</h1>\n<blockquote><p><strong>TL;DR.</strong> \"WTF is a loop?\" was the most-repeated phrase in AI coding in early June 2026, and almost nobody saying it could define it. The clean answer: <strong>a loop is cron plus a decision-maker in the body.</strong> Cron runs a <em>fixed script</em> on a timer; a loop runs a <em>model</em> that looks at the current state, decides the next action, does it, checks whether it worked, and decides whether to keep going — <strong>the decision is the agent's, not a hardcoded branch.</strong> That one added part is the whole game. The term hides a five-stage lineage (ReAct → AutoGPT → ralph → <code>/goal</code> → orchestration), the on-ramp in Claude Code is now a single slash command, and the punchline of 2026 is that <strong>the loop, not the model, is the expensive part.</strong></p></blockquote>\n<h2>The lineage — five things \"loop\" can mean</h2>\n<p>The replies were a brawl because \"loop\" hides at least five different things. Oldest to newest, so you stop talking past people:</p>\n<ol><li><strong>ReAct (2022)</strong> — the academic while-loop: one model reasons, calls a tool, reads the result, repeats, with a human watching. One model, one loop.</li><li><strong>AutoGPT (2023)</strong> — gave the loop a <em>goal</em> and let it prompt itself. Famous for spinning forever doing nothing; that failure seeded years of \"agents are a toy.\"</li><li><strong>The ralph loop (July 2025, Geoffrey Huntley)</strong> — a bash one-liner piping the same prompt file into the agent over and over. Its innovation was <strong>discipline</strong>: every iteration <em>resets the context to a fixed set of anchor files</em> instead of letting the conversation grow. (Huntley reportedly built a small programming language this way for ~$297.)</li><li><strong><code>/goal</code> (spring 2026)</strong> — productized the ralph loop: run the loop until a small validator model confirms the task is done. This is Claude Code's <a href=\"#\" class=\"kc-link\" data-slug=\"goal-mode\"><code>/goal</code></a> — condition-driven, self-checking. ⚠️ The article claims Codex shipped the same command; <strong>Codex parity is the author's claim, unverified here.</strong></li><li><strong>Orchestration loops (2026)</strong> — what Steinberger and Cherny actually mean, and genuinely new. Four things changed: the <strong>loop became the unit of work</strong> (not the task); loops <strong>supervise other loops</strong>, concurrently and on a schedule; <strong>scheduling replaced the human kickoff</strong>, so the loop runs on infrastructure time instead of your attention; and <strong>durability became explicit</strong> — git-backed state and crash recovery, because these runs must survive a restart. Ralph assumed your terminal stayed open; the 2026 version assumes it does not.</li></ol>\n<p>So the skeptic line <em>\"cronjobs have funny re-branding rn\"</em> is <strong>half right</strong>: the scheduling layer <em>is</em> cron (Claude Code's <code>/loop</code> uses cron under the hood). What cron never had is the part in the middle — the decision-maker. <strong>Single-agent ralph is old hat; multi-agent supervision is the new layer.</strong></p>\n<h2>\"I don't prompt Claude anymore. My job is to write loops.\"</h2>\n<p>Boris Cherny (creator of Claude Code) on stage at WorkOS Acquired Unplugged, June 2 2026: <em>\"I have loops that are running. They're the ones that are prompting Claude and figuring out what to do. My job is to write loops.\"</em> Steinberger's version, June 7 2026: <em>\"You shouldn't be prompting coding agents anymore. You should be designing loops that prompt your agents.\"</em> You stop being the thing inside the loop typing prompts; you become the <strong>author of the loop</strong>, and the model becomes a subroutine. The on-ramp is one line — <code>/loop babysit all my PRs. Auto-fix build issues, and when comments come in, use a worktree agent to fix them.</code> — and Boris's five tips for running a model autonomously for hours: <strong>auto mode</strong> for permissions, <strong>dynamic workflows</strong> to orchestrate many agents, <strong><code>/goal</code> or <code>/loop</code></strong> to keep it going, <strong>the cloud</strong> so you can close your laptop, and <strong>a way to self-verify end to end.</strong> Tip five is the one practitioners obsess over: <em>a loop is only as trustworthy as its ability to check its own work.</em></p>\n<h2>The plot twist — the loop is now the expensive part</h2>\n<p>Once the model writes the code for almost nothing, the cost moves to the <em>loop running it.</em> One engineer's deflation: <em>\"Every AI agent I shipped this year is a for-loop, an LLM call, and a try/catch around the JSON parsing. The only thing agentic about it is the Anthropic bill at the end of the month.\"</em> The receipts are real — Uber reportedly capped engineers at <strong>$1,500/person/month per tool</strong> for Claude Code and Cursor after burning its annual AI budget in four months. The failure mode everyone in production fears is <strong>the loop that doesn't stop.</strong> So every serious 2026 write-up converges on the <strong>same three hard stops</strong>:</p>\n<ul><li>a <strong>maximum iteration count</strong>,</li><li><strong>no-progress detection</strong> (kill it if nothing changed), and</li><li>a <strong>token or dollar budget ceiling.</strong></li></ul>\n<p>Without those guardrails you get infinite loops and billing surprises orders of magnitude over budget. <strong>The expensive resource shifted from tokens to loop management.</strong></p>\n<h2>It's not loops. It's skills.</h2>\n<p>The durable asset isn't the loop — <strong>the loop is plumbing; the asset is the skill it calls.</strong> A loop with no reusable <a href=\"#\" class=\"kc-link\" data-slug=\"skills\">skills</a> inside it is just a <code>while-true</code> around a stranger; a loop that calls a library of sharp, tested, named skills is a system that <strong>compounds.</strong> Steinberger's pairing rule: <em>if you do something more than once, turn it into a skill; if you do something hard, turn it into a skill afterward so next time it's free.</em> This is the bridge between the two halves of the stack — the loop is <em>how</em> it keeps going, the skill is <em>what</em> it knows how to do well.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Lead with the definition.</strong> When a client says \"we want agents,\" the useful reframe is: you want a loop — cron plus a decision-maker — wrapped in guardrails. That single sentence cuts through most of the hype.</li><li><strong>Price the loop, not the model.</strong> Budget the <em>loop management</em> explicitly: set the iteration cap, no-progress detector, and dollar ceiling <strong>before</strong> you let it run unattended. This is now the line item that surprises people.</li><li><strong>Sell the skill library.</strong> The compounding value you deliver is the named, tested skills the loop calls — not the loop itself. A loop without skills doesn't accrue.</li><li><strong>Reality check the hype.</strong> Gartner pegged ~17% of organizations as actually deploying agents (peak of inflated expectations). The timeline is loud; the receipts are thinner. Build the loop, stay the engineer.</li></ul>\n<h2>Provenance &amp; caveats</h2>\n<ul><li>Source: <strong>Matt Van Horn, \"WTF Is a Loop? Peter Steinberger vs. Boris Cherny\"</strong> (X, Jun 8 2026, 2.3M views), a synthesis of ~80 voices across X / Reddit / HN. Framing and opinion, not a spec. Retrieved 2026-06-09.</li><li>✅ Verified vs official docs: <code>/goal</code> is a real Claude Code command (a small fast model checks a condition each turn); <code>/loop</code> is a bundled skill that schedules via cron tools. See <a href=\"#\" class=\"kc-link\" data-slug=\"goal-mode\">goal mode</a> and <a href=\"#\" class=\"kc-link\" data-slug=\"scheduled-tasks\">scheduled tasks</a>.</li><li>❓ Author's claims, <strong>not verified</strong>: Codex shipped an equivalent <code>/goal</code>; Steve Yegge's \"Gas Town\" (20–30 Claude Code instances under a \"Mayor\" agent). Treated as illustrative, like the vault's handling of Osmani's Codex-parity claim.</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>builds-on <a href=\"#\" class=\"kc-link\" data-slug=\"designing-loops-not-prompts\">Loop engineering — design the system that prompts the agent</a></strong> — the same thesis from a second source; this note adds the lineage, the one-line definition, and the economics.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"automation-control-flow-spectrum\">Keeping Claude working — the two axes</a></strong> — the lineage's endpoints (<code>/loop</code>, <code>/goal</code>, routines) are exactly the primitives that note tells apart.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">Never let the agent declare its own victory</a></strong> — \"self-verify end to end\" is externalized completion, restated as what makes a loop trustworthy.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-routines\">Claude Code Routines — unattended cloud automation</a></strong> — stage five (durable, scheduled, laptop closed) is what a cloud routine provides.</li></ul>",
      "connCount": 5
    },
    {
      "slug": "model-context-protocol",
      "title": "Model Context Protocol (MCP) — From Zero to Plumbing",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "mcp",
        "tool-use",
        "agents",
        "integrations",
        "json-rpc"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/model-context-protocol.md",
      "html": "<h1>Model Context Protocol (MCP) — From Zero to Plumbing</h1>\n<p><img src=\"notes/model-context-protocol.jpeg\" alt=\"Model Context Protocol complete architecture\" loading=\"lazy\"></p>\n<blockquote><p>Source: \"Model Context Protocol — From Zero to Plumbing, The Complete Architecture\" by Brij Kishore Pandey (@brijpandeyji).</p></blockquote>\n<h2>TL;DR</h2>\n<p>MCP is a single open protocol that turns the <strong>N×M</strong> integration problem (every model wired to every tool) into <strong>N+M</strong> (each model and each tool speaks one protocol). \"One protocol. Any model. Any tool.\" It isn't magic — it's plumbing: simple primitives over clean transport on JSON-RPC 2.0.</p>\n<h2>The problem it solves</h2>\n<p>Before MCP, connecting <em>N</em> models to <em>M</em> tools meant <em>N×M</em> bespoke integrations. With MCP it's <em>N+M</em>: each side implements the protocol once.</p>\n<h2>Timeline</h2>\n<ul><li><strong>Nov 2024</strong> — Anthropic open-sources MCP</li><li><strong>Mar 2025</strong> — OpenAI adopts MCP</li><li><strong>Apr 2025</strong> — Google DeepMind adopts</li><li><strong>2025</strong> — Cursor, Zed, Windsurf, Claude Code ship native support</li><li><strong>2026</strong> — de facto standard</li></ul>\n<h2>What MCP actually is (three roles)</h2>\n<ul><li><strong>Host</strong> — the AI application (Claude Desktop, Cursor, Claude Code).</li><li><strong>Client</strong> — lives inside the host; one client per connected server.</li><li><strong>Server</strong> — your code, exposing capabilities; a local process or remote service.</li></ul>\n<p>Host contains Client → Client talks to Server. Built on <strong>JSON-RPC 2.0</strong>.</p>\n<h2>The three primitives</h2>\n<ul><li><strong>Tools</strong> — <em>model-controlled</em>: the AI decides when to call (e.g. <code>search_database</code>, <code>send_email</code>).</li><li><strong>Resources</strong> — <em>app-controlled</em>: the app injects them as context (e.g. file contents, schema docs).</li><li><strong>Prompts</strong> — <em>user-controlled</em>: the user explicitly invokes (e.g. <code>/summarize</code>, <code>/review-pr</code>).</li></ul>\n<blockquote><p>Most cheatsheets skip this distinction — it's the whole mental model.</p></blockquote>\n<h2>How it travels (transport)</h2>\n<ul><li><strong>stdio</strong> — local subprocess; fastest, no network; for local tools (filesystem, git).</li><li><strong>Streamable HTTP</strong> — remote servers; replaced legacy SSE in 2025; for cloud services (GitHub, Notion).</li></ul>\n<h2>Lifecycle</h2>\n<p><code>initialize</code> (protocol version, caps) → <code>capabilities</code> (tools, resources, prompts) → <code>initialized</code> (notification) → <strong>operation phase</strong> (<code>tools/list</code>, <code>tools/call</code>, <code>resources/read</code>, <code>prompts/get</code>) → <code>shutdown</code>.</p>\n<h2>On the wire</h2>\n<p>Every MCP call is a JSON-RPC message:</p>\n<pre><code>{ \"jsonrpc\": \"2.0\", \"id\": 42, \"method\": \"tools/call\",\n  \"params\": { \"name\": \"query_database\",\n              \"arguments\": { \"sql\": \"SELECT * FROM users LIMIT 10\" } } }</code></pre>\n<h2>What nobody tells you (the gotchas)</h2>\n<ul><li><strong>Token tax</strong> — every tool schema rides in every LLM call. 50 tools = 50 schemas, every turn.</li><li><strong>OAuth sprawl</strong> — secret rotation across N servers becomes a real ops burden.</li><li><strong>Tool sprawl</strong> — mirrors the microservices problem: more servers, more breakage surface.</li><li><strong>Schema drift</strong> — a server v1.2 changes a field and your agent silently misroutes.</li><li><strong>Observability gap</strong> — no standard tracing yet; debugging is grep across logs.</li></ul>\n<h2>When MCP wins (vs function calling / LangChain tools)</h2>\n<p>Lower vendor lock-in, growing tool ecosystem, high transport flexibility (stdio + HTTP), and portability across hosts — at the cost of medium ops overhead.</p>\n<h2>Why it matters / how I'd apply it</h2>\n<p>MCP is the standard way to expose tools/data to Claude Code and other hosts, so it's the default integration layer for anything I build. Practical takeaways: keep the <strong>tool count lean</strong> (token tax is per-call), pin/version schemas to avoid drift, plan for <strong>OAuth and secret rotation</strong> early, and add my own tracing since the protocol gives none. Map this onto the Tools &amp; Integrations layer of the <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">agentic reference architecture</a>.</p>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>part-of</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a> — MCP fills layer 4 (Tools &amp; Integrations).</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — MCP is the primitive that gives the harness new tools; Claude Code is an MCP host.</li></ul>",
      "connCount": 9
    },
    {
      "slug": "progressive-disclosure-instructions",
      "title": "One giant instruction file fails — structure CLAUDE.md by progressive disclosure",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "claude-md",
        "context-engineering",
        "instructions",
        "harness"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/progressive-disclosure-instructions.md",
      "html": "<h1>One giant instruction file fails — structure CLAUDE.md by progressive disclosure</h1>\n<p><strong>TL;DR</strong> — Every mistake tempts you to \"add a rule,\" so <code>CLAUDE.md</code>/<code>AGENTS.md</code> only grows. A 600-line instruction file (~10–20K tokens) crowds out the code, tool output, and history the model needs, <strong>and</strong> buries your real constraints where the model won't read them. The fix is <strong>progressive disclosure</strong>: a small always-loaded entry file that links to detailed topic docs the agent pulls <em>on demand</em>.</p>\n<h2>The four failure mechanisms of a bloated file</h2>\n<ol><li><strong>Context-budget exhaustion</strong> — 10–20K tokens of standing instructions leaves too little room for the actual task on anything complex.</li><li><strong>Lost-in-the-middle</strong> (Liu et al., 2023) — LLMs attend strongly to a document's start and end and weakly to the middle; a hard constraint at line 300 of 600 has a high chance of being ignored.</li><li><strong>Indistinguishable priority</strong> — a red-line (\"never use <code>eval()</code>\"), a style preference, and a historical war-story all look identical, so the agent can't tell rules from suggestions.</li><li><strong>Instruction decay</strong> — files only grow; deletion <em>feels</em> risky and addition <em>feels</em> free, so the signal-to-noise ratio rots over time.</li></ol>\n<h2>The architecture: overview now, detail on demand</h2>\n<ul><li><strong>Entry file</strong> (<code>CLAUDE.md</code>/<code>AGENTS.md</code>, 50–200 lines): 1–2 sentence overview, quick-start commands, <strong>≤15 non-negotiable global constraints</strong>, and links to topic docs <em>with their applicability conditions.</em></li><li><strong>Topic docs</strong> (<code>docs/</code>, 50–150 lines each): API patterns, DB rules, testing standards, security policy — loaded only when the task touches them.</li><li><strong>Code-embedded info</strong>: types, interface comments, config explanations — naturally visible while reading code, so no duplication in the instruction file.</li></ul>\n<p><strong>Rule of hygiene:</strong> every instruction should record its <em>origin</em>, its <em>applicability condition</em>, and its <em>removal criteria</em> — otherwise you can't safely shrink the file later.</p>\n<h2>Mental model / why it matters</h2>\n<p>An instruction file isn't a junk drawer; it's a <strong>routing table</strong>. The entry file's job is to be small enough to always fit and ordered so the red-lines sit at the extremes (start/end) where attention is highest — then <em>route</em> to depth on demand. The lecture's team went from 45%→72% task success and 60%→95% security-constraint compliance just by refactoring one fat file into entry + topic docs. Note the second number: compliance rose because the constraints became <em>findable</em>, not because there were more of them. More text ≠ more control; <strong>placement and structure are the control.</strong></p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Cap and split.</strong> When a client's <code>CLAUDE.md</code> passes ~200 lines, that's the signal to extract topic docs and leave ≤15 hard constraints in the entry file. Put the red-lines at top and bottom.</li><li><strong>Tag every rule with a removal criterion.</strong> It's the only thing that lets the file shrink instead of only grow — directly attacks instruction decay.</li><li><strong>Lean on Claude Code's own progressive disclosure.</strong> Nested <code>CLAUDE.md</code> files, <code>@import</code>s, and skills that load only when triggered are the native expression of this pattern — wire detail behind triggers rather than front-loading it into the always-on context.</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>Builds on <a href=\"#\" class=\"kc-link\" data-slug=\"repository-as-system-of-record\">The repository is the agent's only durable memory — make it the system of record</a></strong> — the repo is where instructions live; this is how to shape them so the always-loaded landing page stays small and the detail stays findable.</li><li><strong>Part of <a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a></strong> — the design discipline for the harness's Instructions subsystem.</li><li><strong>Used with <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></strong> — governs how to author the <code>CLAUDE.md</code> every session loads.</li><li><strong>Builds on <a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a></strong> — a 600-line instruction file is a context-engineering failure: standing tokens crowding out the working set.</li></ul>",
      "connCount": 4
    },
    {
      "slug": "prompt-context-harness-engineering",
      "title": "Prompt vs Context vs Harness Engineering",
      "kind": "concept",
      "layer": "foundations",
      "tags": [
        "prompting",
        "context-engineering",
        "harness",
        "agents"
      ],
      "depth": "evergreen",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/prompt-context-harness-engineering.md",
      "html": "<h1>Prompt vs Context vs Harness Engineering</h1>\n<p><img src=\"notes/prompt-context-harness-engineering.jpeg\" alt=\"Prompt vs Context vs Harness Engineering\" loading=\"lazy\"></p>\n<h2>TL;DR</h2>\n<p>Three nested levels of control over an LLM, each a bigger unit of work than the last. <strong>Prompt engineering</strong> shapes one input. <strong>Context engineering</strong> manages what stays in the window across steps. <strong>Harness engineering</strong> wraps the whole gather → act → verify loop — and contains the other two inside it.</p>\n<h2>The three levels</h2>\n<h3>1. Prompt engineering — <em>the message</em> (unit of work: one input)</h3>\n<p>Compose a single best input from five ingredients, send it, get a response, then refine the weakest ingredient and repeat.</p>\n<ul><li><strong>Role</strong> — \"You are a senior engineer…\"</li><li><strong>Context</strong> — docs, history</li><li><strong>Instructions</strong> — what to do</li><li><strong>Examples</strong> — few-shot pairs</li><li><strong>Format</strong> — JSON / schema</li><li>Loop: <code>compose → send → generate → refine the weakest ingredient</code>.</li></ul>\n<h3>2. Context engineering — <em>the memory</em> (unit of work: what stays in the window, step by step)</h3>\n<p>You have a <strong>finite context budget</strong>. A <em>curator</em> selects, compresses, and drops material so only what matters reaches the model each step.</p>\n<ul><li>Inputs to curate: user query, system prompt, retrieved docs, tool outputs, memory, prior turns.</li><li>Flow: <code>gather → curate (select/compress/drop) → feed → inference → step output</code>.</li><li>After each step, <strong>update the working set</strong>: new outputs become context for the next step. (Core loop only — omits state, guardrails, error handling.)</li></ul>\n<h3>3. Harness engineering — <em>the machine</em> (unit of work: the whole loop)</h3>\n<p>The harness is the outer machine with three zones; prompt and context engineering <strong>live inside its Gather step</strong>.</p>\n<ul><li><strong>① GATHER</strong> — context-engineering zone: curator + context window (compressed + dropped, finite budget). Sources: user request, memory (CLAUDE.md, sessions), prior tool outputs, retrieved docs.</li><li><strong>② ACT</strong> — prompt-engineering zone: assemble one prompt → LLM inference → branch to <strong>Tools</strong> (exec, fetch) or <strong>Sub-agents</strong> (specialists).</li><li><strong>③ VERIFY</strong> — a verifier (tests, LLM-as-judge) checks the result. On pass → final response. On fail → <strong>retry: re-run gather → act → verify with updated context.</strong></li></ul>\n<h2>Why it matters / how I'd apply it</h2>\n<p>This is the clearest mental model for <em>where a problem lives</em> when an agent misbehaves. Bad single answer → fix the <strong>prompt</strong>. Model forgetting / drowning in irrelevant tokens → fix <strong>context</strong> (the curator). Agent does the right thing once but can't recover, loop, or self-check → fix the <strong>harness</strong> (verify + retry). Maps directly onto how Claude Code itself works: CLAUDE.md + session = memory, the curated context window = gather, tool/subagent calls = act, tests/judges = verify.</p>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>alternative-to</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a> — the canonical narrative of this same three-level model.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a> — each agent runs this gather→act→verify loop internally.</li><li>see also → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — how every Claude Code primitive maps onto this loop.</li></ul>",
      "connCount": 8
    },
    {
      "slug": "repository-as-system-of-record",
      "title": "The repository is the agent's only durable memory — make it the system of record",
      "kind": "concept",
      "layer": "patterns",
      "tags": [
        "harness",
        "state",
        "memory",
        "agents-md",
        "continuity"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/repository-as-system-of-record.md",
      "html": "<h1>The repository is the agent's only durable memory — make it the system of record</h1>\n<p><strong>TL;DR</strong> — An agent has exactly three input sources: the system prompt, repository file contents, and tool output. Slack threads, Confluence pages, and \"what the senior eng knows\" are <em>invisible</em> to it. <strong>Information that doesn't exist in the repo doesn't exist for the agent.</strong> And because the context window is session-bound and ephemeral, anything that must survive a session has to be written into the repo as a structured file. The repo isn't documentation <em>about</em> the system — for an agent it <em>is</em> the system of record.</p>\n<h2>Why the repo, not the context window</h2>\n<ul><li><strong>The window is temporary.</strong> It exhausts fast, and crucial facts buried deep cost real budget just to rediscover. The repo is the durable, retrievable store.</li><li><strong>Agents can't compensate.</strong> A human fills gaps by asking around; an agent can only use what's documented. So agent success correlates directly with <strong>repository completeness</strong>.</li><li><strong>Knowledge decay is worse than absence.</strong> Stale docs misdirect an agent that confidently executes the wrong instruction — more dangerous than no docs at all. Bind doc updates to code changes.</li></ul>\n<h2>The file set (knowledge lives next to code)</h2>\n<ul><li><strong><code>AGENTS.md</code> / <code>CLAUDE.md</code></strong> (50–100 lines): the landing page — <em>what is this, how do I run it, how do I verify it.</em></li><li><strong><code>ARCHITECTURE.md</code></strong> in module dirs: local decisions, documented where the code lives.</li><li><strong><code>CONSTRAINTS.md</code></strong>: explicit MUST / MUST NOT hard rules.</li><li><strong><code>PROGRESS.md</code></strong>: current commit, test status, done / in-progress / blocked, next actions.</li><li><strong><code>DECISIONS.md</code></strong>: the <em>why</em> behind choices — \"the code is the what; the next session sees the what but not the why\" unless you record it. Stops cross-session <strong>drift</strong>.</li></ul>\n<h2>State management as ACID</h2>\n<p>Treat the repo like a transactional store:</p>\n<ul><li><strong>Atomicity</strong> — one logical change = one git commit; failure rolls back.</li><li><strong>Consistency</strong> — verification predicates (tests/lint) hold after each operation.</li><li><strong>Isolation</strong> — separate progress files per agent; git branches prevent races.</li><li><strong>Durability</strong> — only git-tracked files count; nothing ephemeral survives.</li></ul>\n<h2>Mental model / why it matters</h2>\n<p>Two metrics make this concrete. The <strong>fresh-session test</strong>: can a brand-new agent session, using <em>only</em> the repo, answer the five basic questions (what is this, how to run, how to verify, what's done, what's next)? If not, the repo isn't the system of record yet. And <strong>rebuild cost</strong>: the minutes a new session needs to reach an executable state — a good <code>PROGRESS.md</code> compresses that from ~15 min to ~3 min. A team with 30 microservices and 70% task-failure (decisions scattered across Confluence/Slack/heads) fixed it not by changing models but by consolidating into local <code>ARCHITECTURE.md</code> / <code>CONSTRAINTS.md</code> / <code>PROGRESS.md</code>. The lesson: <strong>always ready to hand off</strong> — at any moment the project should be operable by a fresh agent from documentation alone.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Run the fresh-session test as an audit.</strong> Spin a clean Claude Code session with zero verbal context and ask it the five questions. Every \"I can't tell\" is a missing repo-of-record file — that list <em>is</em> the onboarding backlog.</li><li><strong>Move tribal knowledge into the repo, near the code.</strong> The consulting win is rarely a new tool; it's relocating decisions out of Slack/heads into <code>CONSTRAINTS.md</code> + per-module <code>ARCHITECTURE.md</code>.</li><li><strong>Make doc-with-code a commit rule.</strong> Pair every behavior change with its doc change in the same PR (this vault's own house rule) so knowledge decay never gets a foothold.</li></ul>\n<h2>Connections</h2>\n<ul><li><strong>Part of <a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a></strong> — this is the Instructions + State subsystems made concrete and durable.</li><li><strong>Builds on <a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a></strong> — the context window is the ephemeral layer; anything that must outlive a session gets pushed down into the repo.</li><li><strong>Used with <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide to the primitives</a></strong> — <code>CLAUDE.md</code> is precisely the <code>AGENTS.md</code> landing-page mechanism prescribed here.</li><li><strong>Used with <a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">One shared context vs. many isolated contexts — what actually forks in Claude Code</a></strong> — per-agent progress files and git branches are the ACID isolation that lets forked/parallel agents coordinate without clobbering shared state.</li><li><strong>Used with <a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">The agent must never declare its own victory — externalize \"done\" into the harness</a></strong> — the feature-list state machine that owns \"done\" is itself a repo-of-record artifact the next session reads to know what's left.</li><li>See also <a href=\"#\" class=\"kc-link\" data-slug=\"progressive-disclosure-instructions\">One giant instruction file fails — structure CLAUDE.md by progressive disclosure</a> for <em>how</em> to structure the instruction files so the landing page stays small.</li></ul>",
      "connCount": 8
    },
    {
      "slug": "tool-call-taxonomy",
      "title": "Tool call, function call, MCP, skill — one containment model",
      "kind": "concept",
      "layer": "api",
      "tags": [
        "tool-use",
        "mcp",
        "skills",
        "function-calling",
        "api",
        "claude-code"
      ],
      "depth": "budding",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/tool-call-taxonomy.md",
      "html": "<h1>Tool call, function call, MCP, skill — one containment model</h1>\n<blockquote><p><strong>TL;DR.</strong> \"Tool call,\" \"function call,\" \"MCP call,\" and \"invoking a skill\" feel like four different things; they're mostly <strong>one mechanism with a few specializations.</strong> <strong>Tool use</strong> is the umbrella API capability. A <strong>tool call</strong> is one instance of it: the model emits a <code>tool_use</code> block, your code (or Anthropic's) runs it, you return a <code>tool_result</code>. <strong>\"Function call\" is the same thing under an older name</strong> (OpenAI's term; Anthropic says \"tool use\"). An <strong>MCP</strong> server tool <em>is</em> an ordinary tool call — same JSON, routed through the MCP client. And in Claude Code, <strong>invoking a skill is itself a <code>Skill</code> tool call</strong> — even though a skill is a knowledge package, not an external function. So: <em>yes, calling an MCP is a tool call; yes, invoking a skill is a tool call; \"function call\" ≈ \"tool call.\"</em></p></blockquote>\n<h2>The containment model</h2>\n<pre><code>TOOL USE  (the Claude API capability: you pass a `tools` array; the model may call them)\n│\n└── TOOL CALL  (one instance: model emits `tool_use` { name, id, input } → you return `tool_result`)\n      ├── client tool   — your code runs it (Bash, Read, Edit, a custom function)\n      ├── server tool    — Anthropic runs it for you (web_search, code_execution, web_fetch)\n      ├── MCP tool call  — a tool exposed by an MCP server; same `tool_use` JSON, routed via the MCP client\n      └── Skill tool call (Claude Code) — the model calls the `Skill` tool to load a SKILL.md into context\n\n  \"function call\"  = legacy / OpenAI synonym for \"tool call\" — same idea, different vocabulary</code></pre>\n<h2>Term by term</h2>\n<ul><li><strong>Tool use</strong> — the <em>mechanism</em>. You send messages plus a <code>tools</code> array (name, description, JSON schema). On <code>tool_choice: auto</code> the model decides whether to call a tool or answer directly. This is the \"act\" step of the agent loop made concrete. See <a href=\"#\" class=\"kc-link\" data-slug=\"tool-use\">tool use</a>.</li><li><strong>Tool call</strong> — the <em>instance</em>. Response comes back with <code>stop_reason: \"tool_use\"</code> and one or more <code>tool_use</code> blocks; you execute and reply with matching <code>tool_result</code> blocks; repeat until the model stops calling tools. <strong>Client tools</strong> your code runs; <strong>server tools</strong> Anthropic runs for you.</li><li><strong>Function call</strong> — <strong>the same concept, older word.</strong> OpenAI shipped \"function calling\"; Anthropic chose \"tool use\" because a tool can be a function, an API, an integration, or a server-side capability — not just a local function. When someone says \"function call,\" read \"tool call.\"</li><li><strong>MCP call</strong> — <strong>a tool call, full stop.</strong> An <a href=\"#\" class=\"kc-link\" data-slug=\"mcp\">MCP</a> server advertises its tools via <code>tools/list</code>; Claude Code surfaces them in the <em>same tool namespace</em> as built-ins; calling one emits the <em>same</em> <code>tool_use</code> block and gets routed to the server. MCP is a <em>protocol for supplying tools</em>, not a separate calling mechanism. (MCP also exposes prompts → slash commands and resources → <code>@mentions</code>, which are <em>not</em> tool calls.)</li><li><strong>Skill invocation</strong> — in Claude Code, <strong>a <code>Skill</code> tool call.</strong> The model (or you, via <code>/name</code>) calls the <code>Skill</code> tool, which loads the rendered <code>SKILL.md</code> into the conversation. Permission rules like <code>Skill(deploy *)</code> and <code>disable-model-invocation: true</code> govern it exactly like any other tool. The nuance: <strong>a skill invocation is a tool call, but a skill is not a tool</strong> — it's a knowledge/procedure package that the <code>Skill</code> tool <em>loads</em>, after which the model does the work (often by making more tool calls).</li></ul>\n<h2>The one-sentence answers</h2>\n<ul><li><strong>Calling an MCP — is that a tool call?</strong> <strong>Yes</strong> — identical <code>tool_use</code> shape, just routed to a server.</li><li><strong>Invoking a skill — is that a tool call?</strong> <strong>Yes</strong>, it's a <code>Skill</code> tool call; but the skill itself is instructions, not an external function.</li><li><strong>Tool call vs function call?</strong> <strong>Synonyms.</strong> Anthropic standardized on \"tool use\"; \"function calling\" is the OpenAI-era name for the same thing.</li></ul>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Stop drawing four boxes.</strong> When a client's team argues \"should this be a tool, an MCP, or a skill,\" the clarifying move is: MCP and skills both <em>bottom out in tool calls</em> — the real questions are <em>where the capability lives</em> (your code = tool; an external system = MCP) and <em>whether it's a function or knowledge</em> (function = tool; procedure = skill).</li><li><strong>Permissioning is uniform.</strong> Because skills and MCP both run <em>through</em> tool calls, you gate them with the same permission-rule surface — useful when scoping what an unattended loop or routine is allowed to do.</li></ul>\n<h2>Provenance</h2>\n<p>Distilled from <code>platform.claude.com/docs</code> (tool use) and <code>code.claude.com/docs</code> (MCP, skills), retrieved 2026-06-09. The \"function calling → tool use\" terminology mapping is the documented Anthropic convention; the \"skill invocation = <code>Skill</code> tool call\" framing follows the Claude Code permission model (<code>Skill(...)</code> rules).</p>\n<h2>Connections</h2>\n<ul><li><strong>builds-on <a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">Claude API agent primitives</a></strong> — tool use is the primitive everything here specializes.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol</a></strong> — an MCP tool call is an ordinary tool call routed to a server, not a separate mechanism.</li><li><strong>used-with <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code — field guide</a></strong> — a skill invocation is itself a <code>Skill</code> tool call, so the model explains the harness's surfaces too.</li></ul>",
      "connCount": 4
    },
    {
      "slug": "workflow-journal-resume",
      "title": "Workflow runs are journaled — resume replays finished agents from cache, so orchestration is durable, not just parallel",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "claude-code",
        "workflows",
        "orchestration",
        "reliability",
        "resume"
      ],
      "depth": "seedling",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/workflow-journal-resume.md",
      "html": "<h1>Workflow runs are journaled — resume replays finished agents from cache, so orchestration is durable, not just parallel</h1>\n<h2>TL;DR</h2>\n<p>Every <code>agent()</code> call in a dynamic workflow is journaled with its <code>(prompt, opts)</code> pair and result. Relaunching with <code>Workflow({scriptPath, resumeFromRunId})</code> replays the <strong>longest unchanged prefix</strong> of calls from that journal instantly and runs only the edited/new/failed calls live. Same script + same args = 100% cache hit. That makes a scripted workflow <strong>durable</strong> in a way N ad-hoc <code>Agent</code> calls never are — and durability, not parallelism, is the decisive argument for scripting the orchestration when the work is destructive or expensive to redo.</p>\n<h2>The lived example (Tide, 2026-06-10 — how to recollect this)</h2>\n<p>Removing Tide's Progress/challenges tab end-to-end (mobile UI, backend, 5 DB tables, AI prompt, evals, docs) ran as a 4-agents-in-<code>parallel()</code> + verify workflow. Mid-run, the <strong>monthly spend limit hit</strong>: the mobile agent had finished its removal; the backend, docs, AI/evals, and verify agents all died.</p>\n<p>One call fixed it after credits were enabled:</p>\n<pre><code>Workflow({ scriptPath: &lt;same script&gt;, resumeFromRunId: \"wf_0d5bb96d-f7d\" })</code></pre>\n<ul><li>The completed <strong>mobile agent replayed from the journal cache</strong> — zero tokens, instant, and its structured report flowed into the verify agent's prompt exactly as designed.</li><li>Only the four dead agents re-ran. The re-run backend agent even found the worktree already partially edited (file state had survived alongside the journal), verified those edits with grep instead of redoing them, and created only the missing drop migration.</li><li>Net cost of the crash: <strong>nothing structural</strong>. The run finished green (65/65 tests) and shipped as tide PR #180.</li></ul>\n<p>Had the same task been four ad-hoc <code>Agent</code> tool calls, the crash would have left an untracked half-edited tree and a manual archaeology session: which agent finished? which files are half-done? Re-prompting from memory risks double-edits on a <em>destructive</em> change.</p>\n<h2>Why it works (the mechanism, and what it forbids)</h2>\n<p>Replay is only sound because a workflow script is <strong>deterministic</strong>: each <code>agent()</code> result is fully keyed by its <code>(prompt, opts)</code>. This is exactly why <code>Date.now()</code>, <code>Math.random()</code>, and argless <code>new Date()</code> are banned inside workflow scripts — a prompt that embedded a timestamp would never match its journal entry, silently breaking the cache. (Pass timestamps in via <code>args</code>; stamp results after the run returns.) Two complementary stores do the recovery: the <strong>journal</strong> preserves compute (agent results), the <strong>git worktree</strong> preserves file state (edits already on disk). Resume reunites them.</p>\n<h2>Mental model</h2>\n<p>This is <strong>checkpointing for compute</strong>. Claude Code's file checkpointing lets you rewind bad edits; the workflow journal lets you <em>fast-forward past good work</em>. Same risk-math shift: once partial progress is recoverable, mid-run failures (rate limits, spend caps, a killed session, an edited script) stop being catastrophic, so you can take on bigger, more destructive multi-surface tasks in one orchestrated run. It's the same idea as Temporal/ durable-execution engines in distributed systems — event-source the step results, replay on restart — built into the harness.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Default destructive multi-surface work to one scripted workflow</strong>, not N ad-hoc agents — migrations, feature removals, repo-wide rewrites. The journal is the rollback-free safety story you can give a client: \"if anything dies mid-run, we resume, we don't redo.\"</li><li><strong>Iterate on workflows cheaply</strong>: edit the persisted script file and resume — every agent <em>before</em> your edit returns from cache, so debugging stage 3 doesn't re-bill stages 1–2. (Prefix rule: the first changed call and everything after it re-runs.)</li><li><strong>Keep prompts deterministic</strong> (no timestamps/randomness inline; thread them through <code>args</code>) or you silently forfeit the cache.</li><li><strong>Make removal agents idempotent-aware</strong>: instruct them to verify-before-redo (grep what already happened in the worktree), so a resumed run converges instead of double-editing.</li></ul>\n<h2>Connections</h2>\n<p><em>Typed links — mirrored from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the Ultraplan loop</a> — the journal records the results of the primitives that note explains.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns</a> — durability is the unlisted seventh reason to script the fan-out: every shape becomes restartable.</li><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">One shared context vs. many isolated contexts</a> — replay is only valid because each agent's (prompt, opts) fully determine its work.</li><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"worktree-base-ref-fresh-vs-head\">Worktree base ref: fresh vs head</a> — journal preserves compute, worktree preserves file state; resume reunites both.</li></ul>",
      "connCount": 4
    },
    {
      "slug": "worktree-base-ref-fresh-vs-head",
      "title": "worktree.baseRef — fresh vs head decides what an agent's worktree branches from",
      "kind": "concept",
      "layer": "claude-code",
      "tags": [
        "claude-code",
        "worktrees",
        "subagents",
        "isolation",
        "config",
        "git"
      ],
      "depth": "seedling",
      "claudeSpecific": true,
      "url": "https://github.com/vikkybharadwaj/ai_knowledgecenter/blob/main/knowledge/notes/worktree-base-ref-fresh-vs-head.md",
      "html": "<h1>worktree.baseRef — <code>fresh</code> vs <code>head</code></h1>\n<blockquote><p><strong>The one question this setting answers:</strong> when Claude Code spins up a new worktree for a subagent, <em>what code does that agent start from</em> — the clean published branch, or your messy in-progress HEAD?</p></blockquote>\n<h2>TL;DR</h2>\n<p><code>worktree.baseRef</code> controls the base ref new worktrees branch from when subagents/worktrees are created. <strong><code>fresh</code> (the default)</strong> branches from clean, <strong>published</strong> code — your uncommitted and unpushed work is invisible to the agent. <strong><code>head</code></strong> branches from your current <strong>HEAD</strong>, so the agent sees your in-progress feature branch, including uncommitted/unpushed changes. Default to <code>fresh</code> for safety and reproducibility; switch to <code>head</code> only when an agent genuinely needs to operate on work you haven't pushed yet.</p>\n<h2>The two values</h2>\n<table><thead><tr><th></th><th><code>fresh</code> (default)</th><th><code>head</code></th></tr></thead><tbody><tr><td><strong>Branches from</strong></td><td>Clean, published code (remote/base branch)</td><td>Your current HEAD</td></tr><tr><td><strong>Sees your WIP?</strong></td><td>No — uncommitted &amp; unpushed work is excluded</td><td>Yes — includes uncommitted / unpushed changes</td></tr><tr><td><strong>Best when</strong></td><td>You want isolation, reproducibility, a clean separation between your WIP and what agents see</td><td>The agent must build on your in-progress feature branch / live state</td></tr></tbody></table>\n<p><strong>Choose <code>fresh</code> if:</strong> you want subagents and worktrees working on clean published code; you're worried unpushed commits could leak into isolation or hurt reproducibility; you want a hard wall between your WIP and what agents see.</p>\n<p><strong>Choose <code>head</code> if:</strong> you need subagents to operate on your in-progress feature branches; you want worktrees to include uncommitted or unpushed work; you're actively developing and want agents to see your current state.</p>\n<h2>Mental model / why it matters</h2>\n<p>Worktree-based parallelism is the <em>isolation</em> lever — each agent gets its own copy of the repo so parallel work doesn't collide (see <a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">execution-context-isolation</a> for <em>context</em> isolation, this is its <em>filesystem/git</em> sibling). <code>baseRef</code> answers the second-order question that isolation alone doesn't: <strong>isolation <em>from what state</em>?</strong> <code>fresh</code> makes the fork a clean-room — every agent starts from the same known-good published baseline, so results are reproducible and your local mess can't contaminate them. <code>head</code> trades that clean room for <em>currency</em> — the agent inherits exactly where you are right now, WIP and all. It's the same trade the multi-agent gotchas warn about (#3 <code>.env</code> files don't auto-carry, #5 clean up worktrees): a worktree is a <em>partial snapshot</em>, and <code>baseRef</code> is the dial for how much of your live state it snapshots.</p>\n<h2>How to apply (in practice / consulting)</h2>\n<ul><li><strong>Leave it on <code>fresh</code> by default.</strong> For most parallel/feature work, agents should build on published code — it keeps runs reproducible and isolates your half-finished work.</li><li><strong>Flip to <code>head</code> deliberately, not habitually.</strong> Use it when you're mid-feature and want an agent to extend <em>this</em> branch (e.g. \"write tests for the function I just wrote but haven't committed\"). Then consider flipping back.</li><li><strong>Tell clients the reproducibility story.</strong> <code>fresh</code> means \"every agent saw the same committed baseline\" — that's the answer when someone asks <em>why did two agents diverge?</em> On <code>head</code>, divergence can come from your moving local state, which is harder to audit.</li><li><strong>Pair the choice with the worktree hygiene rules:</strong> unpushed work + <code>head</code> means agents depend on state that lives only on your machine — commit/push if you need that work to survive.</li></ul>\n<h2>Connections</h2>\n<p><em>Typed links — the concept graph is built from the <code>connections:</code> frontmatter above.</em></p>\n<ul><li><strong>used-with</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a> — the config knob behind the worktree method.</li><li><strong>builds-on</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">One shared context vs. many isolated contexts</a> — forking is the lever; <code>baseRef</code> sets <em>which state</em> you fork from.</li><li><strong>part-of</strong> → <a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture</a> — a \"coordinate work\" setting that tunes the git substrate under worktree parallelism.</li></ul>",
      "connCount": 4
    }
  ],
  "edges": [
    {
      "from": "agentic-ai-reference-architecture",
      "to": "harness-vs-context-engineering",
      "type": "builds-on",
      "why": "The production system view of the same gather→act→verify loop each agent runs."
    },
    {
      "from": "agentic-ai-reference-architecture",
      "to": "claude-code-multi-agent-development",
      "type": "used-with",
      "why": "Multi-agent methods realize the Agent Layer (3) of this blueprint."
    },
    {
      "from": "anatomy-of-an-agent-harness",
      "to": "harness-vs-context-engineering",
      "type": "builds-on",
      "why": "The article's prompt ⊂ context ⊂ harness framing is exactly this note; it then dissects the harness level into components."
    },
    {
      "from": "anatomy-of-an-agent-harness",
      "to": "claude-code-architecture",
      "type": "used-with",
      "why": "Claude Code/Agent SDK is the canonical thin harness — most of the 12 components map onto its primitives."
    },
    {
      "from": "anatomy-of-an-agent-harness",
      "to": "agentic-ai-reference-architecture",
      "type": "used-with",
      "why": "The nine-layer reference architecture is the production-grade view of the same 12 components."
    },
    {
      "from": "anatomy-of-an-agent-harness",
      "to": "model-context-protocol",
      "type": "used-with",
      "why": "Tools (component 2) are exposed to the harness via MCP."
    },
    {
      "from": "anatomy-of-an-agent-harness",
      "to": "dynamic-workflows-and-ultraplan",
      "type": "used-with",
      "why": "Subagent orchestration (component 11) — Fork/Teammate/Worktree — is the scripted-fan-out idea workflows generalize."
    },
    {
      "from": "automation-control-flow-spectrum",
      "to": "claude-code-routines",
      "type": "contrasts-with",
      "why": "Routines are the cloud/unattended corner of the grid; /loop and /goal are the in-session corner — same 'repeat' idea, opposite infrastructure."
    },
    {
      "from": "automation-control-flow-spectrum",
      "to": "completion-is-externalized",
      "type": "builds-on",
      "why": "/goal is condition-driven because a separate evaluator decides 'done' — externalized completion is what makes the condition axis trustworthy."
    },
    {
      "from": "automation-control-flow-spectrum",
      "to": "designing-loops-not-prompts",
      "type": "used-with",
      "why": "These primitives are the scheduling/heartbeat block of loop engineering — the spectrum says which one to reach for."
    },
    {
      "from": "automation-control-flow-spectrum",
      "to": "loop-is-cron-plus-a-decision-maker",
      "type": "used-with",
      "why": "That note defines what a loop IS; this one tells the concrete Claude Code primitives apart."
    },
    {
      "from": "bootstrapping-company-ai-stack",
      "to": "claude-code-vs-build-your-own",
      "type": "used-in",
      "why": "The four-altitude decision tree is the engine that decides what to buy, configure, and own for this client."
    },
    {
      "from": "bootstrapping-company-ai-stack",
      "to": "model-context-protocol",
      "type": "used-in",
      "why": "Phase 2 of the rollout is wrapping the company's internal systems as MCP servers — the integration backbone."
    },
    {
      "from": "bootstrapping-company-ai-stack",
      "to": "agentic-ai-reference-architecture",
      "type": "used-in",
      "why": "The nine-layer reference architecture is the target end-state the rollout converges toward."
    },
    {
      "from": "bootstrapping-company-ai-stack",
      "to": "claude-code-architecture",
      "type": "used-in",
      "why": "Phase 1 gives the dev team Claude Code + shared skills/hooks/CLAUDE.md — the fastest first win."
    },
    {
      "from": "claude-agent-sdk",
      "to": "claude-api-agent-primitives",
      "type": "builds-on",
      "why": "The SDK wraps the raw Messages API tool-use loop into a managed agent loop so you don't hand-roll it."
    },
    {
      "from": "claude-agent-sdk",
      "to": "claude-code-architecture",
      "type": "enables",
      "why": "Anthropic calls the Agent SDK 'the agent harness that powers Claude Code' — the CLI is the SDK's most-polished consumer."
    },
    {
      "from": "claude-agent-sdk",
      "to": "model-context-protocol",
      "type": "used-with",
      "why": "The SDK connects external systems via mcp_servers config (stdio or remote)."
    },
    {
      "from": "claude-agent-sdk",
      "to": "anatomy-of-an-agent-harness",
      "type": "builds-on",
      "why": "The SDK ships most of the 12 harness organs as configurable primitives instead of code you write."
    },
    {
      "from": "claude-api-agent-primitives",
      "to": "claude-agent-sdk",
      "type": "enables",
      "why": "The SDK automates the raw tool-use loop these primitives expose — same engine, less boilerplate."
    },
    {
      "from": "claude-api-agent-primitives",
      "to": "model-context-protocol",
      "type": "used-with",
      "why": "The Messages API MCP connector lets a raw API call reach external MCP tools without any framework."
    },
    {
      "from": "claude-api-agent-primitives",
      "to": "anatomy-of-an-agent-harness",
      "type": "used-with",
      "why": "These primitives are the API-level implementation of the harness's tools / orchestration-loop organs."
    },
    {
      "from": "claude-code-architecture",
      "to": "harness-vs-context-engineering",
      "type": "builds-on",
      "why": "Every Claude Code primitive exists to tune one zone of the gather→act→verify loop."
    },
    {
      "from": "claude-code-architecture",
      "to": "model-context-protocol",
      "type": "used-with",
      "why": "MCP is one of the nine primitives — the model's reach into external tools."
    },
    {
      "from": "claude-code-architecture",
      "to": "claude-code-multi-agent-development",
      "type": "used-with",
      "why": "Subagents, agent teams and worktrees are the 'coordinate work' primitives."
    },
    {
      "from": "claude-code-architecture",
      "to": "dynamic-workflows-and-ultraplan",
      "type": "used-with",
      "why": "Dynamic workflows are the script-orchestrates-many-agents primitive."
    },
    {
      "from": "claude-code-architecture",
      "to": "claude-code-routines",
      "type": "used-with",
      "why": "Routines are the unattended cloud 'operate' primitive — a saved config a trigger runs on Anthropic's cloud."
    },
    {
      "from": "claude-code-multi-agent-development",
      "to": "claude-code-architecture",
      "type": "part-of",
      "why": "Worktrees, subagents and agent teams are the 'coordinate work' primitives in the field guide."
    },
    {
      "from": "claude-code-multi-agent-development",
      "to": "prompt-context-harness-engineering",
      "type": "builds-on",
      "why": "Subagents run inside the harness ACT step."
    },
    {
      "from": "claude-code-routines",
      "to": "claude-code-architecture",
      "type": "part-of",
      "why": "Routines are the unattended cloud 'operate' member of the Claude Code primitive set — the field guide's tenth entry."
    },
    {
      "from": "claude-code-routines",
      "to": "dynamic-workflow-patterns",
      "type": "used-with",
      "why": "/loop is the in-session local cousin of a scheduled routine; both run a saved procedure repeatedly, on different infrastructure."
    },
    {
      "from": "claude-code-routines",
      "to": "model-context-protocol",
      "type": "used-with",
      "why": "A routine bundles connectors/MCP servers into its saved config so the unattended cloud run can reach external systems."
    },
    {
      "from": "claude-code-routines",
      "to": "execution-context-isolation",
      "type": "contrasts-with",
      "why": "Routine runs are fully autonomous cloud sessions with NO permission prompts/approval mode — the opposite end of the spectrum from an interactive local session."
    },
    {
      "from": "claude-code-routines",
      "to": "claude-code-multi-agent-development",
      "type": "alternative-to",
      "why": "Both run Claude work off your main thread, but a routine runs unattended on Anthropic's cloud on a trigger, not as peer/worker agents you supervise."
    },
    {
      "from": "claude-code-vs-build-your-own",
      "to": "anatomy-of-an-agent-harness",
      "type": "builds-on",
      "why": "Operationalizes the harness's 'thickness' and 'single-vs-multi-agent' decisions into a concrete buy/build/own-the-loop choice."
    },
    {
      "from": "claude-code-vs-build-your-own",
      "to": "claude-agent-sdk",
      "type": "used-with",
      "why": "The SDK is the 'configure a harness' rung — the answer when a finished product is too rigid but the raw loop is too much."
    },
    {
      "from": "claude-code-vs-build-your-own",
      "to": "claude-code-architecture",
      "type": "used-with",
      "why": "Claude Code (the CLI) is the 'use the finished harness' rung — the default for technical/internal work."
    },
    {
      "from": "claude-code-vs-build-your-own",
      "to": "agentic-ai-reference-architecture",
      "type": "used-with",
      "why": "The choice feeds the production layers — whichever rung you pick still needs the nine-layer architecture around it."
    },
    {
      "from": "completion-is-externalized",
      "to": "harness-engineering-discipline",
      "type": "part-of",
      "why": "This is the Feedback subsystem made rigorous — the harness, not the agent, owns the completion signal."
    },
    {
      "from": "completion-is-externalized",
      "to": "dynamic-workflow-patterns",
      "type": "builds-on",
      "why": "Separating worker from checker is adversarial verification and generate-and-filter applied to the 'done' decision."
    },
    {
      "from": "completion-is-externalized",
      "to": "anatomy-of-an-agent-harness",
      "type": "used-with",
      "why": "Operationalizes the verification organ — three layers, E2E, and runtime observability feeding the loop."
    },
    {
      "from": "completion-is-externalized",
      "to": "repository-as-system-of-record",
      "type": "used-with",
      "why": "The feature-list state machine and its passing/blocked states are durable repo files the next session trusts."
    },
    {
      "from": "containment-vs-discrimination",
      "to": "automation-control-flow-spectrum",
      "type": "used-with",
      "why": "That where×trigger grid IS the discrimination view of the keep-working family; this lens names why a grid (siblings), not a ladder (levels), is the right shape for choosing."
    },
    {
      "from": "containment-vs-discrimination",
      "to": "prompt-context-harness-engineering",
      "type": "used-with",
      "why": "prompt ⊂ context ⊂ harness ⊂ loop is the containment view; this lens explains it answers 'what's inside what,' which is a different question than 'which do I pick.'"
    },
    {
      "from": "containment-vs-discrimination",
      "to": "designing-loops-not-prompts",
      "type": "builds-on",
      "why": "Resolves where loop engineering sits: the ④ containment ring for understanding, AND a discrimination grid for deciding — placement and content answer different questions."
    },
    {
      "from": "containment-vs-discrimination",
      "to": "tool-call-taxonomy",
      "type": "used-with",
      "why": "Same disambiguation move at a smaller scale — the docs separate a confusable family by question, not by stacking it into levels."
    },
    {
      "from": "designing-loops-not-prompts",
      "to": "harness-engineering-discipline",
      "type": "builds-on",
      "why": "A loop is the harness put on a timer that feeds itself — loop engineering sits one floor above harness engineering."
    },
    {
      "from": "designing-loops-not-prompts",
      "to": "completion-is-externalized",
      "type": "builds-on",
      "why": "An unattended loop only works if 'done' is externally verified — /goal's separate Haiku evaluator is exactly this move."
    },
    {
      "from": "designing-loops-not-prompts",
      "to": "claude-code-multi-agent-development",
      "type": "used-with",
      "why": "The loop's parallelism (worktrees) and maker/checker split (subagents) are the multi-agent primitives applied on a schedule."
    },
    {
      "from": "designing-loops-not-prompts",
      "to": "repository-as-system-of-record",
      "type": "used-with",
      "why": "The loop's sixth piece — memory — is the repo/disk, because the model forgets everything between runs."
    },
    {
      "from": "designing-loops-not-prompts",
      "to": "claude-code-routines",
      "type": "used-with",
      "why": "Routines / GitHub Actions / Claude Code on the web are how a loop actually runs unattended after you close the laptop."
    },
    {
      "from": "dynamic-workflow-patterns",
      "to": "dynamic-workflows-and-ultraplan",
      "type": "builds-on",
      "why": "These are the named, reusable shapes you script on top of the raw agent()/parallel()/pipeline() primitives."
    },
    {
      "from": "dynamic-workflow-patterns",
      "to": "execution-context-isolation",
      "type": "used-with",
      "why": "Every pattern works by fanning the main window into isolated subagent contexts — the unit this concept describes."
    },
    {
      "from": "dynamic-workflow-patterns",
      "to": "anatomy-of-an-agent-harness",
      "type": "builds-on",
      "why": "Adversarial verification and loop-until-done are the harness's 'verification' and 'loop' organs, externalised across agents."
    },
    {
      "from": "dynamic-workflow-patterns",
      "to": "agentic-ai-reference-architecture",
      "type": "used-in",
      "why": "These orchestration patterns are how the reference architecture's agent layer actually gets composed in Claude Code."
    },
    {
      "from": "dynamic-workflows-and-ultraplan",
      "to": "claude-code-architecture",
      "type": "part-of",
      "why": "Dynamic workflows are one of the orchestration primitives in the field guide."
    },
    {
      "from": "dynamic-workflows-and-ultraplan",
      "to": "claude-code-multi-agent-development",
      "type": "used-with",
      "why": "A workflow scripts fan-out over subagents / parallel agents — the deterministic cousin of agent teams."
    },
    {
      "from": "dynamic-workflows-and-ultraplan",
      "to": "harness-vs-context-engineering",
      "type": "builds-on",
      "why": "A workflow is a scripted harness wrapping many sub-harnesses; each agent runs its own gather→act→verify loop."
    },
    {
      "from": "execution-context-isolation",
      "to": "harness-vs-context-engineering",
      "type": "builds-on",
      "why": "Context is the scarce resource; forking it into fresh windows is the core move every agent/workflow makes."
    },
    {
      "from": "execution-context-isolation",
      "to": "claude-code-architecture",
      "type": "part-of",
      "why": "This is the 'how work is coordinated' axis of the field guide, made concrete: modes stay in one window, agents fork new ones."
    },
    {
      "from": "execution-context-isolation",
      "to": "dynamic-workflows-and-ultraplan",
      "type": "enables",
      "why": "A workflow's whole value is forking the main window into N isolated child contexts — this note explains the unit it forks."
    },
    {
      "from": "execution-context-isolation",
      "to": "claude-code-multi-agent-development",
      "type": "used-with",
      "why": "Subagents, agent teams and the agent view are all ways of running work in separate contexts that report back a summary."
    },
    {
      "from": "harness-engineering-discipline",
      "to": "anatomy-of-an-agent-harness",
      "type": "builds-on",
      "why": "Anatomy names the six organs; this note supplies the why and the five-subsystem audit that says which organ to invest in first."
    },
    {
      "from": "harness-engineering-discipline",
      "to": "prompt-context-harness-engineering",
      "type": "contrasts-with",
      "why": "Reliability is won at the harness level, not the prompt level — the same model swings 20%→100% with the prompt untouched."
    },
    {
      "from": "harness-engineering-discipline",
      "to": "harness-vs-context-engineering",
      "type": "used-with",
      "why": "Same three-level model; this adds the empirical 'controlled exclusion test' for proving which level a failure lives on."
    },
    {
      "from": "harness-engineering-discipline",
      "to": "claude-code-vs-build-your-own",
      "type": "used-with",
      "why": "Deciding whether to buy/use/configure/own is really a decision about how much harness you must build yourself."
    },
    {
      "from": "harness-vs-context-engineering",
      "to": "prompt-context-harness-engineering",
      "type": "builds-on",
      "why": "Expands the original diagram into the canonical narrative model."
    },
    {
      "from": "harness-vs-context-engineering",
      "to": "agentic-ai-reference-architecture",
      "type": "used-with",
      "why": "The agentic reference architecture is the full production system view of this same loop."
    },
    {
      "from": "loop-is-cron-plus-a-decision-maker",
      "to": "designing-loops-not-prompts",
      "type": "builds-on",
      "why": "Same loop-engineering thesis from a second 2026 source — this note adds the lineage, the one-line definition, and the economics the other note doesn't carry."
    },
    {
      "from": "loop-is-cron-plus-a-decision-maker",
      "to": "automation-control-flow-spectrum",
      "type": "used-with",
      "why": "The lineage ends in the exact Claude Code primitives (/loop, /goal, routines) that the spectrum note disambiguates."
    },
    {
      "from": "loop-is-cron-plus-a-decision-maker",
      "to": "completion-is-externalized",
      "type": "used-with",
      "why": "Boris's fifth tip — a loop must self-verify end to end — is externalized completion stated as the thing that makes a loop trustworthy."
    },
    {
      "from": "loop-is-cron-plus-a-decision-maker",
      "to": "claude-code-routines",
      "type": "used-with",
      "why": "Stage five of the lineage (laptop closed, durable, on a schedule) is literally what a cloud routine provides."
    },
    {
      "from": "model-context-protocol",
      "to": "agentic-ai-reference-architecture",
      "type": "part-of",
      "why": "MCP standardizes the Tools & Integrations layer (4) of the reference architecture."
    },
    {
      "from": "model-context-protocol",
      "to": "claude-code-architecture",
      "type": "used-with",
      "why": "MCP is the primitive that gives the Claude Code harness new tools and data."
    },
    {
      "from": "progressive-disclosure-instructions",
      "to": "repository-as-system-of-record",
      "type": "builds-on",
      "why": "The repo holds the instructions; this is how to shape those files so the landing page stays small and findable."
    },
    {
      "from": "progressive-disclosure-instructions",
      "to": "harness-engineering-discipline",
      "type": "part-of",
      "why": "It's the design discipline for the Instructions subsystem of the five-subsystem harness."
    },
    {
      "from": "progressive-disclosure-instructions",
      "to": "claude-code-architecture",
      "type": "used-with",
      "why": "Directly governs how to write the CLAUDE.md every Claude Code session loads."
    },
    {
      "from": "progressive-disclosure-instructions",
      "to": "prompt-context-harness-engineering",
      "type": "builds-on",
      "why": "A 600-line instruction file is a context-engineering failure — it crowds out the code and tool output the model actually needs."
    },
    {
      "from": "prompt-context-harness-engineering",
      "to": "harness-vs-context-engineering",
      "type": "alternative-to",
      "why": "Same prompt ⊂ context ⊂ harness model — that note is the canonical narrative, this is the diagram."
    },
    {
      "from": "prompt-context-harness-engineering",
      "to": "agentic-ai-reference-architecture",
      "type": "used-with",
      "why": "This gather→act→verify loop is what each agent in the reference architecture runs internally."
    },
    {
      "from": "repository-as-system-of-record",
      "to": "harness-engineering-discipline",
      "type": "part-of",
      "why": "Concretizes two of the five subsystems — Instructions and State — into durable repo files."
    },
    {
      "from": "repository-as-system-of-record",
      "to": "prompt-context-harness-engineering",
      "type": "builds-on",
      "why": "The context window is the ephemeral layer; this pushes everything that must survive a session down into the repo."
    },
    {
      "from": "repository-as-system-of-record",
      "to": "claude-code-architecture",
      "type": "used-with",
      "why": "CLAUDE.md is exactly the AGENTS.md landing-page mechanism this note prescribes."
    },
    {
      "from": "repository-as-system-of-record",
      "to": "execution-context-isolation",
      "type": "used-with",
      "why": "Per-agent progress files + git branches are the ACID isolation that lets forked contexts coordinate without clobbering each other."
    },
    {
      "from": "repository-as-system-of-record",
      "to": "completion-is-externalized",
      "type": "used-with",
      "why": "The feature-list state machine that owns 'done' is itself a repo-of-record file the next session reads."
    },
    {
      "from": "tool-call-taxonomy",
      "to": "claude-api-agent-primitives",
      "type": "builds-on",
      "why": "Tool use is the API primitive everything here specializes — this note draws the containment boundaries around it."
    },
    {
      "from": "tool-call-taxonomy",
      "to": "model-context-protocol",
      "type": "used-with",
      "why": "An MCP server tool surfaces to the model as an ordinary tool-use call — MCP is a special case inside the boundary, not a different mechanism."
    },
    {
      "from": "tool-call-taxonomy",
      "to": "claude-code-architecture",
      "type": "used-with",
      "why": "In Claude Code a skill invocation is itself a Skill tool call, so the same containment model explains the harness's own surfaces."
    },
    {
      "from": "workflow-journal-resume",
      "to": "dynamic-workflows-and-ultraplan",
      "type": "builds-on",
      "why": "The journal records the results of the agent()/parallel()/pipeline() primitives that note explains — resume is a property of those calls."
    },
    {
      "from": "workflow-journal-resume",
      "to": "dynamic-workflow-patterns",
      "type": "used-with",
      "why": "Durability is the seventh, unlisted reason to script a workflow: every fan-out shape becomes restartable mid-run instead of all-or-nothing."
    },
    {
      "from": "workflow-journal-resume",
      "to": "execution-context-isolation",
      "type": "builds-on",
      "why": "Replay-from-cache is only valid because each agent is an isolated context whose (prompt, opts) fully determine its work — shared mutable context would make cached results stale."
    },
    {
      "from": "workflow-journal-resume",
      "to": "worktree-base-ref-fresh-vs-head",
      "type": "used-with",
      "why": "The journal preserves compute; the worktree preserves file state. A resumed run recovers both halves of a partially-finished change."
    },
    {
      "from": "worktree-base-ref-fresh-vs-head",
      "to": "claude-code-multi-agent-development",
      "type": "used-with",
      "why": "This is the config knob behind the worktree method — it decides whether each isolated worktree starts from published code or your live HEAD."
    },
    {
      "from": "worktree-base-ref-fresh-vs-head",
      "to": "execution-context-isolation",
      "type": "builds-on",
      "why": "Forking is the isolation lever; baseRef sets *which state* the forked worktree forks FROM — clean remote vs. your WIP."
    },
    {
      "from": "worktree-base-ref-fresh-vs-head",
      "to": "claude-code-architecture",
      "type": "part-of",
      "why": "A 'coordinate work' setting in the field guide — it tunes the git substrate that worktree-based parallelism runs on."
    }
  ],
  "docs": {
    "big-picture": {
      "title": "The Big Picture — how the Claude stack fits together",
      "html": "<h1>The Big Picture 🧠</h1>\n<blockquote><p><strong>This is the spine of the whole knowledge center.</strong> Every concept hangs off one layer below. When you land a new doc/article/feature, it gets placed here — so the map grows into a single connected brain instead of a pile of isolated notes. Read this top-to-bottom to <em>go broad</em>; click into any note to <em>go deep</em>.</p></blockquote>\n<blockquote><p>🗺️ <strong>Two things live on this spine, and they are not the same.</strong> The interactive <span class=\"kc-ref\">concept graph</span> plots <strong>tech-stack concepts</strong> — the primitives, systems, and patterns of the Claude stack (Tool Use, MCP, Subagents, Worktrees, Orchestration…) — wired by <strong>dependency</strong> edges (solid: <em>runs-on, depends-on, part-of, uses</em>; arrows point to what a thing needs) and <strong>relation</strong> edges (dashed: <em>used-with, alternative-to</em>), so you can read the stack bottom-up and learn how it fits. The <strong>source notes</strong> listed below are the distilled articles that <em>back</em> those concepts — the content you read to go deep. Concepts are the map; notes are the territory. The graph is generated from <code>knowledge/concepts/*.md</code>; the notes live in <code>knowledge/notes/*.md</code>.</p></blockquote>\n<h2>The spine (one stack, bottom powers top)</h2>\n<pre><code>┌─────────────────────────────────────────────────────────────┐\n│  PRODUCTS &amp; CONSULTING   real builds, client scenarios       │  ← what you ship\n│        ▲  ship as                                            │\n│  AGENT PATTERNS          chaining, routing, orchestrator-…   │  ← how you compose\n│        ▲  compose into                                       │\n│  CLAUDE CODE PRIMITIVES  skills, hooks, MCP, subagents, …    │  ← the toolbox\n│        ▲  packaged as                                        │\n│  AGENT SDK               build your own agents on the loop   │  ← the framework\n│        ▲  exposes                                            │\n│  CLAUDE API              tool use, caching, thinking, batch  │  ← the engine\n│        ▲  runs on                                            │\n│  FOUNDATIONS             prompt ⊂ context ⊂ harness          │  ← the physics\n└─────────────────────────────────────────────────────────────┘</code></pre>\n<p>Each layer is a <strong>lens on the same gather→act→verify loop</strong>, zoomed out one level. A bug or a design decision always lives on <em>some</em> layer — naming the layer is half the battle.</p>\n<h2>What lives on each layer right now</h2>\n<h3>⚛️ Foundations — <em>the physics of steering an LLM</em></h3>\n<p>The mental models everything else obeys.</p>\n<ul><li><a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a> — the three nested levels of control.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a> — canonical narrative of the same model; <em>where a bug lives tells you which level to fix.</em></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a> — dissects the \"harness\" level into its six organs (loop, context, tools, memory, verification, delegation).</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a> — the discipline: capable models still fail; the <em>same</em> model swings 20%→100% as you add the five harness subsystems (instructions, tools, environment, state, feedback).</li></ul>\n<h3>🔌 Claude API — <em>the engine</em></h3>\n<p>The raw capabilities a request can use.</p>\n<ul><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives</a> — the tool-use loop, MCP connector, prompt caching, batch, computer use; <em>an agent is ~20 lines.</em></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"tool-call-taxonomy\">Tool call, function call, MCP, skill — one containment model</a> — \"tool use\" is the umbrella; an MCP call <em>is</em> a tool call, a skill invocation <em>is</em> a <code>Skill</code> tool call, and \"function call\" is the legacy synonym.</li></ul>\n<h3>🛠️ Agent SDK — <em>the framework</em></h3>\n<p>Building your own agents on top of the loop.</p>\n<ul><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK</a> — the same engine as Claude Code, as a library: hooks, subagents, MCP, sessions, headless/CI, Bedrock/Vertex/Foundry auth.</li></ul>\n<h3>🧭 Claude Code primitives — <em>the toolbox</em></h3>\n<p>The named features that tune one zone of the loop.</p>\n<ul><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide</a> — the one model that slots every primitive into place.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol (MCP)</a> — how the model <em>reaches</em> external tools/data.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a> — worktrees / agent teams / subagents.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the Ultraplan loop</a> — scripting fan-out across many agents.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"workflow-journal-resume\">Workflow runs are journaled — resume replays finished agents from cache</a> — <code>resumeFromRunId</code> makes orchestration <em>durable</em>, not just parallel; the decisive reason to script destructive multi-surface work (lived: Tide PR #180 survived a mid-run spend-limit kill at zero structural cost).</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">One shared context vs. many isolated contexts</a> — what actually forks: <em>modes</em> stay in one window, <em>agents</em> fork fresh ones (+ the six permission modes).</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"worktree-base-ref-fresh-vs-head\">worktree.baseRef — fresh vs head</a> — the git dial under worktree parallelism: branch agents from clean published code (<code>fresh</code>) or your live WIP HEAD (<code>head</code>).</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-routines\">Claude Code Routines — unattended cloud automation</a> — the saved config that runs <em>itself</em> on Anthropic's cloud on a schedule / GitHub event / API call (the \"operate it for me\" primitive).</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"automation-control-flow-spectrum\">Keeping Claude working — the two axes behind /loop, /goal, hooks, routines &amp; cron</a> — WHERE it runs (open session vs independent) × WHAT starts the next unit of work (clock / condition / event); the frame that tells <code>/loop</code>, <code>/goal</code>, hooks and routines apart.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"progressive-disclosure-instructions\">One giant instruction file fails — progressive disclosure</a> — why a 600-line <code>CLAUDE.md</code> rots (lost-in-the-middle, budget exhaustion) and the entry-file + topic-docs + code-embedded structure that fixes it.</li></ul>\n<h3>🧩 Agent patterns — <em>how you compose</em></h3>\n<p>Reusable shapes for putting primitives together.</p>\n<ul><li><a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a> — the nine-layer production blueprint; what separates a prototype from a real system.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own</a> — the four-altitude decision tree (buy → use → configure → own the loop).</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns &amp; the three failure modes they fix</a> — classify-and-act, fan-out-and-synthesize, adversarial verification, generate-and-filter, tournament, loop-until-done.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"repository-as-system-of-record\">The repository is the agent's only durable memory</a> — \"info not in the repo doesn't exist for the agent\"; ACID state files (<code>AGENTS.md</code>/<code>PROGRESS.md</code>/<code>DECISIONS.md</code>), the fresh-session test, and rebuild-cost as the metric.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">Never let the agent declare its own victory</a> — externalize \"done\": feature-list state machines, three-layer/E2E verification, WIP=1 scope control, and observability inside the harness.</li><li><a href=\"#\" class=\"kc-link\" data-slug=\"designing-loops-not-prompts\">Loop engineering — design the loop, not the prompt</a> — the harness on a timer that feeds itself: five Claude Code blocks (scheduling, worktrees, skills, plugins+MCP, subagents) + on-disk memory. The leverage point moves from writing the turn to designing the system; <em>build the loop, stay the engineer.</em></li><li><a href=\"#\" class=\"kc-link\" data-slug=\"loop-is-cron-plus-a-decision-maker\">A loop is cron plus a decision-maker in the body</a> — the lineage (ReAct → AutoGPT → ralph → <code>/goal</code> → orchestration), the one-line definition, and the 2026 economics: <em>the loop, not the model, is the expensive part</em> (cap iterations, detect no-progress, set a budget).</li></ul>\n<h3>🚀 Products &amp; consulting — <em>what you ship</em></h3>\n<p>Real-world scenarios that pull concepts together for a client outcome.</p>\n<ul><li><a href=\"#\" class=\"kc-link\" data-slug=\"bootstrapping-company-ai-stack\">Scenario: bootstrapping a company's AI stack from zero</a> — discovery questions + phased rollout (buy → wire MCP → codify skills/hooks → build the differentiated slice).</li></ul>\n<blockquote><p>🏗️ These last three notes form a cluster with its own Map of Content — <strong><a href=\"#\" class=\"kc-link\" data-slug=\"building-ai-stacks\">Building &amp; advising on AI stacks</a></strong> — the evolving consulting section.</p></blockquote>\n<h2>The narrative (how it all connects)</h2>\n<p>At the bottom, <strong>foundations</strong> say there are only three things to steer — the prompt (one message), the context (what stays in the window), and the harness (the whole loop). The <strong>Claude API</strong> turns that loop into callable capabilities; the <strong>Agent SDK</strong> wraps those into a framework for building agents; <strong>Claude Code</strong> is the most-polished agent built on it, exposing the loop as nameable <strong>primitives</strong> you can mix. Those primitives <strong>compose into patterns</strong> (routing, orchestration, the reference architecture), and patterns <strong>ship as products</strong> — which, for the consulting goal, means helping a client pick the <em>smallest</em> set of primitives that solves their problem. That \"smallest set\" choice now has its own pattern — the <strong><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">four-altitude decision</a></strong> (buy → use Claude Code → configure the Agent SDK → own the raw API loop) — and its own products-layer scenario, <a href=\"#\" class=\"kc-link\" data-slug=\"bootstrapping-company-ai-stack\">bootstrapping a company's AI stack</a>. Together they're the <a href=\"#\" class=\"kc-link\" data-slug=\"building-ai-stacks\">Building &amp; advising on AI stacks</a> map.</p>\n<p>Read the layers as a debugging ladder too: <em>bad single answer</em> → Foundations (prompt). <em>Model forgetting</em> → Foundations (context). <em>Can't recover / loop / self-check</em> → Foundations (harness) <strong>or</strong> the right Claude Code primitive (a hook, a subagent, a workflow). The spine tells you which floor to get off on.</p>\n<p>There's an emerging top to this ladder: <strong><a href=\"#\" class=\"kc-link\" data-slug=\"designing-loops-not-prompts\">loop engineering</a></strong>. Once the primitives exist as products, you can stop prompting turn-by-turn and instead <em>design the loop</em> — the harness on a timer that does its own discovery, fans work to subagents in worktrees, verifies with a separate model (<code>/goal</code>), and remembers state on disk. It's the same gather→act→verify loop, zoomed all the way out until <strong>you</strong> are no longer in the inner loop — only at the verification gate. It's a 2026 preview, not settled practice: the leverage point moved, but so did the failure modes (token cost, slop, comprehension debt). <em>Build the loop, stay the engineer.</em></p>\n<h2>How this map evolves</h2>\n<p>The <code>/land</code> skill updates this file every time you add a concept: it assigns the note a <code>spine_layer</code>, drops a link under the right heading here, and — when a new idea genuinely shifts the story — rewrites the narrative. <strong>Gaps are features:</strong> every layer now has at least one note — the remaining \"what to learn next\" list lives as the explicit backlog at the bottom of the <a href=\"#\" class=\"kc-link\" data-slug=\"building-ai-stacks\">Building &amp; advising on AI stacks</a> map (the framework landscape, eval, and guardrails notes).</p>"
    },
    "readme": {
      "title": "Knowledge 🧠 — the Home Note",
      "html": "<h1>Knowledge 🧠 — the Home Note</h1>\n<p>This is the entry point to the self-organizing half of the AI Knowledge Center. It's <strong>one connected brain</strong>, not a filing cabinet: atomic notes, wired together with typed links, all hanging off a single <a href=\"#\" class=\"kc-link\" data-slug=\"big-picture\"><strong>Big Picture spine</strong></a>.</p>\n<blockquote><p><strong>Go broad</strong> → read the <a href=\"#\" class=\"kc-link\" data-slug=\"big-picture\">Big Picture</a> or open the <a href=\"https://vikkybharadwaj.github.io/ai_knowledgecenter/concepts/graph.html\" target=\"_blank\" rel=\"noopener\">concept dependency graph</a> — the <strong>tech-stack concepts</strong> (primitives, systems, patterns) wired by <em>what depends on what</em>, so you learn the Claude stack visually, bottom-up. <strong>Go deep</strong> → click any concept for a short explainer plus the <strong>source notes</strong> behind it, and follow their connections wherever they lead.</p></blockquote>\n<h2>Start here</h2>\n<ul><li>🗺️ <strong><a href=\"#\" class=\"kc-link\" data-slug=\"big-picture\">The Big Picture</a></strong> — the spine: how the whole Claude stack fits together (the dot-connecting map).</li><li>🏗️ <strong><a href=\"#\" class=\"kc-link\" data-slug=\"building-ai-stacks\">Building &amp; advising on AI stacks</a></strong> — the consulting cluster: <em>when to use Claude Code vs build your own harness</em>, and how to bootstrap a company's AI stack.</li><li>🕸️ <strong><a href=\"https://vikkybharadwaj.github.io/ai_knowledgecenter/concepts/graph.html\" target=\"_blank\" rel=\"noopener\">Concept dependency graph</a></strong> — the tech-stack concepts and what each one depends on, laid out by spine layer (the notes below are its sources).</li><li>📒 <strong><a href=\"#\" class=\"kc-link\" data-slug=\"git-glossary\">Git glossary</a></strong> — plain-English definitions of the git/GitHub terms (commit, branch, push, PR, worktree…) used to maintain this base.</li></ul>\n<h2>How this works (the model in 5 lines)</h2>\n<ol><li><strong>Flat pool.</strong> Every note is one atomic idea in <span class=\"kc-ref\"><code>notes/</code></span> — no topic folders, no 101/201/301 levels.</li><li><strong>Typed connections.</strong> Notes link with <em>labeled</em> relationships (<code>builds-on</code>, <code>enables</code>, <code>part-of</code>, <code>used-with</code>, <code>alternative-to</code>, <code>contrasts-with</code>, <code>used-in</code>) — the wiring of the brain.</li><li><strong>The spine.</strong> Each note declares a <code>spine_layer</code> so it slots into the <a href=\"#\" class=\"kc-link\" data-slug=\"big-picture\">Big Picture</a> — nothing floats free.</li><li><strong>Emergent maps.</strong> <span class=\"kc-ref\"><code>maps/</code></span> holds indexes that appear <em>only when a cluster earns one</em> — never pre-built.</li><li><strong>Land &amp; connect.</strong> Run <code>/land &lt;url | file | pasted text&gt;</code>; it distills, files, <strong>connects the dots</strong>, updates this map, and teaches you where the idea fits.</li></ol>\n<h2>All notes</h2>\n<p><em>(This list is the human index; the <a href=\"https://vikkybharadwaj.github.io/ai_knowledgecenter/concepts/graph.html\" target=\"_blank\" rel=\"noopener\">graph</a> is the visual one. Both are derived from the notes.)</em></p>\n<table><thead><tr><th>Note</th><th>Layer</th><th>Depth</th></tr></thead><tbody><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"prompt-context-harness-engineering\">Prompt vs Context vs Harness Engineering</a></td><td>foundations</td><td>evergreen</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"harness-vs-context-engineering\">Harness vs Context vs Prompt Engineering</a></td><td>foundations</td><td>evergreen</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"anatomy-of-an-agent-harness\">The Anatomy of an Agent Harness</a></td><td>foundations</td><td>evergreen</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"harness-engineering-discipline\">Harness engineering — reliability lives outside the model weights</a></td><td>foundations</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"claude-api-agent-primitives\">The Claude API agent primitives</a></td><td>api</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"claude-agent-sdk\">The Claude Agent SDK</a></td><td>agent-sdk</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-architecture\">Claude Code Architecture — field guide</a></td><td>claude-code</td><td>evergreen</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"model-context-protocol\">Model Context Protocol (MCP)</a></td><td>claude-code</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-multi-agent-development\">Claude Code — Multi-Agent Development</a></td><td>claude-code</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflows-and-ultraplan\">Dynamic Workflows &amp; the Ultraplan loop</a></td><td>claude-code</td><td>evergreen</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"execution-context-isolation\">One shared context vs. many isolated contexts</a></td><td>claude-code</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"worktree-base-ref-fresh-vs-head\">worktree.baseRef — fresh vs head</a></td><td>claude-code</td><td>seedling</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-routines\">Claude Code Routines — unattended cloud automation</a></td><td>claude-code</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"progressive-disclosure-instructions\">One giant instruction file fails — progressive disclosure</a></td><td>claude-code</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"agentic-ai-reference-architecture\">Agentic AI Reference Architecture</a></td><td>patterns</td><td>evergreen</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"dynamic-workflow-patterns\">Six dynamic-workflow patterns &amp; three failure modes</a></td><td>patterns</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"repository-as-system-of-record\">The repository is the agent's only durable memory</a></td><td>patterns</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"completion-is-externalized\">Never let the agent declare its own victory</a></td><td>patterns</td><td>budding</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"designing-loops-not-prompts\">Loop engineering — design the loop, not the prompt</a></td><td>patterns</td><td>seedling</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"claude-code-vs-build-your-own\">Use Claude Code vs build your own</a></td><td>patterns</td><td>evergreen</td></tr><tr><td><a href=\"#\" class=\"kc-link\" data-slug=\"bootstrapping-company-ai-stack\">Scenario: bootstrapping a company's AI stack</a></td><td>products</td><td>budding</td></tr></tbody></table>\n<blockquote><p>Looking for the <strong>CCA-F exam prep</strong>? That's the other view — <span class=\"kc-ref\"><code>exam-prep/</code></span> + the <a href=\"https://vikkybharadwaj.github.io/ai_knowledgecenter/concepts/exam.html\" target=\"_blank\" rel=\"noopener\">interactive exam-prep site</a>.</p></blockquote>"
    }
  }
};
